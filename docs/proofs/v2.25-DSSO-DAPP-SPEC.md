# v2.25-DSSO-DAPP-SPEC.md — "Sign-In With Determ": the mutual-distrust identity provider (threshold-OPAQUE)

**Status:** AUTHORITATIVE for the DSSO DApp (V1.1-PLAN Bundle A). Single DSSO mechanism doc under the B3 consolidation program. Realizes the design of *Identity provider in an environment of mutual distrust* (academia.edu/80188125) with two owner-chosen improvements — **OPAQUE in place of the paper's SRP**, and a **t-of-n, unordered** threshold in place of the paper's sequential all-node chain. **No FROST, no threshold signature, no block-co-signed assertion** — the paper issues its relying-party token by hash challenge-response over handshake-co-generated keys, so no signature scheme is involved at all.

**Authority:** Stoyan Denev — owner decision: drop FROST, keep the paper's design. This spec supersedes the 2026-07-15 "DLT-B" text, which had re-engineered the assertion into a block-anchored / credential-signature form the paper does not use. Recorded: `DECISION-LOG.md` 2026-07-20.

**Chain surface:** none. DApp-layer protocol over already-shipped primitives; the v1.1 consensus path and the no-migrations lock are untouched.

---

## 0.0 Security findings (2026-07-28, round-11 post-ship audit) — ALL THREE CLOSED as of 2026-09-17

A post-ship adversarial audit of the shipped DSSO surface (`wf_f1ca5dcc`, each finding independently verified; the first by an **executed attack**) found three defects. **All three are now CLOSED**: finding 1 on 2026-07-28, and claims **C2** and **C6** on 2026-09-17, each reproduced against the shipped code before it was fixed and each gated with falsify-on-mutant evidence. The C2 and C6 fixes were owner-gated because they change this ratified spec; both are authorized by the owner's 2026-09-17 directive to develop DSSO toward an eIDAS outcome (DECISION-LOG 2026-09-17). Read §0.0(2) and §0.0(3) for what now holds and on what boundary.

1. **CLOSED (`e6bad81`).** §5(ii) bounded `iat` only from below, so a **future-dated** claim passed every freshness leg (the `exp − iat ≤ T_max` guard is nullified by shifting the whole window forward) and replayed indefinitely once the RP evicted its nonce. `DssoAssertionFreshness.md` §5 already *claimed* future-dated claims are rejected; the upper bound `iat > now + skew → reject` now makes that true, gated falsify-on-mutant by **E2E-7b**. §5(ii) below should be read as the **two-sided** window.

2. **CLOSED 2026-09-17 — claim C2 (§6) now holds, on a named trust boundary.** *The defect, as found:* the envelope of §3 step 3 was `AEAD_{HKDF(y)}(cred_sk)` with **AAD = NULL** — carrying no `server_public_key` and no identities — the AKE transcript (`hash_preamble`) omitted **both** static public keys, and the client's `pk_s` was a bare caller argument. So `pk_s` reached the client from nowhere trustworthy: an attacker knowing only the victim's **public** `pk_c` picked her own `(sk_s′, esk_s′)`, computed all three client DH values, emitted a valid `server_mac`, and the client accepted — **full IdP impersonation and a shared `sso_key`**, the attacker never recovering `sk_c`. Before fixing it the attack was **re-executed against the shipped `determ_opaque3dh_server`/`_client`** (not merely re-read from the record): the honest client returned `server_mac_ok == 1` and both sides agreed on `session_key`.

   *The correction* is RFC 9807 §4.1.1, which the module had been missing. The AKE transcript now carries `CleartextCredentials{server_public_key, client_public_key, server_identity, client_identity}` under its own `DTM-DSSO-CLEARCRED-v2-` tag; the preamble — hence every MAC and the session key — commits to it; the separate `pk_s`/`pk_c` **arguments are removed**, so a static key can enter a party only through that one MAC-covered transcript field; and **the same bytes are the AAD** under which the §3-step-3 envelope is sealed, so the envelope tag and the transcript MAC commit to one and the same block.

   *What now holds:* the client accepts the server **iff** the peer holds `sk_s` for the `pk_s` the client anchored **and** ran the identical transcript. A party that does not hold `sk_s` is rejected (`server_mac_ok == 0`) and derives no shared key. *What is assumed:* that the client's anchored `pk_s` is authentic — binding is not trust. **Trust boundary, stated plainly.** (i) *At every login after enrolment:* `pk_s` and the identities are the envelope's AAD, so opening the envelope requires the user's password **and** ≥ `t` OPRF responses (the C1/C3 assumption this design already makes); a substituted `pk_s` fails the AEAD tag and the login **aborts before the AKE**, exactly like a wrong password. (ii) *At enrolment:* the authentic `pk_s` must come from the DSSO registration record on chain, resolved through the committee-authenticated light-client chain of custody — genesis pin (`determ::light::anchor_genesis`, `build_genesis_committee`) → per-block K-of-K committee signatures and registry tracking (`determ::light::verify_chain_to_head`) → the committee-**bound** state root (`determ::light::committee_bound_state_root`) → a state proof (`determ::light::read_account_trustless`), with `determ::light::collect_d5_streams` the shipped pattern for reading DAPP_CALL records completely. **Named residual: no DSSO-specific `pk_s` resolver exists in `light/` today** — those functions are the components such a resolver must be built from, not the resolver, so until it ships the enrolment anchor is a requirement on the deployment, not shipped code. An attacker who controlled enrolment owns the account from the start; an attacker who controls only the network can still deny service, observe metadata and guess online (§6), but can no longer be accepted as the IdP. *Rotation:* the DSSO service key is registered to the service domain, not to a committee member, so committee rotation does not rotate `pk_s`; rotating `sk_s` requires re-sealing every user's envelope (the §8 re-deal), and until a user re-seals, a new `pk_s` fails closed at that user's AEAD tag rather than silently taking effect.

   *Permanence.* The domain tags and the encoding are frozen once a deployment exists. **None exists**, so no login transcript had to reproduce and the v1 encoding was **replaced in place** rather than kept beside a v2: a retained v1 would be dead code and a downgrade target — it *is* the impersonable construction. The tags nonetheless moved (`DTM-DSSO-OPAQUEv1-`/`-OPAQUE3DH-v1-` → `…v2-`) so the two encodings can never be confused, the v1 KAT is **retired**, and the v2 bytes are frozen under the same rule. Gated by `determ test-dsso-opaque3dh` (43 assertions, C2-a…C2-g) + `determ test-dsso-login-e2e` (E2E-10), dual-oracle KAT re-derived python-first in `tools/verify_opaque3dh.py`; mutants M1–M6 (drop `pk_s`, drop `pk_c`, drop the identities, bind server-side only, weaken the MAC compare, restore the v1 preamble wholesale) and M7 (re-null the envelope AAD) each RED.

3. **CLOSED (2026-09-17).** Claim C6 was FALSE twice over, and both halves were reproduced before the fix (an isolated harness transcribing the shipped `rp_accept` verbatim; see the DECISION-LOG 2026-09-17 entry). (a) The stateless §5 accept rule verified only that the presenter knew `tenant_key` over an `H1'` of its **own choosing** — `sso_key` never entered verification — so any `tenant_key` holder (which §5 put in *every user's* hands) minted a token for any subject. (b) The freshness legs were evaluated on a **cleartext claim the RP could not authenticate** (it holds no `sso_key`, so it could not recompute `H1'`), so a legitimate user re-presented the same `(H1', H2)` bytes under a substituted claim — different `sub`, fresh nonce, rewritten `iat`/`exp` — and was accepted as another subject. Reproduction also showed the e2e harness's "sounder" reference rule closes (a) but **not** (b), because its accept predicate still reads no field of the claim. **The fix** rewrites §5 below and ships it as a module (`dapps/dsso/assertion.{h,c}`, gate `determ-dsso selftest-assertion`): the claim goes INSIDE the outer MAC, the user presents no tag at all, and the RP accepts only a tag the IdP delivered over the registered channel — so verification depends on the login's `sso_key` and on the IdP's own claim. The §1-vs-§5 `tenant_key` custody contradiction is resolved in §1's favour: **`tenant_key` is held by the IdP and that one RP, never by a user**, and the §5 sentence that had `U` compute the outer leg is gone. `determ test-dsso-assertion`, which asserted the unsound rule as "C6 correctness", is RETIRED. *(Owner-authorized 2026-09-17; DECISION-LOG 2026-09-17.)*

## 0. The paper's design, and the two improvements

The paper builds a federated identity provider from **K mutually-distrusting servers**: a user authenticates to the *set*, and no single server (below the trust threshold) learns the user's password or can impersonate the IdP. The paper's concrete construction is (a) an **SRP** augmented-PAKE whose verifier material is split across the servers, (b) evaluated as a **sequential hash chain** (server A -> B -> ... -> G, every server required, in order), (c) issuing the relying-party token by a **dual-hash challenge-response**: the PAKE handshake co-generates a shared key, and the RP accepts iff `H2 == H2'` where `H2 = H(tenant_key, H1')`.

Determ maps the K servers onto its **K-of-K committee** and adopts the paper's framework verbatim except for two owner-chosen improvements:

1. **OPAQUE in place of SRP.** OPAQUE (CFRG, RFC 9807-track) is the modern UC-secure aPAKE: OPRF blinding hides the password from every server, and the envelope adds precomputation-resistance against a stolen verifier — properties SRP lacks. The paper's framework is PAKE-agnostic, so the substitution is drop-in.
2. **t-of-n and unordered in place of the sequential all-node chain.** The paper needs every server, in a fixed order — a latency chain and a single point of failure. Determ thresholdizes the OPRF (Shamir, **t-of-n**): the user blinds the password, **broadcasts** it, collects **any t** blinded evaluations **in any order**, and recombines by Lagrange interpolation. This is exactly the "not all nodes needed, no predefined order" property.

**FROST is not part of this design and never was.** The paper issues its token by hash challenge-response over co-generated keys, not by a server signature — so there is no threshold-signature step to implement, and nothing here composes with FROST.

## 1. Roles

- **Servers O_1..O_n** — the K-of-K committee members act as the IdP servers. (A deployment MAY register an opt-in server set instead; recorded in DAPP_REGISTER.)
- **User U** — holds a password `pw` and an OPAQUE credential.
- **Relying party (RP)** — an off-chain service; registers via v2.18 DAPP_REGISTER, establishing shared `tenant_key` material with the IdP. Challenges/responses and the IdP→RP assertion records ride v2.19 DAPP_CALL.
- **`tenant_key` custody (normative).** `tenant_key` is held by **exactly two principals: the IdP and the one RP that registered it**. It never reaches a user, a wallet, the chain or any third party. It is a symmetric MAC key, so a holder can compute every value an RP-local rule accepts; it is also the RP's identity on the registration channel. Registration establishes it as `tenant_key = HKDF-SHA-256(registration secret, info = "determ-dsso/tenant-key/v1" ‖ rp_id ‖ u64(key_epoch))`. Two epochs govern its life: `key_epoch` is bumped on every key rotation (an assertion naming any other `key_epoch` is rejected `DSSO_E_TRUST`), and `reg_epoch` is bumped only when the relationship is re-established, which also rotates the pairwise subjects of §5. An earlier draft of §5 had `U` compute `H2 = H(tenant_key, H1')`, which contradicted this line and was the direct cause of defect C6(a) — §0.0(3); that sentence is gone.
- **Threshold t** — how many servers must respond. Default **t = n-1** (tolerates one down/dishonest server; below-t collusion cannot recover the password). Per-deployment; recorded at registration.

## 2. Primitives — all already shipped

| Primitive | Role | Ref |
|---|---|---|
| P-256 RFC 9497 OPRF/VOPRF | the threshold OPRF substrate (prime-order group + hash-to-curve + DLEQ) | CRYPTO-C99 §3.9b |
| Shamir over the P-256 scalar field | user-dealt t-of-n shares of the OPRF key | shipped (`test_wallet_shamir*`) |
| OPAQUE envelope (AEAD + HKDF) | seals the user credential; unsealed by the OPRF output | ChaCha20-Poly1305 / AES-GCM + HKDF (§3.1/§3.4/§3.5) |
| SHA-256 / HMAC / HKDF | the dual keyed-hash challenge-response (`binder`/`tag`), the pairwise-subject derivation, and key derivation | §3.1 |
| Ed25519 | user credential key; committee identity keys | §3.2 |
| v2.18 DAPP_REGISTER / v2.19 DAPP_CALL | RP registration; challenge/response transport | shipped |
| Light-client committee resolution | RP resolves the on-chain committee (IdP server) set | shipped |

Zero new primitives, zero new hardness assumptions. The threshold OPRF runs on the shipped P-256 RFC 9497 stack because that is the shipped stack with the required group operations (point add, scalar inverse, hash-to-curve); X25519 lacks them. This is an implementation substrate, not a change to the paper's design.

## 3. Registration

1. U picks `pw` and generates an OPAQUE credential (a fresh **P-256** keypair `cred = (sk_c, pk_c)` — the DSSO identity is separate from the on-chain Ed25519 identity; single-curve P-256 is what the OPAQUE-3DH AKE requires; owner decision 2026-07-21, DECISION-LOG, supersedes the earlier "Ed25519 keypair" text that predated the RFC-9807 AKE choice).
2. U computes the OPRF key `k` and **deals it as t-of-n Shamir shares** `k_i` to the servers (user-as-dealer — no DKG, no server-side ceremony; an inconsistent dealing can only self-DoS U's own account). Each server stores its `k_i`; the share commitments `PK_i = k_i·G` are published so responses can be checked (§4).
3. `y = OPRF_k(pw)` computed locally; `envelope = AEAD_{HKDF(y)}(cred_sk ; AAD = CleartextCredentials)` where `CleartextCredentials = {server_public_key, client_public_key, server_identity, client_identity}` (RFC 9807 §4.1.1) — the byte-identical block the §4-step-5 AKE transcript binds. The envelope is stored (client-side and/or on-chain). The AAD is what makes the `pk_s` the client later anchors AUTHENTIC: opening the envelope needs `pw` and >= t OPRF responses, so a substituted `pk_s` fails the AEAD tag and the login aborts before the AKE (§0.0(2)).
4. RP registration (once per RP): v2.18 DAPP_REGISTER establishes the RP's identity and shared `tenant_key` material with the IdP.

## 4. Login (t-of-n threshold OPRF — unordered)

1. U blinds: `r` random, `B = r·H2C(pw)`.
2. U **broadcasts** `B` to the servers — no ordering, no server-to-server communication, no per-server state beyond the stored share.
3. Each responding server returns `Z_i = k_i·B` with a DLEQ proof `pi_i` (shipped P-256 VOPRF) that `Z_i` is consistent with its published `PK_i`.
4. On **any t** valid responses, in **any order**: U discards any response whose `pi_i` fails, Lagrange-combines `Z = sum(lambda_i·Z_i) = k·B`, unblinds `N = r^{-1}·Z = k·H2C(pw)`, finalizes `y`.
5. `y` unseals the envelope -> `cred`. The OPAQUE handshake **co-generates a shared session key** between U and the IdP (the paper's "co-generation of a common key"). No server saw `pw`; below-t servers cannot combine.

## 5. Assertion to the RP (the paper's dual keyed hash — no signature)

**This section was rewritten on 2026-09-17 to close defect C6 (§0.0(3)); it is implemented, not merely specified, by
`dapps/dsso/assertion.{h,c}` and gated by `determ-dsso selftest-assertion`.** The construction is still the paper's:
an inner keyed hash under the login's co-generated key, an outer keyed hash under the RP's registration key. No
signature, no FROST, no block co-sign, no new primitive — HMAC-SHA-256 throughout.

### 5.1 The claim, and its canonical encoding

The IdP asserts a SIWE-class claim
`(iss, sub, aud, sid, nonce, iat, exp, reg_epoch, key_epoch)`, where `sub` is the **pairwise** subject of §5.4,
`aud` the intended audience, `sid` the request/session id the RP opened for this login, and `nonce` a 32-byte
single-use value. Its canonical encoding is unambiguous — fixed field order, every variable-length field prefixed
by a big-endian u64 length, the whole prefixed by a domain tag:

```
canon(claim) = LP("determ-dsso/assert/claim/v1")
             ‖ LP(iss) ‖ LP(sub) ‖ LP(aud) ‖ LP(sid)
             ‖ nonce(32) ‖ u64(iat) ‖ u64(exp) ‖ u64(reg_epoch) ‖ u64(key_epoch)
where LP(x) = u64be(len x) ‖ x
```

### 5.2 The two legs

```
binder = HMAC-SHA256( sso_key,    LP("determ-dsso/assert/binder/v1") ‖ canon(claim) )
tag    = HMAC-SHA256( tenant_key, LP("determ-dsso/assert/tag/v1")    ‖ canon(claim) ‖ binder )
```

This is the paper's `H2 = H(tenant_key, H1')` with `H1' = H(sso_key, challenge)`, with the two corrections C6 forced:
the "challenge" **is** the canonical claim, so the inner leg commits to every field the RP will act on; and the outer
leg's message is `canon(claim) ‖ binder`, **not `binder` alone**, so the value the RP checks commits to the claim as
well as to the login.

### 5.3 Who computes what, and the accept rule

- The **IdP** holds `sso_key` (from the §4-step-5 handshake), `tenant_key` (§1) and the per-user `user_root` (§5.4);
  it computes `sub`, `binder` and `tag`. **`U` computes no outer leg and never holds `tenant_key`.**
- **IdP → RP**, over the registered v2.19 DAPP_CALL channel: the **reference `tag`**. The RP retains it in a bounded
  table for `T_max + skew` from the claim's `iat`.
- **IdP → U → RP**, front channel: the user is given, and presents, `(claim, binder)`. **The user is given no `tag`
  and presents none.**

> **Accept rule.** The RP accepts iff (i) the claim's `key_epoch` and `reg_epoch` are its current ones; (ii)
> `HMAC(tenant_key, LP(DS_TAG) ‖ canon(claim) ‖ binder)` recomputed over the PRESENTED claim and binder equals, in
> constant time and over the full 32 bytes, one of the reference tags the IdP delivered to this RP and has not yet
> expired; (iii) the authenticated `sid` is the session THIS verifier is completing; (iv) `iat ≤ now + skew`,
> `exp > now` and `exp − iat ≤ T_max`; and (v) the claim's `nonce` has not been accepted before within its retention
> window.
>
> Leg (iii) is not redundant with the `sid` inside the MAC. The MAC keeps the IdP's statement about `sid` honest; the
> comparison is what stops a party who influenced WHICH `sid` was asserted from fixating someone else's session with a
> token perfectly valid for its own — login-CSRF. The session id the verifier compares against is the one the RP itself
> opened, never a value read from the presentation. Correspondingly the IdP MUST take `aud` and `sid` from its own
> state and never from an untrusted party: the module authenticates whatever it is asked to assert.

Why it must be this and not something simpler: an accept rule that is a pure function of `tenant_key` and
presenter-supplied bytes can always be satisfied by a `tenant_key` holder — it need only run the honest minting
algorithm with an `sso_key` of its own. That was the old rule, and it is exactly defect C6(a). The only escapes are an
asymmetric signature (forbidden by §7) or an accept rule that consults per-login state the IdP delivered. §5 takes the
second, which is what "the RP computes `H2'` **from the same material**" always had to mean.

**No cleartext field of an unauthenticated claim is compared against anything.** The verifier authenticates first and
reads the claim afterwards; that is why the MAC covers `aud`, `sid`, `sub`, `iss` and both epochs. The epoch check is
the one unavoidable exception, because it selects *which key* the verifier is entitled to verify under.

**Fail-closed, with a named `dsso_status` for every rejection:** a missing or over-long input is `DSSO_E_ARG`; an
unreadable clock (`now == 0`) is `DSSO_E_UNAVAILABLE`; an unknown relying party, a rotated-out `key_epoch` or a previous
`reg_epoch` is `DSSO_E_TRUST`; a tag matching no live reference is `DSSO_E_CRYPTO`; a clock-leg failure is
`DSSO_E_EXPIRED`; a seen nonce is `DSSO_E_REPLAY`; a nonce cache with no free or expired slot is `DSSO_E_UNAVAILABLE`
— the verification is REJECTED rather than accepted without memory, because accepting while unable to remember is
accepting a replay. Nothing is written and no table mutated on any rejected path.

### 5.4 Pairwise subject identifiers

```
sub = HMAC-SHA256( user_root, LP("determ-dsso/assert/pairwise-sub/v1") ‖ LP(rp_id) ‖ u64(reg_epoch) )
```

`user_root` is a 32-byte per-user secret the **IdP alone** holds, fixed at enrolment, never on the chain and not derived
from `sso_key` (which is per-login and would make the subject unstable). The result is DIFFERENT for each relying party
— two RPs comparing subjects learn nothing, since the key is one neither holds — and the SAME for one relying party
across every login. The IdP derives it; it is never an input a caller supplies.

**On re-registration** (`reg_epoch` bumped) the subject **changes** and the RP must re-link its accounts. That is the
fail-safe default: a re-registration means the relationship was re-established — a new operator, a post-compromise
re-enrolment — and carrying the identifier across would hand the new holder the previous holder's linkage to every
user. An operator wanting account continuity rotates `key_epoch` and keeps `reg_epoch`.

### 5.5 Bounded state at the RP

Both RP tables are fixed-size arrays, never heap-grown and never proportional to attacker input: a nonce cache and a
reference table. An entry is retained until `iat + T_max + skew`, which is at least `exp + skew` for every claim that
passed the clock legs — so an entry is forgotten only once the clock alone already rejects any token carrying it. On
insert the module takes an empty slot, else a slot whose retention has elapsed; **it never evicts a live entry**, and a
table with no free or expired slot is a rejection. Sizing is a deployment parameter: the nonce cache must exceed peak
ACCEPTED logins per retention window, and only assertions the IdP actually issued can consume a slot.

An AUTHENTICATED party can wedge a verifier by completing enough real logins to fill the nonce cache inside one
retention window, after which honest logins fail closed until entries expire. That is the price of never forgetting a
live nonce, and it is the right side of the trade — the alternative accepts replays. The levers are deployment ones:
size the table above peak, and apply the per-account login rate limit §6 already requires. A per-subject slot quota
would bound it further and is not implemented.

**Residuals, recorded rather than papered over.** The `binder` is a bearer secret in transit — front-channel
confidentiality is the deployment's (TLS or the AKE-derived channel), and the single-use nonce, `T_max` and the `sid`
binding bound what a stolen one is worth. The IdP→RP reference channel is authenticated by the registration, not by
this module; an adversary who can write to it *is* the IdP for that RP, which no symmetric construction can improve on.
A compromised IdP can assert anything — the mutual-distrust property is a property of the *login* (C1/C3/C4), not of
the assertion.

*Optional, off by default:* a deployment wanting a public login audit trail MAY additionally emit a DAPP_CALL record of
`(sub, aud, iat)` — an audit anchor only, not part of the authentication, and not the assertion mechanism.

## 6. Security claims -> backing

| # | Claim | Backing |
|---|---|---|
| C1 | <= t-1 colluding servers learn nothing about `pw` beyond online guessing | Shamir (information-theoretic) + threshold 2HashDH OPRF (JKK 2014; TOPPSS threshold form JKKX 2017), Gap-OMDH/ROM; RFC 9497 P256-SHA256, KAT-gated (§3.9b) |
| C2 | ✅ **Re-established 2026-09-17 — see §0.0(2).** No single server (below t) can impersonate the IdP | The AKE authenticates **the server that holds `sk_s`**: the transcript binds `CleartextCredentials{server_public_key, client_public_key, server_identity, client_identity}` (RFC 9807 §4.1.1), the static keys enter only through that MAC-covered field, and the same bytes are the envelope AAD — so a party without `sk_s` is rejected and derives no shared key. The old backing ("the shared key needs the user's OPAQUE credential") was disproved by an executed attack and is NOT what carries the claim. **Residual:** the client's trust in `pk_s` rests on the envelope AAD at login (password + >= t OPRF responses) and, at enrolment, on the on-chain DSSO registration record via the committee-authenticated light-client read — for which no DSSO-specific resolver exists in `light/` yet |
| C3 | Offline dictionary attack needs >= t shares | Shamir + OMDH |
| C4 | A byzantine server cannot corrupt the login undetected | Per-response DLEQ vs the published `PK_i` (RFC 9497 VOPRF soundness) |
| C5 | OPAQUE upgrades over the paper's SRP | precomputation-resistance + verifier-secrecy (OPAQUE, JKX 2018) |
| C6 | ✅ **Re-established 2026-09-17 (§5 rewritten, §0.0(3)).** RP assertion soundness: a party that did not complete the login cannot produce an accepted token, and the RP cannot act on a claim other than the one the IdP asserted | The rewritten §5 rule. The accepted value is `HMAC(tenant_key, DS_TAG ‖ canon(claim) ‖ binder)` compared against a reference the IdP delivered; by HMAC's PRF and collision resistance the only `(claim, binder)` reaching a delivered reference is the IdP's own, whose `binder = HMAC(sso_key, ·)` requires the login key. Claim substitution dies on the same comparison because `canon(claim)` is inside the MAC. Gated falsify-on-mutant by `determ-dsso selftest-assertion` (61 assertions; 7 mutants — restoring the unsound rule, dropping `sso_key`, dropping the audience, dropping the session id, skipping the nonce insert, a constant pairwise subject, a truncated compare — each turns a named leg RED). The old backing ("keyed-hash challenge-response over PAKE-authenticated keys") was true of the CONSTRUCTION and false of the shipped ACCEPT RULE; that gap is what §0.0(3) recorded |
| C7 | Liveness: any t honest reachable servers + the transport complete a login | protocol structure; unordered by construction |

Online guessing is metered as in the paper (per-account attempt limits); with t-of-n the cap must be aggregate since an attacker can rotate subsets. **This is no longer a deployment choice — it is IMPLEMENTED** (`dapps/dsso/authn.c`, gate `determ-dsso selftest-authn`): a per-account grow-only counter with one slot per server (a state-based G-Counter CRDT — each server increments only its own slot, views merge by per-slot maximum, so the set converges by gossip with **no consensus rule and no new primitive**), capped on the **sum** of the slots. Because a login needs `t` servers to serve, `A` attempts consume at least `A·(t − b)` counter units when `b < t` of the serving servers refuse to count, giving `A ≤ floor(cap / (t − b))` for **every** rotation of subsets; the gate pins `n = 5, t = 3, cap = 12` → exactly 4 attempts, then `DSSO_E_RATELIMIT`, whichever of the ten 3-subsets is used. A server whose view is stale beyond `merge_max_age` refuses to serve rather than serve blind (fail-closed), counters never leak between accounts, and a successful two-factor login clears the budget. At `b >= t` the adversary holds an evaluation quorum and can evaluate offline — no online limiter bounds that, and C1/C3 is what excludes it. Derivation and bound: `DssoAuthenticationAssurance.md` §6.

**§6b Authentication factors (added 2026-09-17).** Everything above is ONE factor. `y`, `cred_sk`, `sso_key` and the §5 token are all functions of the password, so the login as specified in §3-§5 is **single-factor** and cannot be equivalent to level *substantial* of CIR (EU) 2015/1502 (Annex §2.2.1 wants two factors from different CATEGORIES; n servers are one factor evaluated in a distributed way). A **device-resident P-256 possession factor** is now implemented alongside it — generated on the user's device, never transmitted, not derivable from the password or from anything the servers store, and required to answer a fresh challenge binding the login session, the server set and a timestamp at every login. See `DssoAuthenticationAssurance.md` for the factor analysis, the enrolment/recovery/revocation state machine, the assurance the module reports to this section's assertion layer, and the blunt list of what is not claimed.

## 7. Explicit non-goals

- **No FROST / threshold signature / DKG** — the paper co-signs nothing; the OPRF key is user-dealt.
- **No block-anchored assertion** — the token is the paper's hash challenge-response (an earlier draft's block-anchoring is dropped).
- **No post-quantum claim; no assertion-traffic anonymity; no phishing defense** — out of scope.

## 8. Recovery / rotation

- Password change / share refresh / server churn: U re-deals fresh shares + envelope (user-as-dealer; no DKG), authorized under U's chain identity key.
- **Authentication-factor loss and recovery — IMPLEMENTED 2026-09-17** (`dapps/dsso/authn.c::dsso_authn_recover`, gate `determ-dsso selftest-authn`). The binding rule is that evidence of ONE factor may only REDUCE an account's assurance; restoring it needs a SECOND independent evidence, which is a fresh, single-use, subject-matched PID presentation. Device lost with the password known, or password forgotten with the device held, each degrade the account to a LOW state that is visible to the §5 assertion layer (`dsso_authn_assertion_authorize` refuses a LOW session for a relying party that requires substantial) and each is restored to substantial only by knowledge-or-possession **plus** a PID presentation; both lost is rebuilt by a PID presentation alone, which is enrolment re-run under a fresh proofing event. Revocation bumps an `auth_epoch` that every session carries, so every live session stops verifying at once; adding a device deliberately does not. A password alone can never enrol a device. Full state machine and refusals: `DssoAuthenticationAssurance.md` §5.
- Chain-identity key loss/compromise: v2.26 ROTATE_KEY. **Status verified 2026-09-17: NOT SHIPPED** — there is no `ROTATE_KEY` slot in `TxType` (`include/determ/chain/block.hpp`), no payload codec and no apply path; it exists as design text only (`PHASE2-PRIMITIVES-KICKOFF.md` §1 records it as "0% built today"). It remains an unmet precondition for production, and nothing in the authentication-factor recovery above depends on it.

## 9. Green gates before Bundle A merge

- **G1** t-of-n identity: Lagrange over every t-subset == direct OPRF evaluation.
- **G2** per-response DLEQ reject paths (tampered response rejected).
- **G3** envelope AEAD/HKDF KATs.
- **G4** end-to-end: register -> login (t-of-n; one server down; one byzantine) -> dual-hash assertion -> RP accepts.
  - *G4 login half* — SHIPPED (`test-dsso-threshold-oprf`): the fault-tolerant t-of-n login (n=5, t=3, one crash + one byzantine, DLEQ filter load-bearing).
  - *G4 assertion half* — SHIPPED AS A MODULE (`dapps/dsso/assertion.{h,c}`, gate `determ-dsso selftest-assertion`, `tools/test_dsso_assertion_module.sh`, FAST): the rewritten §5 rule, claim C6 — issue at the IdP, verify at the RP against the IdP-delivered reference, pairwise subjects, the three clock legs and a bounded single-use nonce cache with a stated eviction rule. The earlier harness `test-dsso-assertion` asserted the pre-fix accept rule as "C6 correctness" and is RETIRED (2026-09-17); it gated a lambda inside a test block, not a shipped rule.
  - *G4 AKE core* — SHIPPED (`test-dsso-opaque3dh`, DSSO G4 inc.1; C2 binding added 2026-09-17): the §4-step-5 OPAQUE-3DH handshake that co-generates `sso_key` — both parties derive the SAME session key from the 3DH + transcript, transcript-MAC mutual auth, the RFC 9807 §4.1.1 `CleartextCredentials` binding of both static public keys and both identities (§0.0(2)), dual-oracle KAT byte-frozen. Owner chose **OPAQUE-3DH (RFC 9807 §6.4)** for the AKE (DECISION-LOG 2026-07-21). Zero new primitive (P-256 mult + HKDF + HMAC); see `CRYPTO-C99-SPEC.md` §3.26 + `DssoThresholdOprfSoundness.md` §7.
  - *G4 end-to-end (COMPLETE)* — SHIPPED (`test-dsso-login-e2e`, DSSO G4 inc.2 + inc.3, 18 assertions): the full register → t-of-n login → OPAQUE-3DH AKE → **RP dual-hash assertion** flow with the fresh P-256 credential. inc.2 (E2E-1..4): honest login agrees + authenticates (credential recovered via the OPRF), a wrong password aborts before the AKE, a MITM swap of `cred_response` breaks the transcript MAC (falsify-proven), a 1-crash + 1-byzantine login still succeeds. inc.3 (E2E-5..9): the §5-6 dual-hash RP token bound to the login's `sso_key` is accepted, with the RP's normative Option-A freshness enforced (audience + `iat/exp` window + single-use nonce cache) and session binding (replay / expired / stale / wrong-audience / wrong-session all rejected — the session-binding leg falsify-proven). Composes the shipped OPRF/AEAD/HKDF/opaque3dh/HMAC; zero new primitive; see `DssoThresholdOprfSoundness.md` §8. **This is the exact §9 G4 gate ("register → login (t-of-n; one down; one byzantine) → dual-hash assertion → RP accepts").**
  - *Remaining (owner-gated)* — only G5 (constant-time review) + G6 (zeroization) below, once a production ceremony module exists; the functional G4 flow is fully gated.
- **G5** constant-time review of secret scalar paths; **G6** zeroization (`k`, coefficients, `k_i`, `r`, `y`, keys).

## 10. Relation to earlier drafts

The 2026-06-07 "DLT-A" and 2026-07-15 "DLT-B" drafts re-engineered the assertion (block-anchored DAPP_CALL + committee/user signatures) — a Determ-specific mechanism the paper does not use. This spec drops that: the assertion is the paper's dual-hash challenge-response, and the "DLT-A/-B" labels are retired. What carries over from those drafts (and is correct) is the t-of-n OPRF on the shipped P-256 stack. FROST removal is unchanged (`FROST_DEVIATION_NOTICE.md`) — this design needs no threshold signature.

## 11. The OTHER role: DSSO as a WALLET-RELYING PARTY (Reg. (EU) 2024/1183 Art. 5b)

Everything in §§0-10 is DSSO's role as a **private, non-notified identity provider** for its own relying parties: it authenticates a RETURNING user (threshold-OPRF + OPAQUE-3DH) and asserts to an RP (the §5 dual-hash token). DSSO is being built toward a **second** role, and only these two.

**The second role is wallet-relying party.** At **ENROLMENT**, an EUDI Wallet Unit presents **Person Identification Data** to DSSO; DSSO verifies that presentation and binds the subject to a DSSO account. This is a different party, a different protocol and a different trust source from §§0-10, and the two must not be conflated: in §§0-10 DSSO is the thing being trusted, and here DSSO is the thing doing the trusting.

**Where it sits relative to the login.** The PID flow happens **once**, before the §3 registration has anything to be registered against; the §4 login happens every time afterwards. They meet at exactly one place — the ACCOUNT — and nowhere else. No PID attribute, no raw identifier and no presentation byte enters the §4 login path, the §5 assertion path, or the chain. What the login path can learn about an enrolment is that an account carries a binding, and at what assurance level.

**DSSO never claims the wallet's level for its own later logins.** Identity proofing at enrolment is INHERITED from the PID (CIR (EU) 2015/1502 Annex §2.1.2), which the wallet issues under a notified scheme; DSSO records what it verified. DSSO's own authentication is the separate question §§0-10 answer, targeted at EQUIVALENCE TO level substantial via Annex §2.2.1 (two factors from different categories) and §2.3.1 (dynamic authentication). "Equivalent to" is the only phrasing this repository uses; never "certified", "notified" or "compliant".

**What is implemented, and what is not.** The verification and account-binding half is SHIPPED and gated — [`DssoPidVerification.md`](DssoPidVerification.md), gate `determ-dsso selftest-pid` (`tools/test_dsso_pid.sh`, FAST). The format is **SD-JWT VC** with a Key Binding JWT; **ISO/IEC 18013-5 mdoc is NOT implemented**, so an mdoc-only wallet is out of scope until a second increment. Nine separately-testable rules: bounded structure, issuer trust from a configured PID Provider trust anchor list (**ARF OIA_12**), ES256, selective disclosure, holder binding (**ARF OIA_02**), audience, the DSSO-issued nonce, freshness, IETF Token Status List (**fail-closed**), and assurance. Account binding requires the holder's own fresh re-authentication plus a single-use challenge, allows no silent double-binding or re-binding, and stores only a keyed pseudonym — never a raw national identifier or document number.

**NOT implemented, and external.** **OpenID4VP** (OIA_03/OIA_03b/OIA_04) — this module verifies a presentation some transport already delivered. The **wallet-relying-party ACCESS CERTIFICATE** (**RPA_01..RPA_06**) — obtaining it requires registration with a **Member State registrar**, which cannot happen in a repository; nothing in DSSO authenticates DSSO *to* a wallet, and a real deployment needs that certificate before a wallet would answer a request at all. No eIDAS Trusted List (ETSI TS 119 612) is fetched or parsed. Passing the gate is not a compliance claim.

## 12. The requirements mapping — requirement by requirement, what is implemented TODAY

**Verification date: 2026-09-17.** This section is the canonical mapping from the external
requirements DSSO's two roles carry (§11) onto what this repository actually contains. It
exists because every other DSSO document describes a mechanism; none of them answers, in
one place, the question a reviewer asks first — *which obligation is discharged, by what
code, proved by which gate, and which are not discharged at all*.

### 12.0 Read this before the tables

**This mapping is NOT a compliance claim, a conformity assessment, or evidence of one.**
It is an inventory produced by reading the code in this repository against the published
texts named in §12.5. Nine statements are load-bearing and are repeated in the rows
themselves rather than buried here:

1. **A passing gate is never a requirement met.** Every gate in this repository proves
   that the code enforces what the gate asserts. It cannot prove the assertion is the
   obligation, and no test result is a legal conclusion. Where a row cites a gate, the
   gate is evidence about the CODE, nothing more.
2. **"Equivalent to" is never "certified", "notified" or "compliant".** DSSO is not
   notified under Art. 9 (that binds a Member State), not certified under CIR (EU)
   2024/2981, not a wallet or wallet provider, and not a qualified trust service provider.
3. **The wallet-relying-party access certificate and the Member State registration of
   Art. 5b(1) are EXTERNAL and are NOT obtained.** Nothing in this repository
   authenticates DSSO *to* a wallet. A conforming Wallet Unit performs relying-party
   authentication in **every** presentation transaction using an access certificate
   (ARF RPA_03) and, when that authentication fails, tells the user that the identity of
   the relying party could not be verified and that the request is **not trustworthy**
   (RPA_05). So the wallet-relying-party role is, in production terms, **not operable** —
   it is verified but not reachable.
4. **OpenID4VP is NOT implemented, and neither is ISO/IEC 18013-5 mdoc.** There is no
   Authorization Request, no DCQL or `presentation_definition`, no response mode, no
   response encryption, no CBOR and no COSE. `dsso_pid_verify` verifies a presentation
   that some transport already delivered; that transport does not exist here.
5. **No Trusted List — and at ARF v2.9.0 no "List of Trusted Entities" (LoTE) — is
   fetched, parsed or validated.** `dsso_trust_list` is a configured C array. Populating
   it from a Member State's published list — an ETSI TS 119 612 Trusted List, or the
   Topic 31 LoTE publication the current ARF text names — is a deployment act with its own
   unwritten code.
6. **The C2 closure rests on a stated boundary whose enrolment-time resolver does not
   exist.** The AKE now binds `pk_s` into the transcript, so a party without `sk_s` is
   rejected — but that only moves the question to whether the client's ANCHORED `pk_s` is
   authentic. At login the anchor is the credential envelope's AAD; **at enrolment the
   anchor is supposed to be the on-chain DSSO registration record read through the
   committee-authenticated light client, and NO such resolver exists in `light/`**
   (§0.0(2)). An attacker who controls enrolment owns the account from the start.
7. **Identity-proofing assurance is INHERITED from a PID presented once, not conferred on
   every later login.** CIR (EU) 2015/1502 Annex §2.1.2 is satisfied, if it is satisfied
   at all, by the PID Provider's own proofing under a notified scheme. DSSO records what
   it verified at that moment. No later DSSO login re-establishes proofing, and
   `DSSO_LOA_HIGH` is not producible by any path in `authn.c`.
8. **DSSO has no service layer.** Every module ships as fixed-capacity in-memory
   containers a deployment is expected to replace with a database, and there is no HTTP
   surface, no TLS, no persistence, no logging subsystem and no operator tooling. Rows
   that say IMPLEMENTED say it about a MODULE, not about a running service.
9. **The ARF version this mapping is assessed against, v2.9.0, is NOT the current ARF.**
   A later major release exists (§12.5). **The identifiers are not stable across ARF
   releases**, and this was checked rather than assumed: between v2.4.0 and v2.9.0,
   `OIA_03` went from *Empty* to the cryptographic-algorithms requirement, `OIA_12`'s
   trust source changed from a "PID Provider Trusted List" to a "PID Provider **LoTE**",
   and `RPA_02` changed from an obligation on **the Commission** to one on Wallet Units and
   Relying Party Instances; between v2.9.0 and the later release, `OIA_04` becomes empty.
   **An ARF identifier cited without a version number is therefore meaningless**, and every
   row below carries one.

**Status values.** **IMPLEMENTED** — the repository contains code that discharges the
obligation as it applies to this role, and a gate exercises it. **PARTIAL** — part is
implemented and the row names exactly what is missing. **NOT IMPLEMENTED** — there is no
code. **EXTERNAL** — discharging it requires an act outside this repository; the row names
the act and the body that must perform it. Only those four values are used.

Many of the identifiers the mission named bind somebody else — a Wallet Unit, a Wallet
Provider, a qualified trust service provider, a Member State or the Commission. Those rows
are **EXTERNAL**, and each one opens by naming **whom it binds**, so that no reader takes
DSSO's non-implementation of a wallet obligation for a gap in DSSO. They are listed rather
than omitted, because an identifier silently missing from a mapping reads as an oversight.

### 12.1 Regulation (EU) No 910/2014 as amended by Regulation (EU) 2024/1183

Consolidated text of 2024-05-20 (CELEX 02014R0910-20240520), retrieved and read
2026-09-17. **Only Art. 5b binds a wallet-relying party.** Art. 5a binds Member States and
Wallet Providers; Art. 9 binds a notifying Member State; Arts. 24 and 29 bind qualified
trust service providers and the makers of qualified signature creation devices.

**Provenance of the quotations.** Material in *italic quotation marks* below is verbatim
from that retrieval: Art. 5b(1), (3), (8) and the first sentence of (9). The Art. 5b(2)(c),
Art. 5b(9)-second-sentence, Art. 5b(6), Art. 5b(10), Art. 9, Art. 24 and Art. 29 rows are
**summaries**, not quotations — the retrieval returned those paragraphs in summary form —
and are written without quotation marks so the difference is visible.

| Requirement | What it obliges | What the repository implements TODAY | Gate | Status |
|---|---|---|---|---|
| **Art. 5a** — *European Digital Identity Wallets* | Member States to provide EUDI Wallets, and Wallet Providers to build them to the certification requirements of CIR (EU) 2024/2981. | Nothing. DSSO is not a wallet and holds no wallet key, no WSCA and no WSCD. It CONSUMES a wallet's output. | — | **EXTERNAL** — binds Member States and Wallet Providers, not a relying party. The acts outside this repository are a Member State providing an EUDI Wallet and a Wallet Provider having its Wallet Solution certified under CIR (EU) 2024/2981. DSSO carries no obligation here, and depends on both being done by someone else. |
| **Art. 5b(1)** — registration | *"Where a relying party intends to rely upon European Digital Identity Wallets ... the relying party shall register in the Member State where it is established."* | Nothing, and nothing can. Registration is an administrative act performed by a legal person before a national registrar. | — | **EXTERNAL — NOT OBTAINED.** The act is registration with the Registrar of a Member State by the accountable operator named in `DssoOperations.md` §1. Until it happens no access certificate exists: ARF Reg_10a obliges a Member State to ensure its notified Access Certificate Authority issues access certificates to Relying Parties **registered** under Topic 27, and registration is the precondition. Without one, RPA_03's relying-party authentication cannot be performed and the role is not operable. |
| **Art. 5b(2)(c)** — declaring intended use | Summary: the registration must state, at minimum, the relying party's authentication details, its contact details, and the intended use of the service together with the data it will request. | Nothing in code. There is no declared-purpose object, no request template, and no place where a purpose is recorded. | — | **NOT IMPLEMENTED** (and, for the declaration itself, EXTERNAL — the declaration is made to the registrar, not in software) |
| **Art. 5b(3)** — data minimisation at request time | *"Relying parties shall not request users to provide any data other than that indicated pursuant to paragraph 2, point (c)."* | DSSO issues no request at all, so it cannot over-request — but it also cannot enforce the rule. `dsso_pid_verify` ACCEPTS whatever disclosures the wallet chose to send, up to `DSSO_MAX_CLAIMS` (64), and only `personal_administrative_number` is consumed (`dsso_pid_subject_material`); the rest sit in the caller's `dsso_pid_result` until somebody scrubs it. `dsso_pid_verify` scrubs it itself on **every failure** path; on success the CALLER must — `dsso_bind_commit` does, on every exit. There is no allow-list of requested attributes anywhere. | `determ-dsso selftest-pid` covers the caps and the scrub, not a purpose rule | **PARTIAL.** Missing: the request side entirely (Art. 5b(3) attaches to a request DSSO does not make), and an attribute allow-list bound to a declared purpose that would refuse a presentation carrying more than the declared set. Accepting extra disclosed attributes and discarding them is not the same as not requesting them. |
| **Art. 5b(6)** — keeping registration current | A registered relying party must notify its Member State of changes to the registered information without undue delay. | Nothing. Organisational. | — | **EXTERNAL** (act: notification by the accountable operator to its Member State registrar) |
| **Art. 5b(8)** — identifying to the user | *"Where relying parties intend to rely upon European Digital Identity Wallets, they shall identify themselves to the user."* | Nothing. This is what the access certificate carries, and the wallet is what displays it (ARF RPA_06). DSSO presents no certificate. | — | **NOT IMPLEMENTED.** Depends on the EXTERNAL access certificate of Art. 5b(1)/RPA_02. |
| **Art. 5b(9)** — responsibility for authenticating and validating the data requested | *"Relying parties shall be responsible for carrying out the procedure for authenticating and validating person identification data ... requested from European Digital Identity Wallets."* | The nine-rule pipeline `dsso_pid_verify` — structure, issuer trust (`dsso_trust_lookup`, `header_ok`), ES256 (`dsso_es256_verify`), selective disclosure against `_sd`, holder binding under `cnf` with `sd_hash`, audience, the DSSO-issued nonce, freshness, Token Status List (`check_status`), assurance (`assurance_of`) — each with its own status code, applied in an order in which nothing attacker-supplied is acted on before the issuer signature over it verifies, and all outputs zeroed on any failure. | `determ-dsso selftest-pid` (156 assertions, the gate's own counter; 8400 fuzz mutants across four corpora — 2400 presentations, 3000 JSON documents, 1500 base64url strings, 1500 zlib streams, each loop printed by the gate; 17 mutants each RED per the increment's record) | **PARTIAL.** The VALIDATION of a delivered presentation is implemented and gated. What is missing is the rest of "the procedure": the request, the transport, the response decryption (OIA_09), and the trust source (a configured array stands in for a LoTE). |
| **Art. 5b(9)** second limb — pseudonyms | A relying party may not refuse the use of pseudonyms where user identification is not required by law. | DSSO's own relying parties never see a national identifier: `dsso_pairwise_subject` gives each RP a per-RP value `HMAC(user_root, DS_SUB ‖ LP(rp_id) ‖ u64(reg_epoch))`. Toward the WALLET, DSSO requests `personal_administrative_number` unconditionally — `dsso_pid_subject_material` returns `DSSO_E_FORMAT` without it, so an account cannot be bound to a pseudonymous PID subject at all. | `determ-dsso selftest-assertion` (pairwise subjects differ across RPs, stable across logins) | **PARTIAL.** Downstream (DSSO → its RPs) is implemented. Upstream (wallet → DSSO) is the opposite of pseudonymous: enrolment REQUIRES the national identifier, which is a design choice driven by the one-subject-one-account rule and is stated here rather than softened. |
| **Art. 5b(10)** — intermediaries | Summary: an intermediary acting on behalf of a relying party is itself a relying party and may not store the content of the transaction data. | Nothing. DSSO is not designed as an intermediary and this mapping does not assess it as one. | — | **EXTERNAL** — binds an intermediary, a role DSSO does not occupy. If a deployment ever operates DSSO on behalf of another relying party it becomes one, and this row must be re-assessed; the act is that deployment's own registration as an intermediary under Art. 5b(1). |
| **Art. 9** — *Notification* | A **Member State** may notify an electronic identification scheme to the Commission; publication in the Official Journal is what makes a scheme notified. | Nothing, and nothing could. Notification is an act of a Member State about a scheme, not an act of software. | — | **EXTERNAL** — binds a notifying Member State. The act is a Member State notifying an electronic identification scheme to the Commission and the Commission publishing it in the Official Journal. **It has not happened and is not being sought**: DSSO is explicitly a private, **non-notified** identity provider (§11), and no document in this repository may say otherwise. |
| **Art. 24** — *Requirements for qualified trust service providers* | Obligations on a QTSP (identity verification of applicants, staffing, records, termination plans, supervisory body relations). | Nothing. DSSO issues no qualified certificate, no qualified timestamp, no qualified seal. | — | **EXTERNAL** — binds qualified trust service providers. The act is qualification by a supervisory body. DSSO neither seeks nor holds it. |
| **Art. 29** — *Requirements for qualified electronic signature creation devices* | Summary: obligations attaching to a qualified electronic signature creation device and to the bodies that certify one. | Nothing. DSSO holds no QSCD and asserts no qualified signature. | — | **EXTERNAL** — binds QSCD makers and their certification bodies. Listed because a reader may expect an eIDAS mapping to touch it; DSSO does not. |

### 12.2 Commission Implementing Regulation (EU) 2015/1502, Annex

Text retrieved and read 2026-09-17. **Every row below is assessed against level
SUBSTANTIAL and against nothing else.** No row in this repository is assessed against, or
may claim, level HIGH — and `authn.h` does not define `DSSO_LOA_HIGH`, so the enum cannot
express it. `dsso_pid.h` DOES define `DSSO_LOA_HIGH`, but only as a value a PID Provider's
`acr` claim may assert about ITSELF; it is capped by the trust anchor's `max_loa` and it
says nothing about DSSO's own authentication.

**A correction to the mission brief, recorded rather than propagated: the Annex has no
§2.3.2.** Section 2.3 "Authentication" contains exactly one subsection, 2.3.1
"Authentication mechanism". The failed-attempt / online-guessing obligation lives inside
§2.3.1's control sentence, and that is where this mapping puts it. Record keeping is
**§2.4.4**, not §2.4.6 — §2.4.6 is Technical controls.

**Nothing in this sub-table is a legal obligation on DSSO.** CIR (EU) 2015/1502 specifies
the assurance levels of electronic identification means issued under **notified** schemes.
DSSO is not a notified scheme and is not seeking to become one (§11), so these rows are a
**yardstick the mission chose**, and every IMPLEMENTED here means "equivalent to what this
Annex asks at substantial, on our own reading". **Equivalence to a yardstick is a
self-assessment**; only a conformity assessment body assessing a notified scheme produces
anything more.

**Provenance of the quotations.** Material in *italic quotation marks* below is verbatim
from the retrieval: §2.1.2's opening clause, both §2.2.1 substantial bullets, both §2.3.1
substantial bullets and §2.3.1's level-low bullets, §2.4.3 and §2.4.4 and §2.4.6. The
§2.1.2 alternatives, §2.2.2, §2.2.3, §2.2.4, §2.4.1, §2.4.2, §2.4.5 and §2.4.7 rows are
**summaries** — those subsections were returned in summary form — and are written without
quotation marks so the difference is visible.

| Requirement | What it obliges (level substantial) | What the repository implements TODAY | Gate | Status |
|---|---|---|---|---|
| **§2.1.2** — identity proofing and verification (natural person) | *"Level low, plus one of the alternatives listed in points 1 to 4"* — verified possession of recognised evidence, or an identity document checked in the issuing Member State, or an accepted prior procedure confirmed by a conformity assessment body, or **issuance on the basis of a valid notified eID means at substantial or higher**. | DSSO relies on the **fourth** alternative in substance — issuance on the basis of a valid notified eID means at substantial or higher, without repeating the prior proofing — reading a wallet-issued PID as such a means. **That reading is DSSO's own and has not been confirmed by any supervisory body or conformity assessment body**; it is recorded here as the basis of this row rather than asserted as correct. What the code does: `dsso_pid_verify` requires the credential to assert its own level in the ISSUER-SIGNED payload (`assurance_of` reads OIDC `acr` against the three eIDAS LoA URIs, never a disclosure), caps it at the trust anchor's `max_loa`, and refuses below `pol->required_loa`. `dsso_bind_commit` records the level with the binding; `dsso_authn_enrol_first` records the pairwise subject. | `determ-dsso selftest-pid` (assurance arms, anchor cap, `acr`-absent and `acr`-in-a-disclosure rejections) | **EXTERNAL, with the checking implemented.** The proofing itself is performed by the PID Provider under a notified scheme; DSSO only reads and records the level. The act outside this repository is the PID Provider's proofing and its Member State's notification. **The claim-name is DSSO's own profile choice**: the ARF v2.9.0 PID Rulebook fixes no claim for the credential's own assurance level in the SD-JWT VC profile, so `acr` is a convention documented in `assurance_of` and in `DssoPidVerification.md` §3.9 — if a rulebook fixes another carrier, that one function changes. Proofing is inherited ONCE and is not re-established by any later login. |
| **§2.2.1** — eID means characteristics and design | Two bullets at substantial, and both are quoted because half a requirement is not a requirement: *"The electronic identification means utilises at least two authentication factors from different categories."* and *"The electronic identification means is designed so that it can be assumed to be used only if under the control or possession of the person to whom it belongs."* Art. 2 fixes the categories as possession-based, knowledge-based and inherent. | Two factors from two categories, verified in ONE authentication. Knowledge: the threshold-OPRF/OPAQUE-3DH login's server-side secret, presented as `dsso_authn_knowledge_tag`. Possession: a P-256 key generated on the device from device-local entropy (`dsso_authn_device_keygen`), never transmitted and not derivable from the password, the OPRF output or anything the servers store, answering a fresh challenge (`dsso_authn_challenge`, `dsso_authn_possession_sign`, `dsso_authn_possession_verify`). `dsso_authn_login` refuses either factor alone for an `ACTIVE` account — that refusal is the claim. For the **second** bullet: the device secret is generated on the device and never transmitted, the servers hold only the public point, and no quorum of servers can produce a possession proof — so the means cannot be exercised by the service itself. | `determ-dsso selftest-authn` (114 assertions; password-only rejected, device-only rejected, and the adversary holding the password AND every server share still cannot log in) | **IMPLEMENTED — as equivalence, not as certification.** `n` servers are ONE factor evaluated in a distributed way; that is why the second factor had to be added. **The second bullet is an assumption the code can support but not establish**: "can be assumed to be used only if under the control of the person" is a statement about a device in the world, and the module makes no secure-element claim. What is NOT claimed: a secure element ("device-resident" is a statement about where the key is generated and that it is never transmitted), phishing resistance (the challenge binds the session, the server set and the time, but not an origin or a channel, so a relay that forwards both factors is not defeated), and any conformity assessment. |
| **§2.2.2** — issuance, delivery and activation | The eID means must be delivered so that it reaches only the intended person, and be activated only by that person. | First enrolment (`dsso_authn_enrol_first`) is authorised by the PID event alone, which is the only thing the user holds at that moment. Every later device needs BOTH a `SUBSTANTIAL` session AND a fresh possession proof from an already-active device bound to the new public key (`dsso_authn_enrol_device`) — a password alone never enrols. | `determ-dsso selftest-authn` (arms D) | **PARTIAL.** The authorisation rules are implemented. Missing: everything about DELIVERY — there is no out-of-band channel, no activation code, no device-provisioning flow and no notification to the user that a device was added. Those are `DssoOperations.md` §2 and §5 obligations, not code. |
| **§2.2.3** — suspension, revocation and reactivation | Revocation must be possible in a timely and effective manner, suspension must be reversible only under the same assurance, and reactivation must not lower the level. | `dsso_authn_revoke_device` requires a live `SUBSTANTIAL` session plus a fresh possession proof bound to the target, and bumps `auth_epoch`, which every session carries — so every live session including the one that asked stops verifying at once (`dsso_authn_session_verify`). `dsso_authn_recover` implements the loss/recovery machine under one binding rule: evidence of ONE factor may only REDUCE assurance; restoring `SUBSTANTIAL` needs a SECOND independent evidence, always a fresh, single-use, subject-matched PID presentation. | `determ-dsso selftest-authn` (arms E and F; mutants M4 and M5 each RED) | **PARTIAL.** The state machine is implemented and gated. Missing: the operational side — who may initiate a revocation on a user's behalf, identity checks for a help-desk path, and the timeliness commitment. `DssoOperations.md` §4 and §5 state them; none of them is code. |
| **§2.2.4** — renewal and replacement | Renewal or replacement must not weaken the assurance level or the binding to the person. | Replacement of a device is `DSSO_AUTHN_EV_DEVICE_LOST` followed by `DSSO_AUTHN_EV_RESTORE_DEVICE`, which requires knowledge PLUS a PID presentation; a device id is never recycled (`dev_admit`), and a refused add leaves the account exactly as it was. | `determ-dsso selftest-authn` (arms F) | **IMPLEMENTED for the authentication factors.** Not implemented for anything else a deployment renews (no credential expiry, no scheduled re-proofing, no key-material lifetime), which is `DssoOperations.md` §2. |
| **§2.3.1** — authentication mechanism: dynamic authentication | *"Level low, plus: The release of person identification data is preceded by reliable verification of the electronic identification means and its validity through a dynamic authentication."* Art. 2 defines dynamic authentication as an electronic process creating on demand a proof that the subject controls the identification data, **"which changes with each authentication"**. Substantial also inherits the level-low bullet *"Where person identification data is stored as part of the authentication mechanism, that information is secured in order to protect against loss and against compromise, including analysis offline."* | The possession proof changes with every authentication by construction: the verifier REBUILDS the challenge from its own view — purpose byte, account, device, the single-use session nonce, the deployment's bound server-set digest, the timestamp, and the knowledge factor's own tag for that same login — and verifies the RFC 9497 DLEQ proof against it (`dsso_authn_challenge` + `dsso_authn_possession_verify` inside `dsso_authn_login`). It never accepts a challenge from the client. A replayed nonce is refused (`seen_take`); a proof made for another session, another server set, another purpose or another moment does not verify. For the inherited level-low bullet: **DSSO stores no person identification data at all** as part of the authentication mechanism — what persists is a keyed pseudonym whose key lives outside the account database, which is what makes offline analysis of a leaked database yield uncorrelatable 32-byte strings (`dsso_pseudonym_derive`, `DssoOperations.md` §2.3). | `determ-dsso selftest-authn` (arms C; mutants M1 and M2 RED) | **IMPLEMENTED — as equivalence.** Note the scope: what DSSO gates is the release of DSSO's own §5 assertion (`dsso_authn_assertion_authorize`), not the release of PID by a wallet. The wallet's own dynamic authentication of its user is the wallet's obligation and is invisible here. |
| **§2.3.1** — authentication mechanism: the control sentence (this is where failed-attempt limiting lives; **there is no §2.3.2**) | *"The authentication mechanism implements security controls ... so that it is highly unlikely that activities such as guessing, eavesdropping, replay or manipulation of communication by an attacker with moderate attack potential can subvert the authentication mechanisms."* | **Guessing**: an aggregate per-account limiter — a grow-only counter with one slot per server, per-slot-maximum merge (a state-based G-Counter CRDT, so the set converges by gossip with no consensus rule), capped on the SUM (`dsso_authn_cluster_meter`, `dsso_authn_meter_total`). Because a login needs `t` servers to serve, `A ≤ floor(cap / (t − b))` for every subset rotation with `b < t` non-counting servers. Metering precedes verification, so cheap rejections cost budget; a view stale beyond `merge_max_age` REFUSES to serve. **Replay**: single-use login nonces retained for the full `2·skew + 1` acceptance window (`seen_take`, `nonce_retain`), single-use PID presentation ids (`pid_seen`), a single-use bounded nonce cache at the relying party that never evicts a live entry and fails closed when full (`dsso_rp_verify`). | `determ-dsso selftest-authn` (arms G: full rotation over all ten 3-subsets yields exactly `floor(cap/t) = 4` attempts then `DSSO_E_RATELIMIT`; mutant M6 RED) and `determ-dsso selftest-assertion` (cache bound, fail-closed-when-full, eviction cannot resurrect a used nonce) | **PARTIAL.** Guessing and replay are implemented and gated. **Eavesdropping and manipulation of communication are NOT addressed by any code here**: there is no transport, no TLS, no channel binding. The `binder` of §5 is a bearer secret in transit and front-channel confidentiality is the deployment's. At `b ≥ t` the adversary holds an evaluation quorum and can evaluate the OPRF offline; no online limiter of any design bounds that, and C1/C3 is what excludes it. |
| **§2.4.1** — general provisions | An effective, documented management and organisational structure, with defined roles and liability. | Nothing in code. | `DssoOperations.md` §1 states the requirement | **EXTERNAL** (act: the accountable operator constitutes itself and documents the structure) |
| **§2.4.2** — published notices and user information | Publication of the service definition, terms, conditions, fees, privacy policy and a contact point. | Nothing in code. | `DssoOperations.md` §8 | **EXTERNAL** (act: publication by the operator) |
| **§2.4.3** — information security management | *"Level low, plus: The information security management system adheres to proven standards or principles for the management and control of information security risks."* | Nothing in code. An ISMS is not a library. | `DssoOperations.md` §2 and §4 | **EXTERNAL** (act: the operator establishes an ISMS to a recognised standard and has it assessed) |
| **§2.4.4** — record keeping | *"Record and maintain relevant information using an effective record-management system"* and *"retain ... and protect records for as long as they are required for the purpose of auditing and investigation of security breaches"*; at substantial, *"same as level low"*. | **Nothing.** `determ-dsso` performs no IO except writing selftest output to stdout; there is no audit log, no event record, no retention machinery and no log-protection mechanism anywhere in `dapps/dsso`. The only thing the code does for this requirement is make such a log POSSIBLE by never putting a raw identifier or a reusable secret where a logger would find it (`dsso_pid_result_scrub` after one HMAC, pseudonyms rather than identifiers in every persistent record). | — | **NOT IMPLEMENTED.** `DssoOperations.md` §6 specifies what must be logged, what must NOT be logged, and the retention periods — as an obligation on a deployment, not as shipped code. |
| **§2.4.5** — facilities and staff | Physical and personnel security proportionate to the risk. | Nothing in code. | `DssoOperations.md` §2 and §8 | **EXTERNAL** (act: the operator) |
| **§2.4.6** — technical controls | *"Same as level low, plus: Sensitive cryptographic material, if used for issuing electronic identification means and authentication, is protected from tampering."* | Partially, and only inside the process: every secret buffer on the DSSO secret-scalar paths is secure-zeroed on success and on every reachable error path (`DssoG5ConstantTimeReview.md`, G6 worklist shipped), the constant-time review of those paths returned CT-clean, `dsso_ct_equal` guards every comparison an attacker can grind against, and `dsso_pid_subject_material`'s raw national identifier is scrubbed before `dsso_bind_commit` returns. | `determ test-dsso-opaque3dh`, `determ-dsso selftest-core` (`dsso_ct_equal`), plus the G5 review | **PARTIAL.** In-process hygiene is implemented. Missing: everything that makes key material tamper-protected in a deployment — no HSM or KMS integration exists for `sk_s`, `tenant_key`, `K_pseu`, the session secret or the OPRF shares, and the modules take every key as a caller-owned byte array. `DssoOperations.md` §2 names the custody for each of the five. |
| **§2.4.7** — compliance and audit | Periodic internal audit, and external assessment where the scheme requires it. | Nothing in code. | `DssoOperations.md` §8 | **EXTERNAL** (act: the operator commissions the audit; for a notified scheme, a conformity assessment body performs it) |

### 12.3 EUDI ARF — the high-level requirements a relying party carries

**Assessed against ARF v2.9.0.** Requirement texts below are **verbatim** from the v2.9.0
tag of the ARF source repository, read on 2026-09-17 (§12.5), not from a summary; where a
row's "whom it binds" clause paraphrases, the quotation that follows is the text.
**Which party a requirement binds is the first thing each row states**, because most of the
identifiers the mission brief listed bind a Wallet Unit, a Wallet Provider, a Member State
or a Registrar, and DSSO is none of them. Of the fourteen the brief named, **four bind a
relying party** (OIA_03, OIA_12, OIA_16 and — conditionally on format — OIA_03b/OIA_03c),
**three bind both** the wallet and the relying party (RPA_01, RPA_02, RPA_03), **one is a
relying-party SHOULD** the brief did not name at all (OIA_17), and the rest bind somebody
else.

| Requirement (ARF v2.9.0) | Whom it binds, and what it obliges | What the repository implements TODAY | Gate | Status |
|---|---|---|---|---|
| **OIA_02** | **A Wallet Unit.** *"A Wallet Unit SHALL support proving cryptographic device binding between the WSCA/WSCD or a keystore included in the Wallet Unit and a PID or attestation, in accordance with [SD-JWT VC] or [ISO/IEC 18013-5]."* | Nothing — DSSO is not a Wallet Unit and cannot support anything on the wallet's behalf. What DSSO does is the RELYING PARTY'S side of the same mechanism, which is OIA_17, one row below. | — | **EXTERNAL** — binds the Wallet Unit; the act is a Wallet Provider building device binding into its Wallet Solution. Recorded here because `DssoPidVerification.md` and the 2026-09-17 DECISION-LOG entry both attribute DSSO's holder-binding check to OIA_02; that attribution is imprecise, and OIA_17 is the requirement DSSO actually discharges. |
| **OIA_17** | **A Relying Party Instance.** *"A Relying Party Instance SHOULD verify the device-binding signature or Message Authentication Code provided in the presentation response of the Wallet Unit ... following the steps specified per [ISO/IEC 18013-5] or [SD-JWT VC], as applicable."* Note the strength: **SHOULD**, i.e. guidance, not a SHALL. | Rule 5 of `dsso_pid_verify`: the credential's `cnf.jwk` is decoded to a P-256 point (`dsso_jwk_p256`), the final `~`-part is opened as a KB-JWT with `typ: kb+jwt`, its `sd_hash` is compared constant-time against SHA-256 over the presentation up to and including the final `~` (so a KB-JWT lifted from another presentation or one whose disclosure set was edited fails), and the KB-JWT signature is verified under the `cnf` key — not under "some key". A missing or empty KB-JWT is `DSSO_E_BINDING`. | `determ-dsso selftest-pid` (rule-5 arms; mutants M4 and M5 RED) | **IMPLEMENTED, and stricter than the requirement** — OIA_17 is a SHOULD and DSSO makes it mandatory and fail-closed. For the SD-JWT VC profile only; the ISO/IEC 18013-5 limb is not implemented because mdoc is not implemented. |
| **OIA_03** | **Wallet Units, PID Providers, Attestation Providers AND Relying Parties.** *"When issuing, presenting, or verifying an attestation, ... SHALL only use cryptographic algorithms included in the [ECCG Agreed Cryptographic Mechanisms v2.0]."* | DSSO verifies with ECDSA P-256/SHA-256 (`dsso_es256_verify`, composed on the shipped P-256 primitives because `determ::c99` exposes no ECDSA verifier) and uses SHA-256, HMAC-SHA-256 and HKDF-SHA-256 elsewhere. `header_ok` pins `alg` to exactly `ES256`, so no other algorithm can be selected by a token. | `determ-dsso selftest-pid` (the `alg` pin; the RFC 6979 A.2.5 anchor on `dsso_es256_verify`) | **PARTIAL — and the missing part is a CHECK, not code.** The algorithms are conventional and the pin is implemented, but **conformance to the ECCG Agreed Cryptographic Mechanisms v2.0 has NOT been verified against that document**; it was not retrieved. Nothing in this repository should be read as asserting it. |
| **OIA_03a** | **Wallet Providers.** Their Wallet Solution must support OpenID4VP. | Nothing. | — | **EXTERNAL** — binds Wallet Providers. |
| **OIA_03b** | **Relying Parties and Wallet Units**, but **conditionally**: *"For remote presentation flows using redirects, when the format of the requested attestation complies with [ISO/IEC 18013-5], ... SHALL comply with the requirements in [HAIP] Sections 5, 5.1 and 5.3.1, as well as with the 'ISO mdocs' profile in Section 6."* | Nothing. The condition does not arise, because DSSO requests no attestation at all and would not request an mdoc if it did. | — | **NOT IMPLEMENTED.** A wallet that can present only ISO/IEC 18013-5 mdoc is out of scope for DSSO; DSSO does not negotiate down to a weaker check for such a wallet, it refuses. |
| **OIA_03c** | **Relying Parties and Wallet Units**, conditionally on the format DSSO actually chose: *"For remote presentation flows using redirects, when the format of the requested attestation complies with [SD-JWT VC], Relying Parties and Wallet Units SHALL comply with the requirements in [HAIP] Sections 5, 5.1, and 5.3.2, as well as with the 'IETF SD-JWT VCs' profile in Section 6."* | Nothing. There is no OpenID4VP Authorization Request, no `client_id` scheme, no response mode, no DCQL and no HAIP profile conformance of any kind. `dsso_pid_verify` starts from a presentation string and ends at a claim set. | — | **NOT IMPLEMENTED — and this is the requirement that actually binds DSSO's chosen format.** It was absent from the mission brief's list, which named OIA_03/OIA_03b/OIA_04 for the OpenID4VP obligation; at ARF v2.9.0 those are, respectively, the cryptographic-algorithm requirement, the mdoc-conditional profile, and a Wallet Unit obligation. **OIA_03c is the largest unimplemented requirement this repository could itself build.** RPA_03 is a larger obstacle overall, but RPA_03 is blocked on an EXTERNAL act (the access certificate) and OIA_03c is not blocked on anything. |
| **OIA_04** | **A Wallet Unit.** *"A Wallet Unit SHALL verify and process PID or attestation presentation requests from Relying Parties in accordance with the protocols and interfaces specified in [OpenID4VP] for remote flows."* | Nothing. | — | **EXTERNAL** — binds the Wallet Unit; the act is a Wallet Provider implementing OpenID4VP. DSSO's own missing OpenID4VP side is **NOT IMPLEMENTED** and is the OIA_03c row, not this one. (In the later ARF release this identifier is empty — see §12.0 statement 9.) |
| **OIA_07** | **A Wallet Unit.** Support selective disclosure of attributes. | Nothing on the wallet's side. On DSSO's side, rule 4 of `dsso_pid_verify` VERIFIES a selective disclosure: every `~`-part must hash into a distinct `_sd` entry, duplicates are refused, a disclosure may not carry a registered name (`reserved_name`) or shadow a cleartext claim, `_sd_alg` must be `sha-256`, and the salt must be at least 22 base64url characters (`salt_ok`) so the `_sd` array cannot be brute-forced back into withheld values. | `determ-dsso selftest-pid` (rule-4 arms; mutant M3 and M12 RED) | **EXTERNAL** as written — it binds the Wallet Unit, and the act is a Wallet Provider supporting selective disclosure. The relying-party counterpart — **verifying** rather than supporting a selective disclosure — is IMPLEMENTED and gated, and is listed here so the distinction is not lost. |
| **OIA_09** | **A Wallet Unit.** *"For remote presentation flows the Wallet Unit SHALL ensure that the attributes included in the presented attestation are accessible only to the Relying Party Instance, by encrypting the presentation response."* | Nothing, in both directions: DSSO neither encrypts (it is not the wallet) nor **decrypts**. `dsso_pid_verify` takes a cleartext SD-JWT VC combined-format string. | — | **EXTERNAL** for the obligation itself — it binds the Wallet Unit — but it implies a capability DSSO lacks: **response decryption is NOT IMPLEMENTED**, so a conforming wallet's encrypted presentation response could not be opened by this code. Recorded as a gap, not hidden behind "binds the wallet". |
| **OIA_12** | **A Relying Party.** *"For both proximity and remote presentation flows, a Relying Party SHALL validate the signature of a PID using a trust anchor provided in a PID Provider LoTE made available in accordance with Topic 31."* | `dsso_trust_lookup` takes the token's `iss` and `kid` as SELECTORS and returns a key the caller already trusted; the key is never taken from the token, and `header_ok` refuses `jwk`, `jku`, `x5u`, `x5c`, `x5t` and `crit` outright, so a token cannot choose its own verifier. An unknown issuer/kid pair is `DSSO_E_TRUST` — no fallback, no discovery, no "unknown issuer at reduced assurance". The same rule is applied to the status token, and more strictly than the IETF draft requires. | `determ-dsso selftest-pid` (the trust-anchor provenance pair: empty list → `DSSO_E_TRUST`, one bent bit in the anchor key → `DSSO_E_CRYPTO`; mutants M1 and M17 RED) | **PARTIAL.** The half that says "the anchor comes from configuration, never from the token" is implemented and gated. **The half that says the anchor comes from a PID Provider LoTE published by a Member State under Topic 31 is NOT: no LoTE and no Trusted List is fetched, parsed, signature-checked or refreshed.** `dsso_trust_list` is a static C array and populating it is a deployment act with its own unwritten code. Note also that the v2.9.0 text says **LoTE**; the quotation in `dsso_pid.c`'s header ("PID Provider Trusted List") is the wording of an earlier ARF release. The substance — the anchor is provided, not self-asserted — is unchanged. |
| **OIA_16** | **A Relying Party Instance.** *"When receiving a PID or attestation, a Relying Party Instance SHALL discard the values of all unique elements ... as well as any timestamps, as soon as they are no longer needed. The Relying Party Instance SHALL NOT communicate these values to the Relying Party or to any other party inside or outside the EUDI Wallet ecosystem."* | `dsso_pid_result_scrub` zeroes the whole result; `dsso_bind_commit` calls it on the material it derives from and scrubs the raw subject buffer before returning, so what survives a binding is `HMAC(K_pseu, "DSSO-PID-PSEUDONYM-v1" ‖ iss ‖ subject)` and never the national identifier. `dsso_pid_verify` zeroes `*out` on every failure path, so a caller that ignores the status cannot read stale claims. No PID attribute, no raw identifier and no presentation byte enters the login path, the assertion path or the chain (§11). | `determ-dsso selftest-pid` (the "failure leaves nothing behind" scrub; the pseudonym properties) | **PARTIAL.** The in-process discipline is implemented and gated. Missing: **there is no storage layer whose retention could be audited** — the reference containers are in-memory arrays that a deployment replaces, and the moment a real database exists, "as soon as they are no longer needed" becomes a property of that database and of the logging policy in `DssoOperations.md` §6, neither of which this repository contains. The SHALL NOT-communicate limb is satisfied vacuously today because DSSO communicates with nobody. |
| **RPA_01** | **A Wallet Unit AND a Relying Party Instance.** Implement a mechanism for relying-party authentication that lets the wallet identify and authenticate the relying party, lets the wallet verify the request was not copied and replayed, and uses an access certificate issued under Topic 27. | Nothing. There is no request for the wallet to authenticate, no signature over one, and no certificate. | — | **NOT IMPLEMENTED.** Missing: all three limbs — an identity the wallet can authenticate, a signature by DSSO over the request so the wallet can tell the request was not copied and replayed, and the Topic 27 access certificate the mechanism must use. The first two could be built here; the third cannot (RPA_02). |
| **RPA_02** | **Wallet Units and Relying Party Instances.** *"For performing Relying Party authentication, Wallet Units and Relying Party Instances SHALL support access certificates as specified in [ETSI TS 119 475] and [ETSI TS 119 411-8]."* | Nothing. No X.509 handling, no certificate parsing, no `x509_hash` client identifier. | — | **NOT IMPLEMENTED**, and blocked on an **EXTERNAL** act: the certificate is issued by an Access Certificate Authority notified by a Member State (ARF Reg_10a), only to a relying party already registered under Art. 5b(1). |
| **RPA_03** | **A Wallet Unit and a Relying Party Instance.** Perform relying-party authentication in ALL presentation transactions, proximity or remote, using an access certificate — the relying party signing over data in the request, the wallet validating that signature. | Nothing. | — | **NOT IMPLEMENTED**, and the part that is missing is partly EXTERNAL. This is the single largest blocker in the mapping: a conforming Wallet Unit performs relying-party authentication in **every** presentation transaction and tells its user the request is not trustworthy when it fails (RPA_05), so the whole wallet-relying-party role is verified-but-unreachable. |
| **RPA_04** | **A Wallet Unit.** Accept only trust anchors in the LoTEs of all notified Access Certificate Authorities when verifying access certificates. | Nothing. This is the wallet's validation of DSSO's certificate, not DSSO's validation of anything. | — | **EXTERNAL** — binds the Wallet Unit. Recorded because the mission brief described RPA_01..RPA_06 as one relying-party obligation "validated against Member State Trusted Lists"; **the validation against those lists is the WALLET'S job**, under this identifier, not DSSO's. |
| **RPA_05** | **A Wallet Instance.** Inform the user when relying-party authentication fails, and that the request is therefore not trustworthy. | Nothing. DSSO has no user interface toward a wallet user. | — | **EXTERNAL** — binds the Wallet Instance. It is the requirement that makes §12.0 statement 3 concrete: an unauthenticated DSSO is not merely unverified to the user, it is announced as untrustworthy. |
| **RPA_06** | **A Wallet Instance.** Display the relying party's registered trade names from the access certificate, with the requested attributes, when asking for approval. | Nothing. | — | **EXTERNAL** — binds the Wallet Instance. It is how Art. 5b(8) is satisfied in practice, which is why DSSO cannot satisfy Art. 5b(8) without the certificate. |
| **Reg_08** (Topic 27) | **A registered Relying Party** (among others). *"SHALL make any updates necessary to ensure the continued correctness of the registered information without undue delay."* | Nothing. Organisational, and conditional on a registration that does not exist. | — | **EXTERNAL** (act: the accountable operator updates its registry entry) |
| **Reg_01, Reg_03, Reg_04, Reg_06, Reg_24, Reg_25, Reg_29** (Topic 27) | **Member States and Registrars.** Provide the registry, publish entries in signed machine-readable form, support the common retrieval API, enable remote registration, identify relying parties proportionately to risk, and maintain a cancellation policy. | Nothing, and nothing could. | — | **EXTERNAL** — binds Member States and Registrars. The acts are theirs; DSSO's only obligation in this Topic is Reg_08, one row above. |

### 12.4 IETF Token Status List — credential status

| Requirement | What it obliges | What the repository implements TODAY | Gate | Status |
|---|---|---|---|---|
| **draft-ietf-oauth-status-list** — referenced status check. **This is an Internet-Draft, not an RFC, and not law**: it binds DSSO only because the EUDI/SD-JWT VC ecosystem uses it. Revision current when this mapping was written: **-21** (2026-06-21), in the RFC Editor queue; **the repository pins no revision anywhere in code.** | A credential carries `status.status_list` with a `uri` and an `idx`; the relying party fetches a signed status token whose payload carries `status_list` with `bits` and a base64url'd zlib-compressed bitstring `lst`, and reads the `idx`-th entry, packed least-significant-bits-first. | `check_status`: parses `status.status_list.uri` and `.idx`; fetches through an INJECTED callback (`dsso_status_fetch_fn`) because a gate cannot reach a network and a verifier with an embedded transport cannot be driven adversarially; opens the token as a JWT with `typ: statuslist+jwt` and the same `alg` pin; requires the token's `iss` to equal the CREDENTIAL'S OWN issuer and looks that issuer's `kid` up in the SAME trust anchor list; verifies ES256; requires the token's `sub` to equal the `uri` the credential named; rejects a token past its own `exp`; accepts only `bits` in {1,2,4,8}; inflates `lst` with a from-scratch allocation-free RFC 1950/1951 inflate under a hard output ceiling (`dsso_inflate`), so a compression bomb costs `O(cap)`; indexes the bitstring and treats `0x00` as valid, `0x01` and `0x02` as `DSSO_E_STATUS`, and any other value as an extension it refuses. | `determ-dsso selftest-pid` (rule-8 arms, all five fail-closed shapes, the zlib fuzz corpus; mutants M9, M10 and M17 RED) | **PARTIAL.** The status *check* is implemented and gated; **the status *fetch* is not**, and a check you cannot perform is not a status check — hence PARTIAL rather than IMPLEMENTED. Missing: the transport. There is no HTTP client, no TLS, no cache, no retry and no discovery; `dsso_status_fetch_fn` is a callback seam a deployment must fill, and the security of that path is the deployment's. The `uri` it is handed sits inside the issuer-signed payload and is read only after rule 3 verified that signature, so it is chosen by an anchored PID Provider rather than by an arbitrary attacker — but it is chosen by whoever can get a credential issued, which is not the same as trusted (`DssoOperations.md` §3.1). Also missing: any pin on the draft revision, so a future revision that changed the bitstring packing or the `sub` binding would not be detected by any gate. Three properties of what IS implemented, all stated: (i) **Stricter than the draft**: the draft permits a third-party status provider; DSSO requires the status token to come from the credential's own issuer, because "signed by SOME anchor on the list" would let one compromised PID Provider un-revoke another Member State's credentials. A deployment needing a third-party provider adds it under the credential issuer's own identifier. (ii) **Fail-closed, not fail-open**: unreachable, stale, wrong list, index past the bitstring, or a bomb are each `DSSO_E_UNAVAILABLE` and the verification FAILS. There is no allow-on-outage path and adding one would be a defect. (iii) `DSSO_MAX_STATUS_BYTES` caps a list at 131072 one-bit entries — a REJECT boundary, never a truncation; a larger deployment needs a larger cap or sharded lists. |

### 12.5 Sources, versions, and how each was verified on 2026-09-17

| Source | Version relied on | How it was verified |
|---|---|---|
| Regulation (EU) No 910/2014 as amended by (EU) 2024/1183 | Consolidated text of 2024-05-20, CELEX **02014R0910-20240520** | **Re-read online 2026-09-17.** Art. 5b(1), (3), (8) and the first sentence of (9) came back **verbatim** and are quoted in §12.1; Art. 5b(2), (6), the second sentence of (9) and (10) came back in **summary** form and are written as summaries there. The titles of Art. 5a ("European Digital Identity Wallets"), Art. 9 ("Notification") and Art. 24 ("Requirements for qualified trust service providers") were confirmed from the same retrieval. The title of Art. 29 ("Requirements for qualified electronic signature creation devices") was truncated in that retrieval and was confirmed separately; it is stated here on that basis. |
| Commission Implementing Regulation (EU) **2015/1502** | CELEX **32015R1502**, Annex | **Re-read online 2026-09-17.** The level-substantial text of Annex §2.1.2, §2.2.1, §2.3.1 and §2.4.4 was retrieved verbatim, together with the Art. 2 definitions of "dynamic authentication" and "authentication factor" and the three factor categories, and the complete subsection lists of §2.2, §2.3 and §2.4. **That retrieval is how this mapping established that §2.3.2 does not exist** and that Record keeping is §2.4.4, not §2.4.6 (§2.4.6 is Technical controls). |
| Commission Implementing Regulation (EU) **2024/2981** (wallet certification) | Cited by number only | **NOT retrieved.** It is named here solely to say DSSO is not certified under it. No row is assessed against its text. |
| EUDI **ARF v2.9.0** | The version this mission pinned | **Re-read online 2026-09-17** from the `v2.9.0` tag of the ARF source repository (`docs/annexes/annex-2/annex-2.02-high-level-requirements-by-topic.md`). Every OIA and RPA row above quotes that file. **Material finding: v2.9.0 is NOT the current ARF** — a later major release exists, described on the release index as aligning the framework with the latest amending Implementing Regulations and introducing a Functional Conformance Assessment Framework. Spot-checking the same identifiers in it found `OIA_04` **empty**, while OIA_02, OIA_03, OIA_03b, OIA_07, OIA_09, OIA_12, OIA_16 and RPA_01..RPA_06 read the same in substance. **This mapping has NOT been reviewed against that release**, and reviewing it is named work, not work done. A second spot-check against the older v2.9.0-minus-five release v2.4.0 is what established that the identifiers move: see §12.0 statement 9. |
| ARF technical specifications (TS 5, TS 6), ETSI **TS 119 475**, ETSI **TS 119 411-8**, ETSI **TS 119 612**, **HAIP**, **OpenID4VP**, **SD-JWT VC**, **ISO/IEC 18013-5**, **ISO/IEC TS 18013-7** | Referenced by the rows above | **NOT retrieved.** Every row that depends on one of them is NOT IMPLEMENTED or PARTIAL and says so; no row claims conformance to a document this mapping did not read. |
| **ECCG Agreed Cryptographic Mechanisms v2.0** | Referenced by ARF OIA_03 | **NOT retrieved.** The OIA_03 row is PARTIAL for exactly this reason. |
| IETF **Token Status List** (`draft-ietf-oauth-status-list`) | The mechanism as implemented in `check_status` | **Checked online 2026-09-17.** The document is an OAuth working-group Internet-Draft at revision **-21** (2026-06-21), in the **RFC Editor queue**, intended status Proposed Standard. It is therefore **a draft, not yet an RFC** — the wire details may still change before publication, and `check_status` is pinned to the mechanism as this repository implements it rather than to a specific revision number. The repository does not pin a revision anywhere in code, which is itself a gap: a future revision that changed the bitstring packing or the `sub` binding would not be detected by any gate. |
| The mission's own record | `/root/audit/dsso/BRIEF-COMMON.md` | Where a source above says NOT retrieved, the version is as recorded there and has **not** been re-verified by this mapping. |

### 12.6 What the mapping adds up to

**46 rows: 4 IMPLEMENTED, 11 PARTIAL, 8 NOT IMPLEMENTED, 23 EXTERNAL.** The four
IMPLEMENTED rows are CIR §2.2.1 (two factors from different categories), CIR §2.2.4
(renewal and replacement of the authentication factors), CIR §2.3.1 (dynamic
authentication), and ARF OIA_17 (the relying party's verification of the device-binding
signature, which is a SHOULD that DSSO makes mandatory and fail-closed). Every one of them
is about what DSSO does with something it has already been given.

That is the shape of the result. Everything DSSO is given, it checks well; everything DSSO
must go and get, it cannot. The eleven PARTIAL rows almost all fail on the same seam — the
validation is written and the transport, the storage or the trust source is not — and
everything that faces OUTWARD is NOT IMPLEMENTED, EXTERNAL, or both: registering with a
Member State, obtaining and presenting an access certificate, speaking OpenID4VP under the
HAIP profile OIA_03c, decrypting a conforming wallet's response, fetching a status token,
and reading a trust anchor out of a published LoTE. **The honest summary is that DSSO today
can verify a wallet presentation correctly and cannot obtain one.**
