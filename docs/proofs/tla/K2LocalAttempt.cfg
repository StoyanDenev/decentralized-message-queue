\* TLC configuration for K2LocalAttempt.tla (FB74). Deadlines in abstract
\* ticks: commit phase closes at tick 2, the reveal window at tick 4
\* (the code's 1000 ms / 2000 ms ratio); two explicit attempts.
CONSTANTS
    CommitDeadline = 2
    RevealDeadline = 4
    MaxAttempts = 2

SPECIFICATION Spec

INVARIANTS
    TypeOK
    Inv_EvalNeedsBothMatching
    Inv_RevealNeedsBothCommits
    Inv_AbortHasCause
    Inv_CommitsBeforeDeadline
