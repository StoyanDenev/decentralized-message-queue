\* TLC configuration for AbortEscalation.tla — PRIMARY S-044 EXHIBIT (E1).
\*
\* Models the shipped `web` profile posture (the `determ init` DEFAULT):
\* M=3 pool, K=2 committee (include/determ/chain/params.hpp:142-145;
\* web_test mirrors it at :216-220), bft_enabled with the genesis-default
\* escalation threshold 5 (include/determ/chain/genesis.hpp:145), no fix
\* toggles — i.e., current node.cpp semantics.
\*
\* EXPECTED TLC OUTCOME (this is the exhibit — the failures are the point):
\*
\*   TypeOK, Inv_ModeCoupling, Inv_ActiveCommitteeWellFormed   HOLD
\*   Inv_NoPermanentWedge    FALSIFIED — counterexample trace shape:
\*       StartRoundMD {n1,n2}                       (avail=3 >= K=2)
\*       RoundStraggle(n2)   single-claim quorum    (needed = max(1, 2-1) = 1)
\*       StartRoundMD {n1,n3}                       (avail=2 >= K=2)
\*       RoundStraggle(n3)   single-claim quorum
\*       -> aborted={n2,n3}, avail=1 < K=2, and k_bft=(2*2+2)/3=2=K
\*          gives zero escalation headroom (avail=1 < k_bft=2): WedgeState.
\*   Inv_NoFrozenCounter     FALSIFIED at the same state (avail=1 < k_bft=2,
\*       totalAborts <= 2*(1+DupBound)=4 < BftThreshold=5) — the K=2
\*       instance of S-045, subsumed by S-044 per SECURITY.md:1316.
\*   Prop_ProgressEventually VIOLATED — fair lasso: the cascade above,
\*       duplicate adoptions up to the cap, then Stall self-loop forever
\*       (mirrors node.cpp:788's silent return; sole abort clear at
\*       node.cpp:1856 is unreachable without formation).
\*
\* NOTE: TLC stops at the first violated invariant. To see each exhibit
\* independently, check the invariants one at a time (comment in/out), or
\* run the liveness property alone. The all-green sibling configurations
\* (E3 sanity, E4/E5/E7 fix re-checks) are one-line CONSTANT edits
\* documented in the exhibit table in AbortEscalation.tla's header:
\*   E2: K = 3                                   (S-045 M=K=3 halt)
\*   E3: K = 3, Dead = {n3}, SkewStraggle = FALSE, BftThreshold = 1
\*       (the green test_bft_escalation.sh posture; its genesis pins
\*        threshold=1 at tools/test_bft_escalation.sh:84)
\*   E4: QuorumFloor = 2                         (fix F-a — expect green)
\*   E5: AbortDecay = TRUE                       (fix F-b — expect green)
\*   E6: CountFormationFailures = TRUE           (fix F-c — STILL wedged
\*        at K=2: no escalation headroom; F-c targets S-045, not S-044)
\*   E7: K = 3, Dead = {n3}, SkewStraggle = FALSE, BftThreshold = 5,
\*       CountFormationFailures = TRUE           (F-c rescue — expect green)
\*
\* State space: |Pool|=3, abortEvents in [Pool -> 0..2], height <= 3,
\* budget <= 4, committee subsets of a 3-set — well under 10^5 states;
\* TLC should exhaust in seconds.
\*
\* STATUS: written, model-check pending (TLC unavailable in the authoring
\* environment; matches the directory-wide "pending TLC installation"
\* convention in CHECK-RESULTS.md).

CONSTANTS
    Pool                   = {n1, n2, n3}
    K                      = 2
    BftEnabled             = TRUE
    BftThreshold           = 5
    Dead                   = {}
    SkewStraggle           = TRUE
    QuorumFloor            = 1
    AbortDecay             = FALSE
    CountFormationFailures = FALSE
    DupBound               = 1
    MaxStraggles           = 4
    MaxHeight              = 3

SPECIFICATION Spec

INVARIANTS
    TypeOK
    Inv_ModeCoupling
    Inv_ActiveCommitteeWellFormed
    Inv_NoPermanentWedge
    Inv_NoFrozenCounter

PROPERTIES
    Prop_ProgressEventually
