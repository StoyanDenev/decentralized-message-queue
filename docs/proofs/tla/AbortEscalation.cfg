\* TLC configuration for AbortEscalation.tla — PRIMARY SHIPPED-FIX EXHIBIT (E-fix).
\*
\* S-044 / S-045 are now MITIGATED (docs/SECURITY.md §S-044/§S-045). This config
\* models the SHIPPED fix on the zero-MD-margin stress posture M=K=3 (the cluster
\* / tactical profiles — the case that most exercises the S-045 escalation fix):
\*   * QuorumFloor = 2   — fix F-a: abort-claim quorum max(2,K-1), shipped via
\*                         chain::abort_claim_quorum (include/determ/chain/params.hpp).
\*   * BftThreshold = 1  — the shipped genesis default (was 5), so the escalation
\*                         counter is reached by the first abort event and can
\*                         never freeze below the threshold (include/determ/chain/
\*                         genesis.hpp bft_escalation_threshold{1}).
\*   * K = 3, Dead = {n3}, SkewStraggle = FALSE — one genuinely dead member, no
\*                         healthy-node timing skew (F-a removes skew-cascade at
\*                         any K anyway; here we exhibit the real-fault recovery).
\*
\* EXPECTED TLC OUTCOME (the fix holds — all green):
\*   TypeOK, Inv_ModeCoupling, Inv_ActiveCommitteeWellFormed   HOLD
\*   Inv_NoPermanentWedge    HOLDS — the single dead member yields aborted={n3},
\*       avail=2; k_bft=(2*3+2)/3=2, so avail=2 >= k_bft=2 AND (BftThreshold=1
\*       reached by the first abort) => escalation to a BFT 2-of-3 round fires;
\*       no WedgeState is reachable.
\*   Inv_NoFrozenCounter     HOLDS — with BftThreshold=1 the counter meets the
\*       threshold on the first abort event, so it can never freeze below it.
\*   Prop_ProgressEventually HOLDS — the escalated BFT round finalizes a block,
\*       clears aborts (node.cpp:1856), and the chain advances.
\*
\* COMPARISON EXHIBITS (one-line CONSTANT edits; see AbortEscalation.tla header):
\*   E1-historical (the pre-fix S-044 defect, now removed by the shipped fix):
\*       K = 2, QuorumFloor = 1, BftThreshold = 5, SkewStraggle = TRUE, Dead = {}
\*       -> Inv_NoPermanentWedge + Inv_NoFrozenCounter + Prop_ProgressEventually
\*          VIOLATED (the wedge the shipped fix removes). Kept as the regression
\*          reference; NOT the shipped posture.
\*   E-webfix (the shipped web default M=4/K=3): add n4 to Pool, K = 3,
\*       QuorumFloor = 2, BftThreshold = 1, Dead = {n4} -> all green, and the
\*       single fault is handled by an MD 3-of-3 among survivors (MD margin 1)
\*       WITHOUT escalating (avail=3 = K, gate 1 stays MD).
\*   E-k2-crashstop (F-a at K=2): K = 2, QuorumFloor = 2 -> no abort event can
\*       form (quorum unsatisfiable); a dead member halts (crash-stop) but the
\*       skew-cascade is gone (Inv_NoPermanentWedge holds under SkewStraggle).
\*
\* State space: |Pool|=3, abortEvents in [Pool -> 0..2], height <= 3, budget <= 4
\* — well under 10^5 states; TLC exhausts in seconds.
\*
\* STATUS: written, model-check pending (TLC unavailable in the authoring
\* environment; matches the directory-wide "pending TLC installation" convention
\* in CHECK-RESULTS.md). The shipped code is validated live (tools/
\* test_bft_escalation.sh, θ=1 default) + statically (tools/test_s044_gate_surface.sh).

CONSTANTS
    Pool                   = {n1, n2, n3}
    K                      = 3
    BftEnabled             = TRUE
    BftThreshold           = 1
    Dead                   = {n3}
    SkewStraggle           = FALSE
    QuorumFloor            = 2
    AbortDecay             = FALSE
    CountFormationFailures = FALSE
    DupBound               = 1
    MaxStraggles           = 4
    MaxHeight              = 3

SPECIFICATION Spec

INVARIANTS
    TypeOK
    Inv_ModeCoupling
    Inv_ActiveCommitteeWellFormed
    Inv_NoPermanentWedge
    Inv_NoFrozenCounter

PROPERTIES
    Prop_ProgressEventually
