\* TLC configuration for Receipts.tla
\* Model: 3 distinct receipt IDs, up to 6 blocks (allows duplicates + replays).

CONSTANTS
    ReceiptIds  = {r1, r2, r3}
    MaxBlocks   = 6

SPECIFICATION Spec

INVARIANTS
    TypeOK
    Inv_NoDoubleCredit
    Inv_LogConsistent
