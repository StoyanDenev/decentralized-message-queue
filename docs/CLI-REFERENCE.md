# Determ CLI Reference

All `determ` subcommands. Run `determ --help` for the canonical built-in help.

**See also:** [`QUICKSTART.md`](QUICKSTART.md) for hands-on recipes, [`PROTOCOL.md`](PROTOCOL.md) for wire formats, [`WHITEPAPER-v1.x.md`](WHITEPAPER-v1.x.md) for the standalone technical paper.

## Node lifecycle

| Command | Purpose |
|---|---|
| `determ init [--data-dir D] [--profile P]` | Generate config + Ed25519 keypair in a fresh data dir |
| `determ start --config FILE` | Run the node daemon (foreground) |

Profiles: `cluster` (LAN), `web` (default), `regional`, `global`, `tactical` (mobile-unit swarm; sub-30 ms blocks), plus `*_test` variants (`single_test`, `cluster_test`, `web_test`, `regional_test`, `global_test`, `tactical_test`) with sub-30 ms timers for CI execution mirroring each prod sibling's (chain_role, sharding_mode, M, K). Differ in round timer durations (`tx_commit_ms`, `block_sig_ms`, `abort_claim_ms`) + committee size (M, K) + chain_role + sharding_mode. See `include/determ/chain/params.hpp` for the full profile-constant set.

## Inspection (block-explorer)

All inspection commands hit the running node's RPC. Default RPC port is in the node's config.

| Command | Returns |
|---|---|
| `determ status [--field <name>] [--rpc-port N]` | Chain head, head_hash, role, shard_id, epoch_index, peer_count, mempool, `committee_region` (this node's R1 region tag — empty = global pool), `pending_param_changes` count (A5 Phase 2 governance staging — total entries across all heights, queryable in detail via `determ pending-params`), mode counters + `protections` block (current state of every operator-tunable security/log flag — `rpc_localhost_only`, `rpc_hmac_auth`, `rpc_rate_limit`, `gossip_rate_limit`, `log_quiet`, `bft_enabled`, `sharding_mode`). Monitoring systems can alert on flag drift in production. **`--field <name>` flag** prints just the value of one top-level field (one-shot extraction for shell scripts; e.g., `determ status --field height` prints just the integer, no JSON envelope). Absent/null fields print empty line; non-scalar values (like `protections`) print as compact JSON dumps. |
| `determ summary [--json] [--rpc-port P]` | Operator dashboard. Gathers the most-frequently-checked status fields into a single compact view: height + head_hash, peer + committee + mempool sizes, this node's `committee_region`, sync state, A1 invariant status (computed from `chain_summary` A1 counters). Designed for the SSH-into-server quick-check workflow — one terminal-page summarizes "is the node healthy + caught up + connected?". Uses `status` RPC for most fields + `chain_summary` (last_n=1 for minimal payload) for A1 counter aggregation + `committee` for current K-of-K committee size. **No new server-side RPC method.** Default human output: 8 right-aligned lines (height, head_hash truncated to 24 chars, genesis truncated, peers, mempool, committee + region, sync_state, A1 invariant); `--json` emits structured envelope `{height, head_hash, genesis, peers, mempool, committee, committee_region, sync_state, a1_invariant_ok, live_total_supply, expected_total}`. **Exit code 2 on A1 invariant violation** (in addition to printing VIOLATED); exit 0 otherwise; exit 1 on RPC error. |
| `determ peers [--json] [--count] [--rpc-port N]` | Connected peer addresses. Default: one host:port per line. `--json` emits the raw RPC array (`["host:port", ...]`). **`--count` flag** prints a bare integer (count of connected peers; 0 if the RPC returns a non-array — defends monitoring dashboards against malformed responses). `--count` and `--json` compose: `--count --json` emits `{"count": N}` single-line for machine consumers. Useful one-shot for monitoring scripts (e.g., `if [ $(determ peers --count) -lt 3 ]; then alert; fi`). |
| `determ show-block <i> [--rpc-port N]` | Full block JSON at index `i` |
| `determ chain-summary [--last N] [--json] [--rpc-port N]` | Compact view of last `N` blocks (default 10). `--json` emits the raw RPC response (`{blocks: [...], height, total_supply, genesis_total, accumulated_subsidy, accumulated_inbound, accumulated_slashed, accumulated_outbound}` — the A1 unitary-supply counters). |
| `determ validators [--filter-region R] [--json] [--count] [--rpc-port N]` | Registered validator pool (domain, ed_pub, stake, active_from, region). Default: human-readable table including the region column (empty region renders as `(global)`). `--filter-region R` post-filters to validators whose region equals R (case-sensitive; useful in regional deployments to inspect the locally-eligible pool per rev.9 R1 region-pinned committee selection). `--json` emits the (filtered) RPC array verbatim — feeds directly into `verify-block-sigs --committee` for light-client K-of-K verification. **`--count` flag** prints a bare integer (count of validators after any `--filter-region` filter; 0 if RPC returns a non-array, defending dashboards against malformed responses). Composes with `--filter-region` (count just the regional subset — e.g., `determ validators --count --filter-region us-east` for "how many validators are eligible for the us-east committee?") and with `--json` (emits `{"count": N}` single-line for machine consumers). |
| `determ stakes [--top N] [--json] [--rpc-port P]` | List validators sorted by stake **descending**. Complements `validators` (which sorts alphabetically by domain). Operator use cases: (a) "Who are the top stakers?" — slashing impact analysis, committee-influence assessment, governance signal; (b) "Why does region X have low committee participation?" — sorted-by-stake view immediately shows whether the region's validators are minimally staked or fully staked. `--top N` truncates to top N entries; `--json` emits the (filtered + sorted) entries as a JSON array including an explicit 1-indexed `rank` field per entry. Tie-breaks by domain ascending. Zero-stake entries kept (may be pre-genesis registrants in DOMAIN_INCLUSION mode). Pulls from `validators` RPC + sorts client-side — no new server method. |
| `determ committee [--json] [--count] [--rpc-port N]` | Current epoch's K-of-K committee (deterministic from chain state). `--json` flag same as for `validators` — emits the raw RPC array shape `verify-block-sigs --committee` consumes. **`--count` flag** prints a bare integer (count of committee members; 0 if the RPC returns a non-array). Useful in regional deployments to monitor temporary under-quorum — a region may have `committee_size < K` until enough creators come back online. Composes with `--json` to emit `{"count": N}` for machine consumption. |
| `determ pending-params [--at-height N] [--json] [--rpc-port P]` | A5 Phase 2 governance visibility: list of staged PARAM_CHANGE entries (effective_height, name, value_hex, value_bytes) that have been validated and accepted but not yet activated. Sorted ascending by `effective_height`; within a bucket, in insertion order. Empty for `governance_mode=0` deployments. `--at-height N` post-filters to entries with `effective_height <= N` — useful for operators verifying that a planned upgrade window covers the expected param changes. `status` RPC also exposes the total count as `pending_param_changes` for monitoring drift. Default: human-readable table; `--json` pass-throughs the (filtered) RPC array. |
| `determ abort-records [--top N] [--json] [--rpc-port P]` | S-032 abort_records cache visibility: domains that have aborted in Phase-1, sorted by `count` descending (most-aborted first). Each entry is `{domain, count, last_block}`. Useful for diagnosing committee instability, BFT-escalation patterns, and which validators may need remediation. `--top N` truncates to the first N entries (client-side, since the RPC always returns the full sorted list). Empty for chains with no Phase-1 aborts. Phase-2 (timing-skew) aborts are NOT tracked here — they don't slash or accrue records. |
| `determ supply [--json] [--field <name>] [--rpc-port P]` | Compact A1 unitary-balance summary: `genesis_total` + 4 running counters (subsidy minted / inbound credited / slashed / outbound sent) + computed `expected_total` vs `live_total_supply` + invariant status (OK or VIOLATED). Useful for monitoring scripts that just want supply numbers without the per-block `chain-summary` detail. Pulls from the `chain_summary` RPC (last_n=1 for minimum payload) — no new server-side RPC method. **Exit code 2 on A1 invariant violation** (in addition to printing VIOLATED); exit 0 otherwise. **`--field <name>` flag** prints just the bare value of one field (one-shot scalar extraction for shell scripts; e.g., `determ supply --field live_total_supply` prints just the integer). Allowed: `live_total_supply`, `expected_total`, `genesis_total`, `accumulated_subsidy`, `accumulated_inbound`, `accumulated_slashed`, `accumulated_outbound`, `a1_invariant_ok` (`true`/`false`). Unknown field name → exit 1 with allowed-list diagnostic. In `--field` mode, exit 0 even on A1 violation (the `a1_invariant_ok` field already exposes that bit). |
| `determ tx-summary [--last N] [--by-type] [--json] [--rpc-port P]` | Per-type transaction count over the last `N` blocks (default 10). Operator capacity-monitoring tool: "how many TRANSFERs vs STAKEs vs DAPP_CALLs did we process in the last N blocks?". Useful for dashboards tracking workload mix. Pulls from `chain_summary` RPC (which already returns full block JSON including `transactions[].type`) — no new server-side method. Default human-readable output prints window (block range `[I..J]`), total tx count, and per-type breakdown (TRANSFER / STAKE / UNSTAKE / REGISTER / DEREGISTER / DAPP_REGISTER / DAPP_CALL / PARAM_CHANGE / COMPOSABLE_BATCH / MERGE_EVENT). `--by-type` is the default mode (kept as an explicit flag for forward-compat). `--json` emits a structured envelope `{total: N, by_type: {TRANSFER: N1, STAKE: N2, ...}, block_count: N, first_index: I, last_index: J}` (includes zero-count types omitted from human output). Empty chain prints "no blocks in window". Exit 1 only on RPC error or malformed response. |
| `determ chain-id [--rpc-port P]` | Print just the chain's genesis-block hash (canonical chain identity, 64-char hex). Useful for monitoring scripts cross-checking chain identity across nodes — `if [ "$(determ chain-id --rpc-port 7778)" != "$EXPECTED" ]; then alert; fi`. Pulls from `status` RPC. Exit 1 if no genesis loaded. |
| `determ block-hash <index> [--rpc-port P]` | Print just `compute_hash` of the block at `<index>` (64-char hex, no JSON envelope). Useful for cross-node fork-detection scripts — `[ "$(determ block-hash 100 --rpc-port 7778)" == "$(determ block-hash 100 --rpc-port 7779)" ]` → agree or FORKED. Pulls full block JSON from `block` RPC and extracts the `hash` field. Exit 1 if index out of range or hash field missing. |
| `determ block-info <index> [--field NAME] [--json] [--rpc-port P]` | Print one or more fields of a single block. Extension of `block-hash` (which only emits `compute_hash`) to any block field: `index`, `prev_hash`, `timestamp`, `tx_root`, `state_root`, `block_hash`, `consensus_mode`. Without `--field`, defaults to printing the full block JSON. With `--field NAME`, prints just the field value as bare scalar (no JSON envelope) for shell-script consumption. Operator examples: `determ block-info 100 --field state_root` (audit trail — "what was state_root at block 100?"); `determ block-info 100 --field timestamp` (chain progress velocity — "what was timestamp delta between blocks N and M?"). `block_hash` alias: maps to the server-side `hash` field. Unknown `--field` name → exit 1 with allowed-list diagnostic. Out-of-range index → exit 1. |
| `determ block-range <from> <to> [--field NAME] [--json] [--rpc-port P]` | Bulk-fetch a range of block fields via the `headers` RPC (inclusive on both ends). Builds on `block-hash` (single-height); operators doing fork detection across many heights pay one RPC + pagination round-trip per up-to-256 headers instead of one per height. Default output: one line per height `<index> <block_hash>`. `--field NAME` selects which field to emit and prints bare values (one per line) ready for `diff(1)` against another node's range: e.g. `diff <(determ block-range 100 200 --field state_root --rpc-port 7778) <(determ block-range 100 200 --field state_root --rpc-port 7779)`. Allowed `--field` values: `index`, `prev_hash`, `state_root`, `block_hash`, `timestamp`. `--json` emits a structured envelope `{headers: [...], from, to, received}`. Heights past the server's current height are silently truncated (honest reflection of chain state). Exit 1 on RPC error or invalid `--field` name. |
| `determ check-fork --node-a host:port --node-b host:port --from N --to M [--field NAME] [--json]` | Automated cross-node divergence detection. Fetches the requested range of block fields from BOTH nodes via headers RPC (paged 256/page; same path `block-range` uses internally) and reports the FIRST height where they disagree on `--field`. Real operator use: when two nodes report mismatched `state_root` at the same `height`, this command localizes the divergence to a specific block — operators can then drill into that block's `transactions` / `inbound_receipts` / `equivocation_events` to find which event caused the fork. Default field `state_root` (canonical chain-state commitment); `block_hash` also valuable for "did headers diverge?" checks. Allowed `--field` values match `block-range`. **Exit codes**: 0 ranges identical, 2 DIVERGENCE detected (operator alert gate — distinguishes from "no errors but found a problem"), 1 error (bad args, RPC error, unknown `--field`). `--json` emits `{identical: bool, first_divergence: N\|null, node_a_received: N, node_b_received: N, field, from, to}`. Example: `determ check-fork --node-a 127.0.0.1:7778 --node-b 127.0.0.1:7779 --from 100 --to 200`. |
| `determ head [--field height\|hash] [--json] [--rpc-port P]` | Print just the chain's current head — `<height> <head_hash>` space-separated by default. `--field height` prints just the height (no hash); `--field hash` prints just the hash (no height) — for `expr`-style scripts that need a single value. `--json` emits `{height, head_hash}` for structured scripts. For monitoring scripts tracking chain advancement (`while true; do read h hash <<< "$(determ head)"; ... done`). Pulls from `status` RPC. Exit 1 if chain not loaded or unknown `--field` value. |
| `determ show-account <addr> [--rpc-port N] [--json]` | Balance + nonce + registry record + stake for any address. Default human-readable; `--json` pass-throughs the raw RPC envelope for script consumption. Empty result → `{}` (JSON) or `(no on-chain state for X)` (human). |
| `determ show-tx <hash> [--rpc-port N] [--json]` | Locate a tx in a finalized block (block_index + payload). Default human-readable; `--json` pass-throughs the raw RPC envelope. Empty result → `{}` (JSON) or `(tx ... not found ...)` (human). |
| `determ where-is <addr\|domain> --shard-count N [--salt-hex HEX] [--json]` | Local-only shard-routing diagnostic. Given an address (anon-form `0x...64hex`) or a registered domain name, compute which shard it routes to via `crypto::shard_id_for_address(addr, shard_count, salt)`. Operator use cases: "Which shard does this anon address live on?" (incident analysis — "why is this balance not showing up on shard 2?"); "What shards do my faucet addresses route to?" (capacity planning — "are pre-mine recipients balanced across shards?"). `--shard-count N` required (from genesis `initial_shard_count`); `--salt-hex HEX` optional 64-hex `shard_address_salt` from genesis (defaults to zero salt for testnets / dev). Default output: `<address> -> shard <N> (of <shard-count>)`. `--json` emits `{"address": "<>", "shard": N, "shard_count": N}`. **Pure local computation — no RPC required.** |
| `determ balance <domain> [--rpc-port N]` | Balance only |
| `determ nonce <domain> [--rpc-port N]` | Next expected nonce |
| `determ stake_info <domain> [--rpc-port N]` | Locked stake + unlock_height |

## Wallet / transactions

### Anonymous bearer wallets

| Command | Purpose |
|---|---|
| `determ account create --out FILE [--allow-plaintext-stdout]` | Generate a fresh Ed25519 key + `0x`-prefixed bearer address. `--out` writes to a 0600-permissioned file. Bare `account create` (no `--out`) is refused (S-004); opt in to stdout via `--allow-plaintext-stdout`. |
| `determ account address <privkey_hex>` | Derive address from priv-key (offline, no daemon) |
| `determ send_anon <to> <amount> <privkey_hex> [--fee N] [--rpc-port N]` | Build, sign, submit a TRANSFER from a bearer wallet |

### Domain-authored (registered validator)

| Command | Purpose |
|---|---|
| `determ register --rpc-port N` | Register this node's domain (RPC host's key) |
| `determ send <to> <amount> [--fee N] [--rpc-port N]` | TRANSFER from this node's domain |
| `determ stake <amount> [--rpc-port N]` | Lock balance into stake |
| `determ unstake <amount> [--rpc-port N]` | Begin unstake (subject to UNSTAKE_DELAY) |

## Genesis tooling

| Command | Purpose |
|---|---|
| `determ genesis-tool peer-info <domain> [--data-dir D] [--stake N] [--region T]` | Emit JSON snippet for `initial_creators`. Optional `--region T` tags the entry with the operator's R1 region (only emitted when non-empty — preserves byte-identical output for existing CURRENT-mode + legacy callers). Required for EXTENDED-mode region-pinned shards: operator runs `peer-info myvalidator --stake 1000 --region us-east` instead of hand-editing the JSON before passing it to `genesis-tool build`. |
| `determ genesis-tool build <config.json> [--json]` | Compute genesis block + write `.hash` sidecar. Default human-readable summary listing chain_id, role, M/K, mode, inclusion model, BFT settings, creator + balance counts, and computed genesis hash. `--json` emits a single-line JSON object with the same fields plus `status:"ok"` / `error` envelope for CI consumption (e.g., a build pipeline that pins the genesis hash into deployment manifests). |
| `determ genesis-tool build-sharded <config.json>` | Produce 1 beacon + S shard genesis files (with shared `shard_address_salt`) |

## Snapshots (B6.basic — fast-bootstrap)

| Command | Purpose |
|---|---|
| `determ snapshot create [--out FILE] [--headers N] [--rpc-port N]` | Dump current chain state (accounts/stakes/registrants/dedup + tail headers) |
| `determ snapshot inspect --in FILE [--state-root <hex64>] [--json]` | Validate + summarize a snapshot file (round-trip via `restore_from_snapshot` — S-033 + S-038 gates fire on tampered state). Prints `block_index`, `head_hash`, `state_root`, account/stake/registrant counts, and chain parameters. Optional `--state-root` pins an externally-trusted root for trustless-fast-sync verification: prints `✓ matches --state-root` on agreement; exits non-zero with `FAIL` diagnostic if the snapshot's state_root doesn't match the supplied root (defeats a tampered snapshot pointed at a chain the operator doesn't trust). Optional `--json` emits a single-line JSON object with the same data for script consumption (`{"status":"ok","block_index":N,"head_hash":"...","state_root":"...","accounts":N,...}`); error cases also emit JSON (`{"error":"cannot_open"|"invalid_state_root_length"|"state_root_mismatch"|"exception","..."}`). Useful for bootstrap-orchestrator pipelines that consume snapshot verification results. |
| `determ snapshot fetch --peer host:port --out FILE [--headers N]` | Pull a snapshot from a running node over the gossip wire |
| `determ snapshot diff <file-a> <file-b> [--json]` | Compare two snapshot files and print divergence lines. Operator incident-analysis tool: if two nodes report mismatched state_root at the same height, snapshot each and run this command to localize the divergence to one of `block_index` / `head_hash` / `state_root`, account + stake + registry aggregate counts, balance/stake sums, or any of the 5 A1 unitary-supply counters. Both snapshots validated through `restore_from_snapshot` (malformed input fails fast); pure read-only comparison after. Default output: human-readable `FIELD: A != B` lines, or `(identical)` when equal. `--json` emits a structured envelope `{"a":"<path>","b":"<path>","identical":<bool>,"differences":[{"field":"<name>","a":<val>,"b":<val>},...]}` for script consumption. Exit 0 on success (differences are informational, not a test gate); exit 1 only on load failure. |
| `determ snapshot stats <file> [--json]` | Lightweight metadata-only snapshot inspection. Reads top-level JSON fields without running the full `Chain::restore_from_snapshot` pipeline. Useful for fast triage on large snapshot files before committing to `snapshot inspect`'s heavier restore + state_root verify path. Reports: file size on disk, version, block_index + head_hash, account/stake/registrant counts, and the 5 A1 unitary-supply counters. **Differences from `snapshot inspect`**: stats is O(parse + top-level field read) — fast even on 100 MB+ snapshots; inspect is O(parse + full restore + state_root compute) — slower but verifies S-033/S-038 internal consistency. Default human output is human-readable column; `--json` emits structured envelope `{file, size_bytes, version, block_index, head_hash, accounts, stakes, registrants, genesis_total, accumulated_*}`. Exit 0 on success; exit 1 on file-open / JSON-parse / not-object errors (JSON error responses also emitted to stdout when `--json` is set). |

To bootstrap from a snapshot, set `snapshot_path` in the node's `config.json` and `determ start`. The node will skip per-block replay and install state directly.

## State commitment + light-client RPC (v2.1 + v2.2)

| Command | Purpose |
|---|---|
| `determ state-root [--rpc-port N]` | Print the chain's Merkle state root + height + head_hash. Returns the live `compute_state_root()` value, which post-S-038 also matches the value stored in the head block's `state_root` field (the producer wires this on every finalized block). Operators can call this against multiple nodes to detect silent state divergence — pre-S-038 a real S-030 D1/D2 attack would manifest at the apply layer only; post-S-038, divergent nodes loud-fail at apply time with a `state_root mismatch` diagnostic before ever producing a divergent block. |
| `determ headers [--from N] [--count M] [--rpc-port P]` | Fetch a slice of block headers (Block JSON minus transactions / cross_shard_receipts / inbound_receipts / initial_state). Light-client header-sync primitive: returned headers carry committee + signatures + tx_root + delay_seed / delay_output + cumulative_rand + state_root for verify-state-proof anchoring, plus an explicit `block_hash` per header so the prev_hash chain is verifiable without re-deriving compute_hash (which needs the stripped heavy fields). Defaults: `--from 0`, `--count 16`. Server caps count at 256. Out-of-range `--from` returns an empty array (not an error). Returns `{headers, from, count, height}`. |
| `determ verify-headers [--in <file>] [--genesis-hash <hex64>] [--prev-hash <hex64>]` | Verify the prev_hash chain in a `determ headers` response. Reads JSON from `--in` or stdin. Walks consecutive header pairs and asserts `header[i].prev_hash == header[i-1].block_hash`. Optional anchors: `--genesis-hash` checks against the genesis block_hash when the slice starts at index 0; `--prev-hash` anchors a non-genesis slice (= block_hash of the block immediately before the slice). Without an anchor, the first header's prev_hash is unanchored — internal chain links still verified. Pure chain-of-hashes integrity check (does NOT verify committee signatures; use `verify-block-sigs` for that). |
| `determ verify-block-sigs --header <file> --committee <file> [--bft]` | Verify K-of-K committee Ed25519 signatures on a single block header. Reads the header from `--header` (accepts either a single-block JSON or a `determ headers` envelope — extracts the first header from envelope shape) and a committee pubkey map from `--committee` (a JSON array of `{domain, ed_pub}` entries, or `{members: [...]}` envelope shape — `determ validators --json` and `determ committee --json` both emit this shape directly). Computes `compute_block_digest(b)` over the header fields and verifies each `creators[i]`'s `creator_block_sigs[i]` against the looked-up pubkey. MD mode requires every signature; `--bft` allows up to `K - ceil(2K/3)` sentinel-zero slots. Together with `verify-headers` (chain integrity) + `verify-state-proof` (per-field state), this completes the v2.2 trustless light-client verification chain: fetch headers from any source, verify chain links + committee signatures locally, anchor state_root from a verified header, verify state-proofs against that root. |
| `determ state-proof --ns {a\|s\|r\|d\|b\|k\|c} --key <name> [--rpc-port N]` | Fetch a Merkle inclusion proof for any state entry. RPC-exposed namespaces: `a` = accounts, `s` = stakes, `r` = registrants, `d` = DApp registry (v2.18, key = DApp's owning domain), `b` = abort_records (S-032), `k` = genesis-pinned constants, `c` = A1 counters (via the composite `k:c:<name>` lookup). The full ten-namespace state tree (PROTOCOL.md §4.1.1) also has `i/m/p` but those use composite keys and aren't surfaced by this RPC in v2.2. Light-client primitive: the proof verifies against the current `state_root`, which is committed into the head block's `block_hash` (compute_hash via signing_bytes; the chain's `prev_hash` chain transitively authenticates it forward). |
| `determ verify-state-proof [--in <file>] [--state-root <hex64>]` | Local light-client demonstrator: verifies a state-proof response via `crypto::merkle_verify` without trusting the responding node. Reads JSON from `--in` or stdin. Optional `--state-root <hex64>` pins an externally-trusted root (real light-client mode); without it, the proof's self-claimed root is used (self-consistency check). Prints `OK` + structured summary on success; `FAIL` + reason on tampering / mismatch. Pair with `state-proof`: `determ state-proof --ns a --key alice \| determ verify-state-proof --state-root <trusted-hex>`. |
| `determ verify-genesis --in <file> [--expected-hash <hex64>] [--json]` | Standalone genesis.json validator. Loads, applies the same parsing + sane-bounds checks as `determ start` (LOTTERY-multiplier ≥ 2 if subsidy_mode == LOTTERY, block_subsidy / subsidy_pool_initial / zeroth_pool_initial ≤ 1e18, lottery_multiplier × block_subsidy ≤ 1e18, genesis_message ≤ 256 bytes), computes the chain-identity hash via `compute_genesis_hash`, prints a structured summary. Default output is human-readable; `--json` emits a single-line JSON object for script consumption (`{"status":"ok","genesis_hash":"...","chain_id":"...","m_creators":N,...}`). Optional `--expected-hash <hex64>` pins against an externally-trusted value — exits non-zero with a clear diagnostic when computed hash doesn't match (defeats config-rewrite attacks where a deployment template smuggles in a different chain identity). The output explicitly distinguishes operational params NOT bound to identity hash (m_creators / k_block_sigs / block_subsidy / min_stake / initial_shard_count / bft_enabled — see S-039 in `docs/SECURITY.md`) from identity-bound fields (genesis_message / committee_region), so operators can spot mismatches in unbinded fields by direct inspection. Workflows: pre-deployment validation, cross-team coordination on chain identity, cluster onboarding. |

### Complete v2.2 trustless light-client verification flow

The seven CLIs above (`state-root`, `headers`, `verify-headers`, `verify-block-sigs`, `state-proof`, `verify-state-proof`, plus `validators --json` / `committee --json` for committee pubkey export) compose into an end-to-end trustless verification pipeline. Each step verifies a different property locally without trusting the responding node, gated only by an externally-pinned `--state-root` / committee pubkey set:

```bash
# 1. Fetch recent headers (untrusted full node OK; we'll verify them).
$DETERM headers --rpc-port 8771 --from 0 --count 100 > headers.json

# 2. Verify the prev_hash chain (catches re-ordered / spliced headers).
$DETERM verify-headers --in headers.json   # OK on valid chain

# 3. Get the committee pubkeys for K-of-K signature verification.
#    For genesis-pinned light clients, the operator hard-codes this set;
#    for chains using key rotation, derive from a previously-verified
#    state-proof of the registrants_ map (namespace `r`).
$DETERM validators --rpc-port 8771 --json > committee.json

# 4. Verify K-of-K committee signatures on each header.
$DETERM verify-block-sigs --header headers.json --committee committee.json

# 5. Extract a verified state_root from one of the headers (any will do
#    since we've verified the chain links + signatures).
STATE_ROOT=$(jq -r '.headers[-1].state_root' headers.json)

# 6. Verify any state field against the pinned state_root.
$DETERM state-proof --rpc-port 8771 --ns a --key alice > proof.json
$DETERM verify-state-proof --in proof.json --state-root "$STATE_ROOT"

# 7. (Alternative for whole-state) Verify a downloaded snapshot.
$DETERM snapshot inspect --in snapshot.json --state-root "$STATE_ROOT"
```

What this proves: a malicious full node serving headers / state-proofs / snapshots is caught at one of these checks. The trust chain bottoms out at the externally-pinned committee + the chain-of-hashes link from the verified header forward; the state_root anchor defeats fabricated-root attacks; the committee-sig check defeats forged-header attacks; the prev_hash chain check defeats spliced-header attacks.

## DApp substrate RPC (v2.18 + v2.19)

The DApp's identity is its owning domain (`tx.from` at registration). There is no separate "dapp_id" — the registered Determ domain IS the dapp identifier. CLI verbs that name a DApp use `--from <domain>` for tx-authoring and `--domain <D>` for queries.

| Command | Purpose |
|---|---|
| `determ submit-dapp-register --priv <hex> --from <domain> --service-pubkey <64hex> --endpoint-url <url> [--topics t1,t2,t3] [--retention 0\|1] [--metadata-hex <hex>] [--fee <N>] [--rpc-port N]` | Register / update a DApp on the chain. Idempotent — re-registering with the same `--from` updates the entry. `--service-pubkey` is the libsodium box pubkey used for end-to-end DAPP_CALL encryption. `--retention 0` = full retention, `1` = pruneable-after-K. |
| `determ submit-dapp-register --priv <hex> --from <domain> --deactivate [--fee <N>] [--rpc-port N]` | Deactivate the DApp owned by `--from`. Sets `inactive_from = current_height + DAPP_GRACE_BLOCKS`; in-flight calls finish within the grace window. |
| `determ submit-dapp-call --priv <hex> --from <sender> --to <dapp-domain> [--topic <T>] [--payload-hex <hex>] [--amount <N>] [--fee <N>] [--rpc-port N]` | Submit a DAPP_CALL routed to `--to`. `--topic` must match one of the DApp's registered topics (or `""`). `--payload-hex` is the application's opaque ciphertext (typically AEAD(`service_pubkey`, plaintext, nonce); ≤ 16 KB). `--amount` is an optional payment credited to the DApp's account. |
| `determ dapp-list [--prefix P] [--topic T] [--rpc-port N]` | List registered DApps. Optional `--prefix` filters by domain prefix; `--topic` keeps only DApps whose registered topic list contains a match. |
| `determ dapp-info --domain <D> [--rpc-port N]` | Per-DApp record: `domain`, `service_pubkey`, `endpoint_url`, `topics`, `retention`, `metadata`, `registered_at`, `active_from`, `inactive_from`. |
| `determ dapp-messages --domain <D> [--from <H>] [--to <H>] [--topic <T>] [--rpc-port N]` | Retrospective DAPP_CALL poll. Returns up to 256 events (`DAPP_MESSAGES_PAGE_LIMIT`) in `[from, to]` addressed to `--domain`, optionally topic-filtered. Use the response's `last_scanned + 1` as the next `--from` to paginate. |

## In-process tests (test-* CLI subcommands)

These are deterministic, network-free smoke tests embedded as CLI subcommands. They build an in-process chain + node and assert protocol invariants. Each `tools/test_*.sh` shell script invokes the corresponding `determ test-*` subcommand and asserts the result.

| Command | Purpose | Paired shell test |
|---|---|---|
| `determ test-atomic-scope` | A9 Phase 2D nested-scope rollback primitive | `tools/test_atomic_scope.sh` |
| `determ test-composable-batch` | COMPOSABLE_BATCH all-or-nothing semantics under partial failure | `tools/test_composable_batch.sh` |
| `determ test-dapp-register` | v2.18 DAPP_REGISTER apply path | `tools/test_dapp_register.sh` |
| `determ test-dapp-call` | v2.19 DAPP_CALL routing + apply path | `tools/test_dapp_call.sh` |
| `determ test-s018-json-validation` | S-018 closure: `json_require<T>` / `json_require_hex` / `json_require_array` helpers + converted `from_json` paths across `chain/block.cpp` + `node/producer.cpp` + `chain/genesis.cpp` + `net/messages.cpp` + `crypto/keys.cpp` surface clear field-name diagnostics on malformed gossip / RPC / snapshot / keyfile input (10 assertions including the new `json_require_array` "expected array, got X" path) | `tools/test_s018_json_validation.sh` |
| `determ test-merkle` | v2.1 Merkle primitives (S-035 Option 1 seed): `crypto::merkle_root` + `merkle_proof` + `merkle_verify` + `merkle_leaf_hash` + `merkle_inner_hash` over balanced + unbalanced + edge-case (empty / single-leaf) leaf sets. 12 assertions covering round-trip, tampering detection (value_hash / sibling-hash / target_index), domain separation (leaf vs inner), determinism, and sort-invariance | `tools/test_merkle.sh` |
| `determ test-committee-selection` | Committee-selection primitives (S-035 Option 1 seed): `crypto::select_m_creators` (S-020 hybrid — both rejection-sampling at 2K≤N and partial-Fisher-Yates at 2K>N branches), `select_after_abort_m`, `epoch_committee_seed`. 13 assertions covering determinism, seed-sensitivity, branch coverage at both sides of the 2K vs N threshold, edge cases (K=N, K=1), distinct-without-replacement, in-range invariant, and shard-salt sensitivity. Foundation tests for FA1 / FA2 / FA5 / FA8 | `tools/test_committee_selection.sh` |
| `determ test-shard-routing` | Cross-shard routing primitive (S-035 Option 1 seed): `crypto::shard_id_for_address` (salted SHA-256). 7 assertions covering single-shard degenerate case, determinism, in-range invariant, salt-sensitivity, distribution uniformity (chi-squared sanity on 1000 addresses × 4 shards), case-sensitivity, empty-address handling. Foundation test for FA7 cross-shard receipt atomicity — every cross-shard tx's destination is derived through this function | `tools/test_shard_routing.sh` |
| `determ test-ed25519` | Ed25519 sign/verify foundation (S-035 Option 1 seed): `crypto::sign` / `crypto::verify` / `generate_node_key`. 10 assertions covering key-shape, round-trip, tampering rejection (message / signature / pubkey), RFC-8032 determinism, empty-message edge case, distinct-key distinct-sig property, cross-key verify rejection, 4 KB long-message streaming path. Foundation test for FA1 / FA2 / FA5 / FA6 / FA7 / FA10 — every signature claim in the protocol reduces to Ed25519 EUF-CMA, so a regression here cascades across every safety theorem | `tools/test_ed25519.sh` |
| `determ test-sha256` | SHA-256 wrapper + Big-Endian encoding foundation (S-035 Option 1 seed): `crypto::sha256` + `SHA256Builder`. 10 assertions covering NIST FIPS 180-4 published test vectors (empty input, "abc", 56-byte input), `SHA256Builder` ↔ one-shot equivalence, 3-piece incremental append correctness, **Preliminaries §1.3 big-endian uint64_t / int64_t encoding** (locks in the cross-platform consensus determinism contract — a regression to little-endian would silently fork the protocol across architectures). Foundation test under every hash claim in the codebase | `tools/test_sha256.sh` |
| `determ test-anon-address` | Anon-address helpers (S-035 Option 1 seed): `is_anon_address` / `normalize_anon_address` / `parse_anon_pubkey` / `make_anon_address`. 12 assertions covering S-028 case-insensitive parsing (accepts lower/upper/mixed-case), invalid-input rejection (missing 0x, wrong length, non-hex, registered-domain name), case-normalization to canonical lowercase, round-trip via make/parse, and registered-domain pass-through. Unit-level counterpart to `tools/test_anon_address_case.sh` (which exercises the same surface end-to-end through 3-node RPC) | `tools/test_anon_address.sh` |
| `determ test-genesis-message` | `GenesisConfig::genesis_message` hash-mixing contract (S-035 Option 1 seed). 10 assertions covering the three contract rules: (1) default-skips-mix (backward-compat invariant — pre-message genesis files retain byte-identical chain hashes), (2) custom value (including explicit empty string) yields distinct chain hash, (3) 256-byte size cap with JSON-load validation. Plus determinism, JSON round-trip, absent-key default-fallback, and boundary acceptance. Locks in the operator-facing inscribed-message feature against regressions that would either silently break existing chain identity OR allow chain-identity collisions | `tools/test_genesis_message.sh` |
| `determ test-state-root` | `Chain::compute_state_root()` commitment algebra (S-035 Option 1 seed). 13 assertions over the S-033 / v2.1 state-Merkle surface: determinism (two identical Chains → identical roots — the K-of-K consensus precondition); purity (10 sequential calls on an unmodified Chain return the same hash); non-zero baseline (default Chain has non-zero root because the "k:" namespace always emits leaves); per-field sensitivity for every public `set_*()` that maps into a k:-namespace leaf (block_subsidy, min_stake, subsidy_pool_initial, lottery_jackpot_multiplier, subsidy_mode, shard_routing count+id+salt); invertibility (change-then-revert returns to the original root — no hidden mutation state); cross-namespace distinction (two different mutations produce two different alternate roots — no accidental collisions); order independence (setter call order doesn't affect root — leaves sorted internally). Foundation-level counterpart to network-level `tools/test_state_root.sh` (multi-node RPC) and `tools/test_dapp_snapshot.sh` (S-037 + S-038 snapshot-restore contract) | `tools/test_state_root_unit.sh` |
| `determ test-block-rand` | V8 randomness primitives (S-035 Option 1 seed): `compute_delay_seed` (Phase-1 inputs commitment), `compute_block_rand` (Phase-2 output), `proposer_idx` (BFT-mode designated proposer), `required_block_sigs` (MD vs BFT quorum), `count_round1_aborts` (suspension + escalation tally). 21 assertions: determinism + every-input-field sensitivity for both hash functions + creator_dh_inputs / ordered_secrets ORDER sensitivity (the committee-selection-order contract that pairs Phase-1 commits with Phase-2 reveals — without this, a malicious gather could reorder reveals to bias future randomness); domain separation between compute_delay_seed and compute_block_rand; proposer_idx in-range invariant + abort-rotation mechanism + empty-committee short-circuit; required_block_sigs golden vectors for MD = K and BFT = ceil(2K/3) (K = 1..12); count_round1_aborts round-2 filter. FA1 / FA5 / FA8 foundation — every future committee derives from compute_block_rand → epoch_committee_seed → select_m_creators | `tools/test_block_rand.sh` |
| `determ test-rate-limiter` | S-014 token-bucket rate limiter (S-035 Option 1 seed): `net::RateLimiter` — the shared helper used identically by RpcServer and GossipNet. 16 assertions: default-disabled bypass, configure(0,0) explicit-disable, configure(>0,>0) enables + getter round-trip, first-touch starts FULL (legitimate callers don't get hit cold), burst exhaustion (4th consume fails at burst=3 same-instant), per-key independence (exhausting key A doesn't throttle key B — the central security property), reconfigure-takes-effect-on-next-consume, refill timing (100ms sleep at rate=20/s yields ≥1 new token), burst-cap invariant (long sleep at high rate does NOT exceed burst — defeats overflow attacks), 100-distinct-keys-each-consume-2 scale check. Unit-level counterpart to `tools/test_rpc_rate_limit.sh` + `tools/test_gossip_rate_limit.sh` (which exercise the same algebra end-to-end via wire) | `tools/test_rate_limiter.sh` |
| `determ test-block-digest` | `compute_block_digest` — the FA1 signature target at Phase-2 of K-of-K consensus (S-035 Option 1 seed). 19 assertions covering both contracts: (a) INCLUSION list — every field in compute_block_digest changes the digest when mutated (index, prev_hash, tx_root, delay_seed, consensus_mode, bft_proposer, creators, creator_tx_lists, creator_ed_sigs, creator_dh_inputs); (b) EXCLUSION list (S-030 D2 fence) — fields NOT in compute_block_digest MUST NOT change it (delay_output + creator_dh_secrets + cumulative_rand are Phase-2-reveal; abort_events + equivocation_events + timestamp are v2.7 F2 territory; state_root is apply-time-gated via S-033 + S-038; partner_subset_hash for R4 Phase 3 merge). Locks the digest at exactly the surface FA1 / S-030 D2 / v2.7 F2 assume. Cross-reference: `docs/proofs/S030-D2-Analysis.md` §1 + `docs/proofs/F2-SPEC.md` §1 | `tools/test_block_digest.sh` |
| `determ test-block-hash` | `Block::signing_bytes()` + `Block::compute_hash()` — the FA1 chain-anchor identity (S-035 Option 1 seed). compute_hash binds EVERY consensus-relevant field of the block, including Phase-2-reveal fields and apply-time-recomputed fields, so its output becomes the prev_hash on every subsequent block. 16 assertions: determinism + purity (3); field-sensitivity for timestamp, delay_output, creator_dh_secrets, cumulative_rand, creator_block_sigs (5); zero-skip backward-compat for partner_subset_hash (R4 Phase 3) and state_root (S-033) — both fields contribute to the hash ONLY when non-zero, preserving byte-identical hashes for pre-feature blocks (5); creators[] ORDER sensitivity + S-030 D2 mitigation at chain-anchor level (two same-digest blocks differing in equivocation_events still have different hashes) + abort_events event_hash binding (3) | `tools/test_block_hash.sh` |
| `determ test-binary-codec` | Wire-format codec (A3 / S8) + S-022 cap table (S-035 Option 1 seed). 35 assertions across four blocks: (a) JSON envelope (v0) round-trip for HELLO + STATUS_REQUEST + TRANSACTION — types and payloads preserved byte-for-byte; (b) binary envelope (v1) round-trip for STATUS_RESPONSE + CONTRIB via both `Message::serialize_binary` + `Message::deserialize` (format-detecting) and direct `encode_binary` / `decode_binary`, plus `is_binary_envelope` detection contract (returns true on binary, false on JSON); (c) malformed-input rejection (garbage bytes; truncated valid JSON); (d) S-022 per-MsgType `max_message_bytes` golden vectors for all enumerated MsgType variants (16 MB tier: SNAPSHOT_RESPONSE / CHAIN_RESPONSE; 4 MB tier: BLOCK / BEACON_HEADER / SHARD_TIP / CROSS_SHARD_RECEIPT_BUNDLE / HEADERS_RESPONSE; 1 MB tier: HELLO / CONTRIB / BLOCK_SIG / ABORT_CLAIM / ABORT_EVENT / EQUIVOCATION_EVIDENCE / TRANSACTION / STATUS_REQUEST / STATUS_RESPONSE / GET_CHAIN / SNAPSHOT_REQUEST / HEADERS_REQUEST + default-tight 1 MB fence for future MsgType additions). Locks the wire format + cap table against silent regression at both the encode/decode asymmetry and S-022 boundary | `tools/test_binary_codec.sh` |
| `determ test-wire-types` | Block-internal wire types JSON round-trip + S-018 strict-rejection (S-035 Option 1 seed). 38 assertions covering `CrossShardReceipt` (FA7 / V12 source-side receipt — 10 fields), `AbortEvent` (FA3 abort certificate — 4 fields), `EquivocationEvent` (FA6 slashing evidence — 8 fields), `GenesisAlloc` (chain-identity allocation — 5 fields incl. R1 empty-region backward-compat). Plus S-018 strict-rejection lock-in for all four types: missing required field throws with a clear field-name diagnostic; wrong-length hex throws too. CrossShardReceipt::from_json was hardened in the same commit (previously permissive via `j.value()` defaults; now uses `json_require` / `json_require_hex` to match the rest of the S-018 surface) — defense in depth at the parse layer even though receipts are also bound into the parent block's signing_bytes via K-of-K | `tools/test_wire_types.sh` |
| `determ test-transaction` | `Transaction::signing_bytes` + `compute_hash` + Ed25519 sign/verify + JSON round-trip (S-035 Option 1 seed). 34 assertions: signing_bytes determinism + per-field sensitivity (8 core fields) + sig/hash EXCLUSION (would be circular — sender signs over their OWN signing bytes); compute_hash == SHA-256(signing_bytes) golden contract; Ed25519 sign + tampered-tx-fails verification round-trip; full JSON round-trip for TRANSFER + type-preservation round-trip for each of 9 enum variants (REGISTER through DAPP_CALL); S-018 strict-rejection (missing 'amount' + wrong-length 'sig' hex); unique-tx-identity contract (txs differing in nonce alone have distinct compute_hash — the mempool-dedup foundation) | `tools/test_transaction.sh` |
| `determ test-merge-event-codec` | `MergeEvent::encode` / `::decode` (R4 under-quorum merge wire format; S-035 Option 1 seed). 23 assertions: BEGIN + END round-trips with empty-region preservation, size invariant (encode size = 26 + region_len), decode rejection paths (too-short payload, invalid event_type > 1, region_len > 32 cap, size mismatch), determinism + per-field sensitivity, maximum-region (32 bytes) round-trip. Locks in the canonical-binary wire format that drives cross-shard merge coordination — a regression here would diverge the apply path across shards | `tools/test_merge_event_codec.sh` |
| `determ test-consensus-msgs` | ContribMsg + BlockSigMsg + AbortClaimMsg + commitment-hash helpers (S-035 Option 1 seed). 40 assertions: `make_contrib_commitment` determinism + per-input sensitivity + critical tx_hashes ORDER sensitivity (the sorted-ascending contract — wrong order produces commit that doesn't match peers); `make_abort_claim_message` determinism + per-input sensitivity (including round — defeats Phase-1 vs Phase-2 replay); domain separation between contrib commit and abort claim hash (no cross-domain collision); full JSON round-trip for all three message types (15 fields total); make_contrib produces a sig that verifies under signer's pubkey via real Ed25519 | `tools/test_consensus_msgs.sh` |
| `determ test-tx-root` | `compute_tx_root` — the K-committee union-of-tx-hashes commitment (S-035 Option 1 seed). 10 assertions: union semantics ({A,B} ∪ {B,C} == {A,B,C}, NOT intersection {B}), dedup across lists, list permutation invariance (which member proposes what doesn't affect the canonical root), empty inner list invariance, sensitivity to added tx. The FA2 censorship-resistance primitive — a regression to intersection semantics (S-025 deleted; commented out in producer.cpp) would silently let one member exclude txs | `tools/test_tx_root.sh` |
| `determ test-genesis` | `compute_genesis_hash` + `make_genesis_block` — chain-identity origin (S-035 Option 1 seed). 22 assertions: determinism + chain_id sensitivity; **lock-in of S-039 diagnostic-UX gap** (m_creators / k_block_sigs / block_subsidy / min_stake / initial_shard_count / bft_enabled are NOT bound into the hash — two operators with mismatched configs at same chain_id get cryptic consensus failures rather than a "config mismatch" diagnostic; fix is wire-compat break, deferred); fields that ARE bound (shard_id, chain_role, suspension_slash + merge_threshold_blocks when non-default, genesis_message, committee_region when non-empty); make_genesis_block invariants (index 0, prev_hash zero, compute_hash matches compute_genesis_hash); JSON round-trip preserves identity hash; oversized genesis_message rejected; **S-018 defense-in-depth lock-in** — wrong-type rejection on `initial_creators` / `initial_balances` / `param_keyholders` (the three optional-collection fields in GenesisConfig — genesis files are operator-edited and a typo making any one a scalar/object would previously throw an opaque nlohmann error, now throws clean field-name diagnostic) | `tools/test_genesis.sh` |
| `determ test-envelope` | `wallet/envelope.hpp` AES-256-GCM + PBKDF2-HMAC-SHA-256 AEAD wrapping primitive (A2 Phase 2 wallet recovery share envelopes + S-004 option 2 passphrase-encrypted keyfiles; S-035 Option 1 seed). 27 assertions across four blocks: (1) encrypt/decrypt round-trip with matching pw + AAD; (2) envelope shape (salt >= 16B, nonce == 12B, ciphertext == pt_size + 16B GCM tag, pbkdf2_iters + aad round-trip); (3) AEAD safety properties (wrong-pw / empty-pw / mismatched-AAD / tampered-ct / tampered-tag all fail; fresh salt + nonce per encryption guarantee distinct ciphertexts from same plaintext+passphrase — defeats artifact-correlation attacks); (4) serialize / deserialize canonical hex round-trip + bad-input rejection (garbage, truncated); (5) edge cases (empty plaintext yields 16B tag-only ciphertext; empty AAD round-trips). A regression here would silently weaken every encrypted wallet artifact | `tools/test_envelope.sh` |
| `determ test-resolve-fork` | `Chain::resolve_fork` — S-029 BFT-mode fork-choice rule (S-035 Option 1 seed). 10 assertions covering the documented decision priority: (1) heaviest sig set wins (counts non-zero `creator_block_sigs` entries — sentinel-zero slots in BFT mode don't count toward weight); (2) tie → fewer `abort_events` wins; (3) tie → smallest block_hash (lexicographic, deterministic across peers). Plus edge cases: identical blocks return first arg (final-tie deterministic); zero-sigs case still resolves without crash; sentinel-zero handling; abort-tie-break beats hash-tie-break (priority order). A regression would either let the wrong block win (peers diverge on canonical tip → FA1 violation) or make resolution non-deterministic across nodes | `tools/test_resolve_fork.sh` |
| `determ test-shamir` | Shamir's Secret Sharing over GF(2^8) (wallet/shamir.cpp, A2 Phase 1 wallet recovery primitive; S-035 Option 1 seed). 21 assertions covering: T-of-N reconstruction (3-of-5 round-trip, all 10 subsets verified, T+1 works, T-1 doesn't); share-shape invariants (distinct x-coordinates, no x=0 since Lagrange evaluates at x=0, y-size matches secret size, two independent splits produce different polynomial coefficients); degenerate thresholds (T=1 single-share, T=N all-shares-required); empty-secret edge case (split produces empty-y shares; combine rejects with nullopt per documented behavior); invalid-input rejection (threshold=0 / threshold > share_count / empty share list / duplicate x / mismatched y-sizes). A regression here would either weaken the threshold (information leak with < T shares) or break wallet recovery | `tools/test_shamir.sh` |
| `determ test-random-state` | Random-state primitives in `crypto/random.cpp` — `compute_dh_output` (2-share fold) + `compute_dh_output_m` (M-share fold) + `update_random_state` (per-block chain) + `compute_abort_hash` + `chain_abort_hash` (S5 anti-cartel defense) + `genesis_random_state` (block-0 seed). 28 assertions: determinism + argument-order sensitivity + per-input sensitivity for each function. Critical: the aborting_node sensitivity assertion in `compute_abort_hash` locks the S5 anti-cartel-navigation defense — committee re-selection after an abort must depend on WHO aborted, so an attacker can't pre-plan abort sequences to navigate into a favorable committee. The committee-selection-order contract for `compute_dh_output_m` matches the same invariant in test-block-rand's compute_delay_seed/compute_block_rand assertions (foundation layer below those higher-level helpers) | `tools/test_random_state.sh` |
| `determ test-snapshot-defense` | S-018 defense-in-depth lock-in for `Chain::restore_from_snapshot` (S-035 Option 1 seed). 12 assertions verifying that every optional collection field (accounts / stakes / registrants / applied_inbound_receipts / merge_state / abort_records / dapp_registry / pending_param_changes) throws a clean S-018 diagnostic naming the field when sent as a scalar / number / object instead of an array. Baseline (minimal valid snapshot) loads cleanly; backward-compat case (empty optional fields) still loads. Snapshots arrive via SNAPSHOT_RESPONSE gossip (16 MB cap — only unbounded-tier channel) so wrong-type rejection at the parse layer is the attack-facing diagnostic | `tools/test_snapshot_defense.sh` |
| `determ test-encoding` | `types.hpp` encoding helpers (S-035 Option 1 seed). 25 assertions covering `to_hex` (bytes-to-hex, lowercase, leading-zero preservation, templated overload for Hash/Signature), `from_hex` (case-insensitive parse via std::stoul base-16; rejects odd length), `from_hex_arr<N>` (length-checked array form; rejects short + long inputs), `to_string(ChainRole)` (single/beacon/shard), `to_string(ShardingMode)` (none/current/extended), cross-helper round-trip (Hash → to_hex → from_hex_arr<32> preserves 32 bytes), determinism (no internal-state leak from std::ostringstream), and `now_unix()` post-2017 sanity. **Foundation under every hex serialization** in the codebase — Block JSON, Transaction sig, Merkle leaf hashing, RPC output, snapshot format, light-client headers all transit through these helpers | `tools/test_encoding.sh` |
| `determ test-chain-helpers` | `Chain` read-side API surface (S-035 Option 1 seed). 25 assertions covering `balance` / `next_nonce` / `stake` (locked + `_lockfree` variants used by concurrent RPC handlers); `height` / `empty` / `head_hash`; `shard_count` / `my_shard_id` / `shard_salt` / `is_cross_shard` (single-shard degenerate case + multi-shard local/remote distribution); operator-tunable setter round-trips (`set_block_subsidy` / `set_min_stake` / `set_suspension_slash` / `set_unstake_delay`); A1 supply counters all zero on default chain. Locks safety-critical defaults: `balance(unknown)==0` (defeats accidental crediting on read), `next_nonce(unknown)==0` (first tx uses nonce 0), `shard_count==1` (default = single-shard, is_cross_shard==false unconditionally) | `tools/test_chain_helpers.sh` |
| `determ test-json-validate` | S-018 foundation helpers (`json_validate.hpp`) direct unit test (S-035 Option 1 seed). 24 assertions covering all three helpers (`json_require<T>`, `json_require_hex`, `json_require_array`) — happy paths + every error path. Locks the error-message contract that operators rely on for triage of malformed gossip / RPC inputs: missing field error contains field name + "S-018" prefix + "missing" keyword; wrong type error contains field name + "wrong type" + nlohmann detail; wrong hex length error explicitly states "expected N chars, got M chars" counts; wrong array type error states observed type ("got string" / "got object"). Empty array accepted (size=0 valid). Field-name uniqueness verified with unusual identifier chars. Foundation under every S-018-hardened from_json in the codebase | `tools/test_json_validate.sh` |
| `determ test-block-roundtrip` | `Block::to_json` / `Block::from_json` full field-set round-trip (S-035 Option 1 seed). 41 assertions covering minimal block (required fields), block with transactions, full K-of-K committee block (creators + creator_tx_lists + creator_ed_sigs + creator_dh_inputs + creator_dh_secrets + creator_block_sigs + tx_root + delay_seed + delay_output), BFT-mode block (consensus_mode + bft_proposer), block with abort_events / equivocation_events / cross_shard_receipts (V12) + inbound_receipts (V13) / initial_state (genesis), zero-skip fields (state_root + partner_subset_hash OMITTED from JSON when zero, PRESENT when non-zero), and the CRITICAL `compute_hash` invariance through JSON round-trip (gossip sender + receiver MUST compute the same block_hash or the prev_hash chain breaks) | `tools/test_block_roundtrip.sh` |
| `determ test-config-roundtrip` | `Config::to_json` / `Config::from_json` — operator config save+reload round-trip (S-035 Option 1 seed). 49 assertions covering default Config round-trip (every documented default — listen_port=7777, rpc_port=7778, rpc_localhost_only=true [S-001 secure default], bft_enabled=true, etc.), custom Config full-field round-trip across all 32 operator-tunable fields (ports / peers / rate-limits / regions / sharding mode / governance flags / timing knobs), empty-JSON → defaults path (permissive contract intentional for operator-facing config), and enum integer encoding for chain_role + sharding_mode. Permissive design preserved: missing optional fields silently default rather than throw (strict S-018 rejection reserved for wire-format peer messages) | `tools/test_config_roundtrip.sh` |
| `determ test-tx-binary-codec` | Transaction binary codec round-trip (S-035 Option 1 seed). 24 assertions covering the v1 binary wire-format path for TRANSACTION MsgType (`encode_tx_frame` / `decode_tx_frame` exercised via the public `encode_binary` / `decode_binary` Message-level API). **S-002 critical path**: amount/fee/nonce live in the FIXED 4×32-byte slot area (not the trailer); pre-S-002 closure the decoder dropped these during binary transit, letting corrupted txs into the mempool. Covers TRANSFER full round-trip + S-002 sig-verify invariant (compute_hash unchanged through binary transit) + distinct-tx-distinct-frames + trailer-overflow path for >32-byte payloads + every TxType discriminator + boundary values (zero + UINT64_MAX). Cross-reference: `docs/proofs/S002-Mempool-Sig-Verify.md` | `tools/test_tx_binary_codec.sh` |
| `determ test-chain-append` | `Chain::append` + `head` + `at` + `head_hash` mutation invariants (S-035 Option 1 seed). 17 assertions: empty-chain rejection paths (head/head_hash/at(0) throw with clear messages); genesis-like first append (index 0, prev_hash=zero); second append with correct prev_hash → height advances + head().prev_hash matches; **prev_hash continuity invariant** — append with wrong prev_hash throws 'prev_hash mismatch' (the central chain-integrity invariant; without it a peer could insert a block at the wrong height and silently corrupt state); 5-block chain construction with at(i) consistency; **prev_hash transitivity** — every block's prev_hash matches the prior block's compute_hash across the chain (chain-anchor invariant) | `tools/test_chain_append.sh` |
| `determ test-state-types` | Chain state struct defaults + sentinel semantics (S-035 Option 1 seed). 31 assertions covering AccountState (balance / next_nonce defaults), StakeEntry (locked + **UINT64_MAX active-stake sentinel**), RegistryEntry (ed_pub / registered_at / active_from / **UINT64_MAX inactive_from active sentinel** / region), DAppEntry (service_pubkey / endpoint_url / topics / retention / metadata / registered_at / active_from / **UINT64_MAX inactive_from sentinel**), plus **cross-struct sentinel consistency** — RegistryEntry, DAppEntry, and StakeEntry all share UINT64_MAX as the "active until apply-path event" sentinel (apply paths compare these against block.index uniformly, so divergent sentinels would break comparison semantics). | `tools/test_state_types.sh` |
| `determ test-validator-config` | BlockValidator public configuration API + validate() genesis short-circuit (S-035 Option 1 seed). 16 assertions: default-construct succeeds; all 11 public setters (set_k_block_sigs / _m_pool / _bft_enabled / _bft_escalation_threshold / _epoch_blocks / _shard_id / _committee_region / _sharding_mode / _governance_mode / _param_keyholders / _param_threshold) accept documented value ranges without throwing; **validate() genesis short-circuit invariant** — blocks at index 0 return OK regardless of validator config (genesis trust is anchored in the pinned genesis hash, not signature checks). The check_* helpers are private (need Chain + NodeRegistry fixtures); the network-level integration tests exercise the full validate path | `tools/test_validator_config.sh` |
| `determ test-timing-profiles` | `TimingProfile` constants from `chain/params.hpp` — operator-facing deployment posture (S-035 Option 1 seed). 54 assertions covering all 5 production profiles (cluster=BEACON/CURRENT/M=K=3, web=SHARD/EXTENDED/M=3,K=2, regional=SHARD/CURRENT/M=5,K=4, global=BEACON/EXTENDED/M=7,K=5, tactical=SHARD/EXTENDED/M=K=3) + their round-timer values + all 6 `*_test` profiles' parity with prod siblings (test profiles share M/K/role/mode with their named prod counterpart, differing only in sub-30ms test timers) + test/prod separation invariant (TEST_TX_COMMIT_MS < 30 < PROFILE_CLUSTER.tx_commit_ms = 50). Locks every operator-pinned profile against silent regression that would shift consensus posture without a visible error | `tools/test_timing_profiles.sh` |
| `determ test-params-constants` | `chain/params.hpp` protocol-level constants — deployment-critical defaults (S-035 Option 1 seed). 16 assertions covering: MIN_STAKE=1000 + UNSTAKE_DELAY=1000 + SUSPENSION_SLASH=10 (stake-economy defaults); REGISTER payload geometry (PUBKEY_SIZE=32, REGION_MAX=32, MIN_SIZE=32, MAX_SIZE=65, legacy alias consistency); TRANSFER_PAYLOAD_MAX=128 (A4 operator-visible memo cap); ZEROTH_ADDRESS shape (66 chars, 0x prefix, all-zero hex tail, passes is_anon_address); cross-arithmetic invariants (SUSPENSION_SLASH × 100 == MIN_STAKE — the BFT-safety economic accounting; UNSTAKE_DELAY >= 1 sane lower bound) | `tools/test_params_constants.sh` |
| `determ test-supply-invariant` | Chain A1 unitary supply read API + `expected_total` formula (S-035 Option 1 seed). 16 assertions: default-Chain zero state (all 5 counters: genesis_total / accumulated_subsidy / _slashed / _inbound / _outbound); formula on zero state (`genesis + subsidy + inbound - slashed - outbound`); live_total_supply default; **A1 invariant** (expected_total == live_total_supply on default Chain); determinism (both pure functions); setter independence (set_block_subsidy / _subsidy_pool_initial / _min_stake / _suspension_slash / _unstake_delay / _merge_threshold_blocks all leave expected_total unchanged — formula depends only on the 5 named counters). Apply-path invariant exercised by network-level tests; this unit test pins the read-side contract in <1s | `tools/test_supply_invariant.sh` |
| `determ test-enum-values` | Protocol-level enum integer encodings — wire-format discriminator lock-in (S-035 Option 1 seed). 46 assertions covering every value of TxType (11), MsgType (19), ConsensusMode (2), ChainRole (3), ShardingMode (3), InclusionModel (2) + sizeof invariants (6) confirming each enum is uint8_t wide. Transaction::signing_bytes prepends `static_cast<uint8_t>(type)` — a TxType reorder silently corrupts every tx hash. compute_block_digest binds consensus_mode. MsgType is the gossip envelope type byte. A regression that reorders any of these — even swapping two slots — would silently fork the wire format without an operator-visible error | `tools/test_enum_values.sh` |
| `determ test-block-accessors` | `chain::Block` default-construction + field accessors + value preservation (S-035 Option 1 seed). 36 assertions covering: default-Block scalar defaults (index=0, timestamp=0, prev_hash=zero, tx_root=zero, delay_seed=zero, delay_output=zero, consensus_mode=MUTUAL_DISTRUST, bft_proposer="", cumulative_rand=zero), collection defaults (transactions / creators / 5 committee-aligned vectors / abort_events / equivocation_events / cross_shard_receipts / inbound_receipts / initial_state all empty), zero-skip backward-compat fields (state_root + partner_subset_hash both zero — pre-S-033/pre-R4 defaults), field-assignment preservation (transactions push_back, creators order, parallel committee-vector sizing, BFT mode + proposer, state_root non-zero preserved exactly), and compute_hash determinism + sensitivity. The default-construction invariants are protocol-critical: partial Block construction must not expose half-initialized garbage | `tools/test_block_accessors.sh` |
| `determ test-make-block-sig` | `make_block_sig` Phase-2 BlockSigMsg producer (S-035 Option 1 seed). 15 assertions: data-field preservation (block_index / signer / delay_output / dh_secret); central sign/verify contract (sig verifies under signer's pubkey over block_digest; tampered block_digest fails verification); cross-signer distinctness + key binding (signer A's sig doesn't verify under signer B's pubkey); RFC 8032 determinism (same key + same digest → same sig — defends against future nondeterministic-Ed25519 regressions); sig-domain documentation (block_index / signer string / delay_output / dh_secret don't affect sig — bound to block_digest only; but field-distinct-per-message invariants preserved so the wire format carries them distinctly). Third K-of-K producer helper alongside `make_contrib` + `make_abort_claim` (both covered by test-consensus-msgs) | `tools/test_make_block_sig.sh` |
| `determ test-domain-separation` | Cross-cutting non-collision invariant across every protocol commitment hash (S-035 Option 1 seed). 20 assertions: pairwise non-collision across 9 commitments (compute_block_digest / make_contrib_commitment / make_abort_claim_message / compute_delay_seed / compute_block_rand / compute_tx_root / compute_genesis_hash / Transaction::compute_hash / Block::compute_hash); S-033/S-038 state_root exclusion-fence proofs (state_root mutation leaves compute_block_digest unchanged AND changes compute_hash); determinism sanity (cross-checks non-collision comes from distinct inputs, not internal state). A cross-domain collision would enable cross-protocol replay attacks where an attacker uses a sig made for one commitment in another context | `tools/test_domain_separation.sh` |
| `determ test-tx-signing-bytes` | `Transaction::signing_bytes` byte-layout invariant via golden vectors (S-035 Option 1 seed). 40 assertions covering the exact wire-format layout: type byte at offset 0; from + to strings each null-terminated; amount/fee/nonce each 8 bytes BIG-ENDIAN at offsets [3..10] / [11..18] / [19..26]; payload appended at offset 27 (plus from/to lengths). Locks the byte-for-byte layout via concrete golden vectors (e.g., amount=1 → [0,0,0,0,0,0,0,1] at offsets [3..10]; amount=0x0102030405060708 → BE pattern; default Transaction's signing_bytes is exactly 27 zero bytes). A regression in any byte ordering or BE-vs-LE encoding would silently break sig verification across versions — clients running old code would sign over a different layout than the validator recomputes | `tools/test_tx_signing_bytes.sh` |
| `determ test-merge-event-bytes` | `MergeEvent::encode` byte-layout invariant via golden vectors (S-035 Option 1 seed). 48 assertions covering the exact R4 under-quorum-merge wire format: event_type at offset 0 (BEGIN=0x00, END=0x01); shard_id 4 bytes LITTLE-ENDIAN at offsets [1..4]; partner_id 4 bytes LE at offsets [5..8]; effective_height 8 bytes LE at offsets [9..16]; evidence_window_start 8 bytes LE at offsets [17..24]; region_len byte at offset 25; region utf-8 bytes at offsets [26..]. Total size = 26 + region_len. **Critical contrast with Transaction::signing_bytes** — MergeEvent uses LE (native memory layout for x86/ARM wire targets), Transaction uses BE (cross-platform hash-input convention per Preliminaries §1.3). A regression that flips endianness would silently fork the apply path across shards in under-quorum-merge events | `tools/test_merge_event_bytes.sh` |
| `determ test-make-genesis-block` | `make_genesis_block` invariants (S-035 Option 1 seed). 33 assertions covering structural (index=0, prev_hash=zero, timestamp=0, empty transactions + committee vectors, tx_root=zero), creators[] sorted ALPHABETICALLY (deterministic order regardless of cfg insertion order), initial_state population from initial_creators (domain + ed_pub + stake + region preserved), initial_balances merge semantics (matching-domain balance ADDS to existing entry's balance; non-matching domain creates new entry with stake=0 + ed_pub=zero — pure-balance recipients like faucet/treasury), and determinism (same GenesisConfig → byte-identical Block). The bootstrap contract for every chain instance — a regression here would silently break cross-node genesis agreement | `tools/test_make_genesis_block.sh` |
| `determ test-pending-param-changes` | `Chain::stage_param_change` + `pending_param_changes()` getter — the A5 Phase 2 governance staging primitive (S-035 Option 1 seed). 13 assertions: default empty; single stage at height 100 (map size 1, key 100, bucket size 1, name + value byte-for-byte preserved); two entries at SAME height (vector push_back preserves insertion order — the deterministic-apply contract within a height bucket); three entries at different heights (std::map sorted-by-key iteration = ascending activation order); empty value vector valid (delete-param sentinel form); 256-byte value round-trips intact; independent chains (no static state leak between Chain instances). Read-write surface tested without the apply path — paired end-to-end via `tools/test_governance.sh` | `tools/test_pending_param_changes.sh` |
| `determ test-merge-state` | Chain merge-state read API + R4 governance threshold setters (S-035 Option 1 seed). 14 assertions: default-empty `merge_state()`; `is_shard_merged()` returns false with `out_partner` unmodified; `is_shard_merged(0, nullptr)` safe (permissive null pointer); `shards_absorbed_by()` returns empty vector on empty map; the three R4 threshold setters round-trip with defaults `merge_threshold_blocks=100`, `revert_threshold_blocks=200` (2× merge — hysteresis), `merge_grace_blocks=10`; setters independent (setting one preserves the others); `MergePartnerInfo` default `partner_id=0` + empty `refugee_region` (R1 global pool) | `tools/test_merge_state.sh` |
| `determ test-chain-apply-block` | `Chain::append` / `apply_transactions` — the central state-transition function under every block-application path (S-035 Option 1 seed). 28 assertions across 8 sub-cases: genesis bootstrap (8 — accounts + stakes + registrants populated; alice has registry entry from `initial_creators`, bob has none from `initial_balances` merge-with-zero-ed_pub; A1 baseline holds), empty-block apply (1 — state preserved, height advances), TRANSFER apply (5 — local debit + credit + nonce++ with fee routed to creator; bad nonce silently skipped; insufficient balance silently skipped without negative balances), STAKE apply (2 — stake_entry locked via 8-byte LE payload + balance debited), REGISTER apply (5 — seed TRANSFER funds carol, then REGISTER creates RegistryEntry with `active_from = height + derive_delay(cumulative_rand, tx.hash)`), prev_hash continuity (2 — wrong prev_hash throws + apply rollback leaves no mutation), A1 invariant sequence (4 — invariant holds across genesis + empty + TRANSFER + STAKE). **Critical implementation gotcha** (documented in the wrapper script): every block with fee-charging txs MUST set `b.creators` non-empty or apply will throw an A1 invariant violation that, if uncaught, terminates with STATUS_STACK_BUFFER_OVERRUN via `std::terminate`. The test uses `creators={"alice"}` so fees route back to alice. | `tools/test_chain_apply_block.sh` |
| `determ test-snapshot-roundtrip` | `Chain::serialize_state` + `Chain::restore_from_snapshot` round-trip (S-035 Option 1 seed). 15 assertions across five blocks: basic round-trip (`version=1` JSON object, `block_index` + `head_hash` reflect chain tip); account / stake / registry state preservation (balance, next_nonce, stake locked, registrant ed_pub + region R1); A1 supply invariants (5 counters preserved + invariant holds post-restore); 4 genesis-pinned constants round-trip; **S-033/S-037/S-038 central contract** — `compute_state_root` preserved across round-trip (without it, the state_root verification gate in the validator would reject the first post-restore block); rejection paths (unsupported version + non-object input + minimal-snapshot legacy back-compat); determinism (2 restores from same snapshot → same state_root, critical for cross-node bootstrap agreement). | `tools/test_snapshot_roundtrip.sh` |
| `determ test-state-proof` | `Chain::state_proof` — the v2.2 light-client Merkle inclusion-proof primitive (S-035 Option 1 seed). 12 assertions across three blocks: inclusion + verify (8 — proof returned, key preserved, `crypto::merkle_verify` accepts under root; tampered value_hash + tampered sibling-hash + wrong target_index + cross-proof-swap all rejected — the malicious-server defense surface); non-membership (1 — absent key returns nullopt; sorted-leaves design contract documented in `Chain::StateProof`); determinism + state-root consistency (3 — 2 proofs over unchanged chain are byte-identical; post-TRANSFER mutation changes value_hash AND new proof verifies under new state_root). Network-level `tools/test_state_proof.sh` exercises the RPC path end-to-end; this unit test pins the primitive in <1s. | `tools/test_state_proof_unit.sh` |
| `determ test-abort-event-apply` | Apply-side handling of `AbortEvent` — rev.8 Phase-1 suspension slashing (S-035 Option 1 seed). 13 assertions across six blocks: Phase-1 slashing (stake reduced by SUSPENSION_SLASH = 10); S-032 abort_records cache populated (count + last_block); Phase-2 NO-slashing contract (round=2 timing-skew aborts on healthy creators don't slash and don't accrue records); aborted-without-stake (DOMAIN_INCLUSION mode — records incremented even when no stake to slash); stake exhaustion (51 aborts × 10 slash = 510 > 500 stake → floors at 0, no negative; cache continues to 51); A1 invariant (accumulated_slashed += SUSPENSION_SLASH; live decreases by exactly that; expected == live post-apply). Mirrors registry.cpp's suspension policy. Required for BFT-mode safety. | `tools/test_abort_event_apply.sh` |
| `determ test-equivocation-apply` | Apply-side handling of `EquivocationEvent` — FA6 full equivocation slashing + deregistration (S-035 Option 1 seed). 10 assertions across five blocks: full stake forfeiture (entire locked → 0); registry deactivation (inactive_from = b.index+1, harsher than SUSPENSION_SLASH since equivocation is deliberate double-sign attack); robustness on ghost equivocator (no crash on missing stake/registry; other domains unaffected); A1 invariant (accumulated_slashed += full forfeit; live decreases by exactly that; expected == live); determinism (two chains see same equivocation → same `compute_state_root`). The dual mechanism (stake forfeit + deregistration) unifies STAKE_INCLUSION and DOMAIN_INCLUSION modes — neither leaves the equivocator able to rejoin without registering a fresh domain. | `tools/test_equivocation_apply.sh` |
| `determ test-unstake-deregister-apply` | Apply-side handling of UNSTAKE + DEREGISTER — stake-lifecycle complement to STAKE (S-035 Option 1 seed). 15 assertions across six blocks: UNSTAKE too-early (stake/balance unchanged, fee REFUNDED, nonce++); DEREGISTER sets inactive_from = height + derive_delay + stake unlock_height = inactive_from + unstake_delay_; DEREGISTER on non-registrant is defensive no-op (nonce++ only); UNSTAKE success post-unlock_height (stake reduced, balance += amount); UNSTAKE insufficient locked silently fails with fee refund; A1 invariant — UNSTAKE moves value (stake → balance) with no net supply change. Uses `set_unstake_delay(1)` to keep the flow under 5s without burning 1000+ blocks. | `tools/test_unstake_deregister_apply.sh` |
| `determ test-cross-shard-receipt-apply` | Apply-side handling of `CrossShardReceipt` — rev.9 B3.4 inbound credit (S-035 Option 1 seed). 12 assertions across seven blocks: basic credit (`to` account credited by receipt amount); A1 `accumulated_inbound` counter + invariant holds; **dedup contract** (duplicate `(src_shard, tx_hash)` NOT re-credited under chain replay — the exactly-once-credit guarantee); multiple distinct receipts in a single block (sum credited); receipt to non-existent domain creates account entry; `inbound_receipt_applied(src_shard, tx_hash)` predicate reflects the dedup-set membership (false before, true after); determinism across two chains. | `tools/test_cross_shard_receipt_apply.sh` |
| `determ test-param-change-apply` | Apply-side handling of A5 Phase 2 PARAM_CHANGE — stage at one block, activate at the (effective_height)-th apply (S-035 Option 1 seed). 16 assertions across eight blocks: staging contract (field unchanged before activation; pending map populated); activation (chain field mutated; pending drained); effective_height=0 activates at first non-genesis apply; 3 params at same height all activated in apply order; multi-height staging (earlier first, later still pending; both done after all heights reached); unknown name = silent no-op on chain storage + hook still fires (Node-side forwarding); known param triggers hook AND mutates chain field; determinism. | `tools/test_param_change_apply.sh` |
| `determ test-subsidy-distribution` | Apply-side handling of E1/E3/E4 subsidy distribution (S-035 Option 1 seed). 17 assertions across eight blocks: FLAT mode (full subsidy to 1 creator; 50/50 split among 2; dust to creator[0]; accumulated_subsidy counter); LOTTERY mode (jackpot when `cumulative_rand[0..7]` BE % M == 0 → pays `block_subsidy × M`; miss → 0); E4 finite pool (full → partial → drained as accumulated_subsidy reaches subsidy_pool_initial); A1 invariant under mint (live supply increases by exactly subsidy_this_block); empty-creators no-op (A1-safe contract documented in test-chain-apply-block gotcha). | `tools/test_subsidy_distribution.sh` |
| `determ test-merge-event-apply` | Apply-side handling of MERGE_EVENT — R7 under-quorum-merge state machine (S-035 Option 1 seed). 15 assertions across nine blocks: BEGIN inserts (shard_id → {partner_id, refugee_region}) into `merge_state_`; `is_shard_merged` predicate matches; END removes entry on partner match; END with wrong partner = no-op (defense-in-depth); BEGIN with wrong partner_id (violates `(shard_id+1) % shard_count` ring constraint) = no entry created; single-shard chains (shard_count=1) no-op all merge events; two distinct merges tracked independently; `shards_absorbed_by(partner)` inverse-lookup returns `(shard_id, region)` pairs; determinism. | `tools/test_merge_event_apply.sh` |
| `determ test-cross-shard-outbound-apply` | Cross-shard outbound TRANSFER apply (rev.9 B3, source side) (S-035 Option 1 seed). 11 assertions across five blocks: TRANSFER with cross-shard `to` debits sender (amount + fee, with fee returning via creator); dst address NOT credited locally (credit happens on the destination shard via inbound receipt); nonce++; A1 `accumulated_outbound` += amount (fee stays); live supply decreases by exactly `amount`; expected == live; single-shard fallback (shard_count=1) is_cross_shard always false → local credit happens, accumulated_outbound unchanged; determinism. | `tools/test_cross_shard_outbound_apply.sh` |
| `determ test-supply-lifecycle` | A1 unitary-supply invariant end-to-end across a mixed-tx lifecycle (S-035 Option 1 seed). 17 assertions covering genesis baseline → subsidy mint → intra-shard TRANSFER → STAKE → Phase-1 abort + slash → DEREGISTER → unlock-wait → UNSTAKE → final A1 identity formula (`genesis + subsidy + inbound - slashed - outbound == live`). Verifies per-step + post-loop + final invariant + 3 counter assertions + identity formula. Complements the per-tx-type apply tests; this is the interaction test catching regressions where two paths individually correct don't compose correctly. | `tools/test_supply_lifecycle.sh` |
| `determ test-dapp-state-transition` | Full DApp registry lifecycle (DAPP_REGISTER op=0 create, op=0 update of same domain, op=1 deactivate) (S-035 Option 1 seed). 22 assertions across five blocks: initial registration preserves all fields + sets `inactive_from = UINT64_MAX` (active) + `registered_at = current_height`; update on same domain REPLACES service_pubkey/url/topics/retention/metadata but PRESERVES `registered_at` (creation time invariant); deactivate (op=1) sets `inactive_from = current_height + DAPP_GRACE_BLOCKS` (deferred so queued messages reach the DApp during the grace window) — entry stays in registry; independent bob registration coexists; `dapp_registry()` reflects both; replayed lifecycle on second chain → same `compute_state_root`. Complements network-level `test_dapp_register.sh` + `test_dapp_call.sh` + `test_dapp_e2e.sh` (which exercise gossip + apply end-to-end). | `tools/test_dapp_state_transition.sh` |
| `determ test-overflow-paths` | S-007 overflow-protection apply paths (S-035 Option 1 seed). 11 assertions across six blocks: TRANSFER receiver overflow (apply throws S-007 diagnostic when amount would push recipient past UINT64_MAX); Phase-1 rollback contract (height unchanged + sender balance unchanged + state_root byte-identical after throw); inbound `CrossShardReceipt` overflow throws S-007 with 'inbound' diagnostic; boundary case (credit reaching exactly UINT64_MAX is OK — overflow check is strict `>`, not `>=`); sanity (normal TRANSFER unaffected by check); A1 invariant preserved across throw rollback. Without these checks, an attacker could wrap a victim's balance to 0 by crediting an overflowing amount. | `tools/test_overflow_paths.sh` |
| `determ test-state-root-namespaces` | Exhaustive 10-namespace `compute_state_root` coverage (S-033) (S-035 Option 1 seed). 12 assertions: each of the 10 namespaces (a:/s:/r:/d:/i:/b:/m:/p:/k:/k:c:) mutated via the appropriate apply or setter path verifies state_root changes; baseline equality (identical fresh chains → identical roots); cross-namespace independence (different namespace mutations → distinct roots — no accidental collision). The central S-033 invariant: every namespace contributes to the state-root commitment, and a regression in any namespace (forgotten leaf, wrong key encoding) would silently fork the chain at the verification gate. Complements `test-state-root` (which covers the `k:`-namespace setter sensitivity in detail). | `tools/test_state_root_namespaces.sh` |
| `determ test-multi-tx-block` | Multi-tx block apply semantics (S-035 Option 1 seed). 18 assertions across six blocks: two TRANSFERs from same sender with ascending nonces (both apply, nonce ordering preserved within sender); same sender + wrong nonce on second (defensive skip; nonce only consumed for first); two different senders in same block (independent nonce counters); interleaved alice→bob→alice (alice nonce 0 → 1 → 2 while bob handles its tx); insufficient balance mid-block (silent skip with no nonce++; subsequent tx still applies); A1 invariant after multi-tx. Covers the in-block tx-ordering contract that lets a sender batch operations. | `tools/test_multi_tx_block.sh` |
| `determ test-state-proof-namespaces` | `Chain::state_proof` verify path across all major state namespaces (a/s/r/b/d) (S-035 Option 1 seed). 9 assertions: per-namespace inclusion + merkle_verify under root for accounts (a:), stakes (s:), registrants (r:), abort_records (b:), DApp registry (d:); cross-namespace independence (5 distinct value_hashes → no accidental collision); cross-namespace proof-swap rejection (a:-key with s: value_hash fails merkle_verify); non-existent stake key returns nullopt; determinism (2 proofs for s: key byte-identical). Complements `test-state-proof` (a:-namespace in-depth coverage). | `tools/test_state_proof_namespaces.sh` |
| `determ test-applied-receipt-restore` | applied_inbound_receipts dedup-set survives snapshot serialize/restore (S-035 Option 1 seed). 10 assertions: baseline (3 distinct receipts credit bob 60; dedup-set predicate matches all 3); snapshot serialize → restore preserves balance + dedup-set entries + `inbound_receipt_applied` predicate; **critical contract** — duplicate `(src_shard, tx_hash)` on restored chain silently skipped (exactly-once-credit preserved across bootstrap, defeating double-credit attacks); fresh receipt post-restore credits normally; S-033 state_root preserved through restore (i:-namespace contributes); A1 `accumulated_inbound` counter + invariant preserved. | `tools/test_applied_receipt_restore.sh` |
| `determ test-stake-accounting` | Comprehensive stake-state-machine invariants (S-035 Option 1 seed). 14 assertions across seven scenarios: genesis (locked = initial_stake, unlock_height = UINT64_MAX sentinel); STAKE (locked += amount, unlock_height unchanged); slash (locked -= SUSPENSION_SLASH, unlock_height unchanged); DEREGISTER (unlock_height = inactive_from + unstake_delay, locked unchanged); UNSTAKE pre-unlock_height (silent fail, fee refunded, no state change); UNSTAKE post-unlock_height (locked → balance); A1 conservation (STAKE moves value, no supply change; full STAKE→UNSTAKE round-trip conserves supply). Complements `test-unstake-deregister-apply` (per-tx semantics in detail) with the full lifecycle composition. | `tools/test_stake_accounting.sh` |
| `determ test-fee-distribution-edge` | Fee + subsidy distribution edge cases (S-035 Option 1 seed). 15 assertions across seven scenarios: 3 creators + subsidy 100 → 33/33/34 split (dust 1 to alice = creator[0] alphabetically); fee 30 + subsidy 10 combined into single 40-token pool → 13/13/13 + dust 1; zero fee + zero subsidy = no distribution; zero-fee tx with non-zero subsidy still mints subsidy; exact-divide subsidy 30 / 3 = 10 each (no dust); subsidy 2 < creator count 3 → each gets 0 + dust 2 to alice; A1 invariant under all distribution scenarios. Catches dust-allocation regressions that could silently inflate/deflate creator[0] rewards. | `tools/test_fee_distribution_edge.sh` |
| `determ test-equivocation-multi` | Multi-equivocation edge cases (S-035 Option 1 seed). ~13 assertions across five scenarios: two distinct equivocators in same block (both forfeit, both deregistered, A1 counts each); same equivocator twice in same block (stake forfeit only once, second event no-op on already-zero stake); equivocator with NO stake = DOMAIN_INCLUSION mode (no forfeit possible but registry IS deactivated — the dual mechanism's deregistration arm remains effective); pre-deactivated equivocator (inactive_from OVERRIDDEN to b.index+1, preventing grace-window re-registration attacks); determinism (two chains see same multi-equivocation → same state_root). | `tools/test_equivocation_multi.sh` |
| `determ test-cross-shard-multi-receipt` | Mixed inbound + outbound + multi-receipt + cross-block dedup (S-035 Option 1 seed). 22 assertions across six scenarios: same-block mixed inbound (credit bob) + outbound TRANSFER (debit alice) — both A1 counters (`accumulated_inbound`, `accumulated_outbound`) advance independently, invariant holds; two receipts to SAME destination in one block — cumulative credit + accumulated_inbound sum; two receipts to DISTINCT destinations in one block — independent credits, new-account creation; dedup across BLOCKS in presence of mixed-direction traffic (b1 inbound + outbound, b2 SAME inbound + new outbound — duplicate skip, accumulated_inbound stays single-shot, accumulated_outbound = both txs); A1 invariant held across 3-block sequence (inbound-only, outbound-only, mixed+multi-receipt); determinism (parallel chains apply same sequence → same root). Complements `test-cross-shard-receipt-apply` (inbound-only) and `test-cross-shard-outbound-apply` (outbound-only) — the interaction test catching regressions where individually-correct directions don't compose. | `tools/test_cross_shard_multi_receipt.sh` |
| `determ test-multi-block-chain` | Chain-level continuity invariants across N=10 block append sequence (S-035 Option 1 seed). 10 assertions across seven scenarios: prev_hash linkage (every block.prev_hash matches prior compute_hash); height monotonicity (block[i].index == i for all i); A1 invariant at EVERY boundary (10 blocks, mixed empty/TRANSFER content + per-block subsidy); accumulated_subsidy = N × block_subsidy (linear accrual); state_root mutation (TRANSFER changes the root); determinism (parallel chains apply identical sequence → identical state_root + heights); compute_hash idempotency. Complements per-tx-type apply tests with CHAIN-WIDE continuity invariants — the property snapshot/replay/light-client rely on. | `tools/test_multi_block_chain.sh` |
| `determ test-tx-edge-cases` | TRANSFER corner cases beyond the happy-path apply tests (S-035 Option 1 seed). 28 assertions across 8 scenarios: self-transfer alice → alice (sender.balance -= cost, then accounts_[to=alice].balance += amount on the same map entry → net -fee, with fee returning via creator); zero amount + zero fee (pure no-op except nonce++); zero amount + non-zero fee (fee paid + returned via creator → 0 net); missing sender (operator[] creates 0-balance entry; cost > balance → silent skip with NO nonce bump — defends against forged-tx attacks that would otherwise bump nonces on a forged sender); insufficient balance (amount > balance, fee = 0); edge case where balance covers fee alone but not amount + fee (single-check semantics — no partial debit); BOUNDARY: balance == amount + fee exactly → SUCCEEDS (strict `<` check); determinism across the edge-case mix. Catches regressions where apply-layer skip-vs-fail semantics drift, which could either silently lose value or allow partial-debit attacks. | `tools/test_tx_edge_cases.sh` |
| `determ test-snapshot-then-apply` | Snapshot+restore equivalence — restored chain remains OPERATIONAL, post-restore appends produce identical state to a fresh full replay from genesis (S-035 Option 1 seed). 21 assertions: build chain A (control) via fresh genesis → 5 blocks with mixed TRANSFER + subsidy; build chain B (target) via fresh genesis → 3 blocks → serialize_state → restore_from_snapshot → apply A's blocks 4 + 5. Asserts: B.state_root matches snapshot-time root (S-033/S-038); B at restore point matches A_at_3 (balances, nonces, state_root); block_subsidy + unstake_delay preserved across snapshot; post-restore block 4 apply produces state_root + balances + nonces matching A_at_4; post-restore block 5 apply produces final state_root + head_hash + balances + nonces + accumulated_subsidy matching full-replay A; A1 invariant holds on B throughout. The property snapshot fast-sync relies on — beyond `test-snapshot-roundtrip` (read-side state preserved) this verifies the chain remains a fully-operational starting point. | `tools/test_snapshot_then_apply.sh` |
| `determ test-genesis-with-region` | R1 regional creator coverage (S-035 Option 1 seed). 12 assertions across 9 scenarios: region propagates GenesisCreator → registry on apply; empty region (default) stays empty (pre-R1 backward compat); compute_genesis_hash sensitive to GenesisCreator.region (otherwise an adversary could swap regions silently); compute_genesis_hash sensitive to GenesisConfig.committee_region (so two shards differing only in committee_region can't collide on chain identity); default-empty committee_region: deterministic (pre-R1 hash stable); snapshot round-trip preserves registrant.region (otherwise restored validators silently lose region claim → broken region-pinned committee selection); state_root sensitive to registrant.region (r:-namespace coverage); GenesisConfig JSON round-trip preserves regions; mixed-region creator set: each retains its own region. Pins R1 propagation across the genesis → registry → state_root → snapshot chain — silent drift would break R2 (region-aware committee selection) and R4 (partner_subset_hash binding into block.signing_bytes). | `tools/test_genesis_with_region.sh` |
| `determ test-anon-routing` | Anon-address routing integration (S-035 Option 1 seed). 15 assertions across 6 scenarios. The primitives are pinned separately (`test-anon-address`: parse + normalize + canonical hex; `test-shard-routing`: shard_id_for_address determinism + uniformity). This test verifies the three layers compose CORRECTLY: make_anon_address(pubkey) produces canonical 0x + 64-hex; parse_anon_pubkey round-trips the pubkey; case-variant inputs route to the SAME shard (S-028 closure — if normalize layer drifts from routing layer, the same logical address would land on different shards); distinct pubkeys → distinct addresses; TRANSFER to anon-address that routes locally credits locally (single-shard mode); TRANSFER to anon-address that routes cross-shard generates outbound debit and does NOT credit locally (recipient receives via inbound receipt on destination shard); determinism. Defends against silent drift between parse/normalize and routing layers. | `tools/test_anon_routing.sh` |
| `determ test-merge-event-apply-edge` | R7 under-quorum merge EDGE cases beyond `test-merge-event-apply` (S-035 Option 1 seed). 13 assertions across 9 scenarios: MERGE_END with NO prior BEGIN (lost-gossip scenario — must not corrupt state, merge_state stays empty); double MERGE_BEGIN for same shard (idempotent — exactly one entry, no duplicate accumulation); BEGIN/END/BEGIN cycle with different region on re-BEGIN (re-merge after END is allowed for shards that get absorbed, spin back up, get re-absorbed later); self-merge `shard_id == partner_id` rejected at apply (the (shard+1)%count == partner constraint rules this out); out-of-range `shard_id >= shard_count` rejected; empty refugee_region in BEGIN: entry still created (region is optional metadata); A1 invariant unchanged across MERGE_BEGIN + MERGE_END (pure metadata, no supply impact); state_root sensitive to merge_state (m:-namespace coverage in S-033 — two chains differing only in merge_state must produce different state_roots); determinism across the edge-case mix (parallel chains apply same edge sequence → same root). | `tools/test_merge_event_apply_edge.sh` |
| `determ test-block-event-composition` | Multi-event-type COMPOSITION in a single block (S-035 Option 1 seed). 17 assertions across 3 scenarios. Each event type has isolated apply tests; this exercises the composition — single block carrying TRANSFER tx + AbortEvent (Phase-1 slash) + EquivocationEvent (forfeit + deregister) + subsidy mint (per non-empty creators set) + inbound cross-shard receipt all applied simultaneously. Asserts each effect lands independently (alice debited via TRANSFER, dan credited via BOTH TRANSFER amount and inbound receipt, bob slashed by SUSPENSION_SLASH via abort, carol stake forfeited + registry deregistered via equivocation, subsidy minted 30, accumulated_inbound = 25); A1 invariant holds across all 5 event types; live supply moves by expected delta `+subsidy + inbound − slashed`; disjoint-actor composition (abort on bob, equiv on carol — different domains) produces identical state across chains; same-actor abort + equivocation stack correctly (drains bob's full stake to 0 — abort SUSPENSION_SLASH then equivocation forfeit-remainder, accumulated_slashed = full original stake). Pins the "events compose correctly" property — apply-ordering or shared-state mutation regressions would manifest here. | `tools/test_block_event_composition.sh` |
| `determ test-nef-pool-drain` | E1 Negative Entry Fee mechanism (S-035 Option 1 seed). 18 assertions across 8 scenarios. ZEROTH_ADDRESS is a canonical pseudo-account (all-zero pubkey, no usable private key) seeded at genesis via `GenesisConfig.zeroth_pool_initial`. On every FIRST-TIME REGISTER, the apply layer transfers HALF the current pool balance to the new registrant; re-REGISTERs (key rotation / region update) DO NOT touch the pool — defends against key-churn drain attacks. Asserts: pool seeded correctly at genesis; empty pool: NEF = 0, no credit; first-time REGISTER halves the pool, registrant credited 50%; re-REGISTER (region update) preserves region but pool UNCHANGED; geometric exhaustion across consecutive first-time REGISTERs (1000→500→250 after alice + bob); A1 invariant under NEF (intra-shard balance transfer, not mint); state_root sensitive to pool drain (a:-namespace); determinism. Pins the E1 economic primitive end-to-end — silent drift would over-spend the pool, under-credit new domains, or expose to churn attacks. | `tools/test_nef_pool_drain.sh` |
| `determ test-tx-payload-bounds` | Per-tx-type payload-size apply gates (S-035 Option 1 seed). 16 assertions across 11 scenarios. Each tx type has its own payload-shape contract; the validator enforces these at admission, but the apply path also defensively skips on malformed input. Pins: REGISTER < 32 bytes → silent skip (no nonce bump, no registrant entry, no fee charged — defends against forged-tx attacks that would otherwise create entries); REGISTER == 32 bytes (legacy/no-region form) → accepted with empty region; REGISTER > 32 bytes with mismatched region_len header → skip; REGISTER > 32 bytes with matched region_len → accepted with region preserved; STAKE / UNSTAKE payload.size() != 8 → silent skip (no fee charge, no nonce bump); STAKE == 8 bytes → accepted with amount decoded LE; STAKE > 8 bytes → skip; TRANSFER empty payload → accepted (payload is OPTIONAL per A4 memo design); A1 invariant under all skip paths; determinism. Defends against payload-format regressions that could either crash the apply path or silently corrupt state. | `tools/test_tx_payload_bounds.sh` |
| `determ test-empty-block-apply` | Empty / minimal-content block apply (S-035 Option 1 seed). 16 assertions across 6 scenarios. The happy-path tests use blocks with non-empty creators + transactions; this test pins the degenerate cases. Block with empty creators[] (no committee): subsidy gated OFF in chain.cpp:1286/1390 via `!b.creators.empty()` — live supply unchanged, A1 invariant holds (defends against subsidy double-distribution to nowhere = silent supply inflation); block with creators={alice} but NO transactions: subsidy minted exactly to sole creator; 5 consecutive empty blocks: chain advances cleanly with accumulated_subsidy = N × block_subsidy; block with single TX that FAILS (sender has 0 balance): TX silently skipped via continue in apply loop, subsidy still mints, nonce NOT bumped on skipped sender; state_root sensitivity (parallel empty blocks → same root; genesis-only chain differs from advanced chain because subsidy mint touches a:-namespace); prev_hash chain unbroken across 3 consecutive empty blocks. | `tools/test_empty_block_apply.sh` |
| `determ test-account-create-on-credit` | Account auto-creation paths (S-035 Option 1 seed). 11 assertions across 5 scenarios. `accounts_` is a `std::map` that auto-creates entries on first reference via `operator[]`; this test pins the SAFE creation paths and defends against silent state-corruption from unintended creations. TRANSFER to non-existent domain: account created with `balance += amount`, `accounts.size()` grew by 1, `next_nonce = 0` (no tx from them yet); inbound cross-shard receipt to non-existent domain: account created with credit; DEREGISTER from non-registrant: nonce DID bump (defensive design at chain.cpp:842 — find first then no-op + `next_nonce++`) but NO registry entry auto-created; stacked credit (receipt + TRANSFER to same fresh domain in same block) sums correctly (no double-creation); determinism — same auto-creation sequence → same state_root + same accounts-map size. Defends against `operator[]` regressions creating unintended account entries that affect state_root or balance lookups. | `tools/test_account_create_on_credit.sh` |
| `determ test-randomized-delay` | Randomized height-delay contract via REGISTER active_from + DEREGISTER inactive_from (S-035 Option 1 seed). 9 assertions across 6 scenarios. chain.cpp's static `derive_delay(cumulative_rand, tx_hash)` returns `1 + (sha256(tx_hash || cumulative_rand)[0..7] % REGISTRATION_DELAY_WINDOW)` with `REGISTRATION_DELAY_WINDOW = 10`, so delay ∈ [1, 10]. Pins observable contract via apply layer: determinism (same inputs → same delay); range bound (active_from ∈ [h+1, h+10]); cumulative_rand sensitivity (probes multiple seeds to rule out 1-in-10 collisions); tx_hash sensitivity (multi-payload probe — **requires `tx.hash` set explicitly** because the apply path reads `tx.hash` directly without recomputing, so test fixtures that forget this would see delay invariant to payload); distribution coverage (≥7 of 10 possible delays observed across 60 cross-product runs of cumulative_rand × payload — defends against regression collapsing the formula to a small subset); DEREGISTER inactive_from uses the same formula. Defends against (a) collapse to a predictable value (operator picks own activation height — predictability attack) or (b) escape from the [1, 10] window (silent over- / under-delay of activations). | `tools/test_randomized_delay.sh` |
| `determ test-block-timestamp` | Block.timestamp hash-surface scope (S-035 Option 1 seed). 6 assertions across 6 scenarios. The timestamp field is operator-supplied (no clock-monotonicity at chain layer — validator concern); this test pins which hash surfaces it DOES vs DOES NOT participate in. **IS in** `compute_hash` (block identity changes with timestamp — fork-resolve must see two timestamp-different blocks as distinct); **IS in** `signing_bytes` (creator_block_sigs must not be replayable across timestamp-different blocks); **NOT in** state_root (state_root binds account state, not block-metadata — light-client equivalence proofs depend on this); **NOT in** `compute_block_digest` (the FA1 committee-signature path excludes consensus-time metadata per PROTOCOL.md §4.3 — committee sigs commit to body only); determinism across all four surfaces with identical timestamps. A regression accidentally binding timestamp into state_root would break the light-client equivalence proofs. | `tools/test_block_timestamp.sh` |
| `determ test-node-registry` | `NodeRegistry::build_from_chain` + `eligible_in_region` (S-035 Option 1 seed). 14 assertions across 8 scenarios. FA8 R2 region-aware committee selection foundation — previously this surface was only exercised through end-to-end shell tests via the Node fixture; this direct unit test catches drift at lower fixture cost. Pins: build_from_chain reads (registrants, stakes) from a Chain and produces a sorted-by-domain NodeRegistry; min-stake gate (domains with stake < `chain.min_stake()` excluded); `eligible_in_region("")` returns full pool (R1 backward compat); `eligible_in_region("us-east")` returns only matching subset; **strict-equality** region filter (`"us"` does NOT match `"us-east"`); determinism (two rebuilds → identical sorted_nodes order); `sorted_nodes` lexicographic by domain; zero-stake boundary excludes regardless of registry-entry presence. | `tools/test_node_registry.sh` |
| `determ test-tx-replay-protection` | Nonce-gating contract as replay-defense surface (S-035 Option 1 seed). 13 assertions across 6 scenarios. chain.cpp:739 enforces strict equality `if (tx.nonce != sender.next_nonce) continue;`, so both **past** nonces (replay) and **future** nonces (sequence gap) are silently skipped. Pins: successful apply bumps nonce 0 → 1; replay (stale nonce) silently skipped — sender + recipient balances unchanged, nonce NOT re-bumped, no fee charged (defends against an attacker replaying their own valid tx for fee-extraction); future nonce silently skipped — sender's nonce NOT advanced past the gap; per-sender nonces are **independent** (alice's nonce at 5 doesn't prevent bob from sending at nonce 0); A1 invariance under 3 consecutive replay attempts; determinism. Defends against drift in the strict-equality check to `>=` (future-nonce allowed = sequence break) or `>` (replays allowed = per-tx-once broken). | `tools/test_tx_replay_protection.sh` |
| `determ test-chain-save-load` | `Chain::save` + `Chain::load` file-persistence round-trip (S-035 Option 1 seed). 14 assertions across 7 scenarios. The on-disk `chain.json` format is what every node restarts from (snapshot-bootstrap is the alternative for new joiners; existing nodes load + replay chain.json). Pins: save writes a non-empty file; load reconstructs height + head_hash + state_root + every account's balance + nonce + every stake.locked + every registrant's region (R1 backward-compat) + all 5 A1 counters byte-for-byte; A1 invariant holds on reloaded chain; save→load→save→load idempotent (no entropy across cycles); load of a non-existent path returns an empty Chain (defensive — relied on by first-launch Node startup which has no chain.json on disk yet). | `tools/test_chain_save_load.sh` |
| `determ test-block-validator-basic` | BlockValidator consensus-validation entry via public `validate()` (S-035 Option 1 seed). 10 assertions across 7 scenarios. `validate()` runs ~13 sub-checks in order (`check_prev_hash`, `check_creators_registered`, `check_creator_selection`, `check_creator_tx_commitments`, `check_creator_dh_secrets`, `check_abort_certs`, `check_equivocation_events`, `check_delay`, `check_block_sigs`, `check_cumulative_rand`, `check_transactions`, `check_cross_shard_receipts`, `check_inbound_receipts`, `check_timestamp`) short-circuiting on first failure; all sub-checks are PRIVATE so this test exercises them via the public entry. Genesis (index 0) short-circuits OK (trust via operator-pinned genesis hash, not consensus validation); mismatched `prev_hash` → fail with diagnostic naming "prev_hash"; unregistered creator → fail with diagnostic naming offender ("ghost"); mixed creators (alice valid + ghost unregistered) → fail still naming ghost (not alice); creator-count mismatching `k_block_sigs` → fail (creator_selection gate); determinism (pure on identical inputs); default-constructed BlockValidator handles genesis path. Defends against drift that would either accept malformed blocks (catastrophic safety) or reject valid ones (catastrophic liveness). | `tools/test_block_validator_basic.sh` |
| `determ test-genesis-sharded` | Genesis variants for sharded deployments (S-035 Option 1 seed). 14 assertions across 8 scenarios. `compute_genesis_hash` is the chain-identity anchor; this test verifies that two sharded chains in the same deployment with different `shard_id` OR different `chain_role` produce DIFFERENT genesis hashes. Pins: **chain_role sensitivity** (SINGLE / BEACON / SHARD pairwise distinct hashes — a BEACON's chain identity can never collide with a SHARD's, catastrophic if it could); **shard_id sensitivity** (shards 0, 1, 2 all distinct); JSON round-trip preserves chain_role + shard_id + initial_shard_count; BEACON with non-zero shard_id distinct from BEACON shard_id=0; **S-039 documented gap lock-in** — `initial_shard_count` NOT bound into hash (4→8 produces same hash; wire-compat-break to fix, deferred to coordinated migration); determinism; SHARD make_genesis_block produces valid Block (index=0, prev_hash=zero, compute_hash() == compute_genesis_hash(cfg)); Chain bootstraps cleanly from SHARD genesis. Defends against accidental chain-identity collision across shards (would cross-mount different shards as the same chain). | `tools/test_genesis_sharded.sh` |
| `determ test-cross-shard-atomicity` | Cross-shard atomicity at chain-pair level (S-035 Option 1 seed). 10 assertions across the dual-chain flow. `test-cross-shard-outbound-apply` covers source-side debit on a single Chain; `test-cross-shard-receipt-apply` covers destination-side credit on a single Chain. This test verifies they COMPOSE by modeling BOTH sides simultaneously: source shard S0 applies TRANSFER to a cross-shard address (`accumulated_outbound += amount`); destination shard S1 applies the inbound receipt (`accumulated_inbound += amount`); **cross-shard A1 atomicity asserted as `src.accumulated_outbound == dst.accumulated_inbound`** (the cross-pair conservation property — every coin debited on src equals every coin credited on dst); per-shard A1 invariant on each chain; net supply across the pair conserved (src loses what dst gains); determinism. Defends against drift where the two sides' counters could move by different amounts (silent value loss or creation across the shard boundary — the catastrophic cross-shard failure mode). | `tools/test_cross_shard_atomicity.sh` |
| `determ test-chain-ctor-bootstrap` | Chain constructor bootstrap paths (S-035 Option 1 seed). 15 assertions across 7 scenarios. The `Chain` class has two ctors: `Chain()` default (empty Chain) and explicit `Chain(Block genesis)` (bootstraps by applying genesis as block 0 via `apply_transactions(genesis)`); plus the legacy bootstrap path `Chain c; c.append(make_genesis_block(cfg))`. Pins: `Chain()` is empty (height=0, `empty()` returns true); `head()` on empty Chain throws with "Empty chain" diagnostic; `at(0)` on empty Chain throws std::out_of_range-style; `Chain(genesis)` bootstraps to height=1 with head matching genesis (`compute_hash` + `head_hash` agree); both bootstrap paths (append + ctor) produce equivalent state (head_hash + state_root + balances + stakes byte-identical); both support continued append (height=2 after one more block; tx applied); default-constructed Chain accepts genesis via the legacy append path. Defends against ctor-bootstrap drift that would let two equivalent-on-paper bootstrap paths produce different chain identity. | `tools/test_chain_ctor_bootstrap.sh` |
| `determ test-snapshot-version-rejection` | restore_from_snapshot version-field validation (S-035 Option 1 seed). 10 assertions across 9 scenarios. The restore path at chain.cpp:1703-1709 requires `snap.version == 1` and throws on any other value. Pins: version=1 accepted (round-trip from `serialize_state(empty)`); missing version field (defaults to 0 via nlohmann's `.value`) → rejected with "unsupported snapshot version" diagnostic; version=0 explicitly rejected, error names "0"; version=999 (future) rejected, error names "999"; version=-1 (negative) rejected; non-object input (JSON array / string / null) rejected with **distinct** "not a JSON object" diagnostic that fires BEFORE the version check; wrong-type version field (string instead of integer) rejected via nlohmann's type_error path. Defends against future migration drift (regressions accepting arbitrary version numbers would compromise the v2 migration gate) and against breaking v1 (existing snapshots become unloadable). | `tools/test_snapshot_version_rejection.sh` |
| `determ test-config-defaults` | Operator-facing security defaults at `Config::from_json({})` (S-035 Option 1 seed). 22 assertions. The most important pin is **S-001 `rpc_localhost_only=true`** default — a pre-S-001 config or a bare-minimum new operator's config.json must NOT expose RPC on all interfaces. Other defaults pin the "out-of-the-box safe" posture: S-014 rate limits disabled (operators opt-in to throttling); `bft_enabled=true` with `bft_escalation_threshold=5` (auto-escalation enabled); `m_creators=3` and `k_block_sigs=m_creators` (strong K-of-K default — hybrid mode requires explicit operator config); `chain_role=SINGLE` (unsharded — rev.8 behavior preserved); `sharding_mode=CURRENT` (pre-R1 baseline); `shard_id=0`, `initial_shard_count=1`; `listen_port=7777`, `rpc_port=7778`; 200ms consensus phase delays; `region=""` and `committee_region=""` (R1 pre-feature byte-identical); `log_quiet=false`; `rpc_auth_secret=""` (HMAC auth opt-in). Defends against default-drift weakening the out-of-the-box security posture. | `tools/test_config_defaults.sh` |
| `determ test-required-block-sigs` | BFT/MD quorum arithmetic — `required_block_sigs(mode, committee_size)` formula (S-035 Option 1 seed). 14 assertions across 11 scenarios. The formula is at the heart of safety/liveness proofs (BFTSafety.md, PROTOCOL.md §5.3, Liveness.md L-4.3). **Formula**: MD mode requires `committee_size` (full K-of-K); BFT mode requires `ceil(2 * committee_size / 3)` — permits up to `(committee_size - ceil(2k/3))` sentinel-zero slots. Concrete BFT values pinned: BFT(3)=2, BFT(4)=3, BFT(5)=4, BFT(6)=4, BFT(7)=5, BFT(9)=6; MD(k)=k always; **invariant BFT(k) ≤ MD(k) for k in [1,16]** (BFT-mode is the liveness escape hatch — can never require MORE sigs than MD, would defeat escalation's purpose); determinism (pure function). Defends against formula drift that would either over-tighten (liveness break — escalation can't fire) or under-tighten (safety break under f < N/3). | `tools/test_required_block_sigs.sh` |
| `determ test-config-load-save` | `Config::load(path)` + `Config::save(path)` file IO round-trip (S-035 Option 1 seed). 14 assertions across 6 scenarios. Complements `test-config-roundtrip` (in-memory JSON) and `test-config-defaults` (default values pin) by exercising the FILE IO layer. Pins: save writes well-formed JSON (parseable); load preserves all fields losslessly (domain, ports, **`rpc_localhost_only` S-001 critical field**, auth secret, committee params, BFT, chain_role, sharding_mode, R1 region tags, log_quiet); save→load→save→load idempotent; load of non-existent file throws with diagnostic naming path or "Cannot open"; load of malformed JSON throws cleanly; save creates parent directories (mkdirp). Defends against drift in the operator config-persistence path — file format must round-trip losslessly across save+load cycles. | `tools/test_config_load_save.sh` |
| `determ test-block-from-json-minimal` | Block::from_json minimum-valid input + S-018 required-field rejection (S-035 Option 1 seed). 16 assertions across 11 scenarios. `Block::from_json` at chain/block.cpp:451 requires **7 fields outside any `j.contains()` guard**: `index`, `prev_hash`, `timestamp`, `transactions`, `creators`, `cumulative_rand`, `abort_events`. All other arrays + hex fields are optional. Pins: minimal valid Block (7 required fields) parses cleanly; missing each required field throws with diagnostic naming the field; wrong-type rejection on `transactions`=number, `creators`=object, `prev_hash`=short-hex; compute_hash post-parse + to_json/from_json round-trip preserves the 7 required fields. Defends against gate weakening (silent acceptance of malformed blocks → safety break) and v1 breakage (adding/removing required fields would make existing gossip messages unparseable). **Note**: uses `fputs` to stdout (not `std::cout`) due to an MSVC Release stdout-stream issue specific to this test's mix of nlohmann::json operations. | `tools/test_block_from_json_minimal.sh` |

## Forensics / governance

| Command / RPC | Purpose |
|---|---|
| RPC `submit_equivocation { event }` | External submission of equivocation evidence (validates two-sig proof, gossips for slashing) |

There's no built-in CLI wrapper for the equivocation RPC — see `tools/test_equivocation_slashing.sh` for a Python template that synthesizes evidence and submits via raw JSON-RPC.

### Governance (A5)

| Command | Purpose |
|---|---|
| `determ submit-param-change --priv <hex> --from <domain> --name <NAME> --value-hex <hex> --effective-height <N> --keyholder-sig <idx>:<priv_hex> [--keyholder-sig ...] [--fee N] [--rpc-port N]` | Sign + submit a `PARAM_CHANGE` tx. `--name` must be on the validator whitelist (MIN_STAKE, SUSPENSION_SLASH, UNSTAKE_DELAY, bft_escalation_threshold, tx_commit_ms, block_sig_ms, abort_claim_ms, param_keyholders, param_threshold). Each `--keyholder-sig` supplies one signature; threshold must be met. Sender (`--from`) is a registered domain that pays the fee. See `tools/test_governance_param_change.sh` for an end-to-end example. |

Genesis fields enabling governance:
- `governance_mode: 0` (uncontrolled, default) or `1` (governed)
- `param_keyholders: [<hex pubkey>, ...]` (Ed25519 founder set)
- `param_threshold: <N>` (default N-of-N when keyholders are set)

### Under-quorum merge (R4)

| Command | Purpose |
|---|---|
| `determ submit-merge-event --priv <hex> --from <domain> --event {begin\|end} --shard-id N --partner-id N --effective-height N --evidence-window-start N [--refugee-region <region>] [--fee N] [--rpc-port N]` | Sign + submit a `MERGE_EVENT` tx. Requires `sharding_mode == EXTENDED`. Operator-driven for v1.x; auto-detection on the beacon is a v1.1 work item. See `tools/test_under_quorum_merge.sh`. |

Genesis thresholds:
- `merge_threshold_blocks` (default 100) — consecutive blocks of `eligible_in_region < 2K` before `MERGE_BEGIN` validates.
- `revert_threshold_blocks` (default 200) — 2:1 hysteresis on the way back.
- `merge_grace_blocks` (default 10) — gap between block height and `effective_height`.

## Common flags

- `--rpc-port N` — connect to an RPC port other than the config default (useful when running multiple nodes locally).
- `--data-dir D` — point at a non-default data directory.

## Operator config fields (`config.json`)

The node's `config.json` (generated by `determ init`) carries the runtime knobs. Live values are visible at `determ status` → `protections` block. The most operationally-relevant entries:

### Network / consensus

| Field | Default | Purpose |
|---|---|---|
| `domain` | (required) | This node's registered identity (must match a `genesis.initial_creators` entry) |
| `data_dir` | (CWD) | Root for chain.json / keyfile / snapshot output |
| `listen_port` | 7777 | Gossip listen port |
| `rpc_port` | 7778 | RPC listen port |
| `bootstrap_peers` | `[]` | Initial intra-chain peers (e.g. `["127.0.0.1:7771","127.0.0.1:7772"]`) |
| `beacon_peers` | `[]` | Cross-chain peers for SHARD-role nodes (beacon addresses) — role-filtered, isolated from `bootstrap_peers` traffic |
| `shard_peers` | `[]` | Cross-chain peers for BEACON-role nodes (shard addresses) — role-filtered |
| `m_creators` | 3 | Committee size K (genesis-pinned at chain init; per-node override rejected) |
| `k_block_sigs` | 3 | Phase-2 threshold (genesis-pinned; K = M = strong BFT, K < M = weak hybrid) |
| `bft_enabled` | true | Per-height BFT escalation after `bft_escalation_threshold` aborts |
| `bft_escalation_threshold` | 5 | Round-1 + Round-2 abort count that triggers BFT mode (governance-mutable) |
| `tx_commit_ms` / `block_sig_ms` / `abort_claim_ms` | 200/200/100 | Round timer durations |
| `chain_role` / `sharding_mode` / `shard_id` / `committee_region` | `single`/`current`/0/`""` | Sharding posture (genesis-pinned) |
| `initial_shard_count` | 1 | Number of shards S in this deployment (genesis-pinned) |
| `epoch_blocks` | 1000 | Committee re-seeding period (genesis-pinned) |
| `region` | `""` | This validator's self-declared region tag (R1 — mirrored into REGISTER payload). Must match the chain's `committee_region` to be selected for committees under EXTENDED sharding |

### RPC security (S-001 / S-014 / v2.16)

| Field | Default | Purpose |
|---|---|---|
| `rpc_localhost_only` | `true` | Bind RPC to 127.0.0.1 only (S-001) — flip to `false` ONLY with `rpc_auth_secret` set, or behind a reverse proxy with auth |
| `rpc_auth_secret` | `""` | Hex-encoded shared secret for HMAC-SHA-256 RPC auth (v2.16); empty = no auth. Generate with `openssl rand -hex 32` |
| `rpc_rate_per_sec` | 0 | RPC calls/sec per peer-IP token bucket (S-014 RPC side); 0 = disabled. Suggested external-bind: 100 |
| `rpc_rate_burst` | 0 | Bucket capacity; 0 = disabled. Suggested external-bind: 200 |

### Gossip security (S-014 / S-022 / S-026)

| Field | Default | Purpose |
|---|---|---|
| `gossip_rate_per_sec` | 0 | Inbound-message rate cap per peer-IP (S-014 gossip side); 0 = disabled. Suggested external-bind: 500 |
| `gossip_rate_burst` | 0 | Bucket capacity; 0 = disabled. Suggested external-bind: 1000 |

(S-022 per-message-type body cap and S-026 TCP keepalive are unconditional — no operator knob; see `docs/SECURITY.md` §S-022 + §S-026.)

### Operator hygiene (S-027)

| Field | Default | Purpose |
|---|---|---|
| `log_quiet` | `false` | When `true`, suppress per-block / per-bundle / per-connection diagnostic lines. WARN/ERROR diagnostics surface regardless. Production deployments wanting fewer logs set this to `true` |

### Paths

| Field | Purpose |
|---|---|
| `domain` | This node's registered identity (must match a `genesis.initial_creators` entry) |
| `genesis_path` / `genesis_hash` | Authoritative genesis file + pinned hash for eclipse defense |
| `chain_path` | On-disk chain.json — wrapped `{head_hash, blocks}` form post-S-021 |
| `key_path` | Node's Ed25519 keypair (0600 permissions) |
| `snapshot_path` | If set on a fresh node, bootstrap from this snapshot instead of replaying from genesis |
| `shard_manifest_path` | BEACON-role nodes: per-shard committee_region map (required in EXTENDED mode) |

A live readback of which protections are active is in `determ status` → `protections` (see top of this doc). Wallet keys live separately in `determ-wallet` keyfiles (encrypted at rest via v2.17 AES-256-GCM envelope; see `determ-wallet` section below).

## determ-wallet binary (A2)

Separate executable from the `determ` daemon. Secret material never enters the chain daemon's address space — by design. The wallet handles the user's Ed25519 seed and threshold recovery; the daemon never sees them.

### Primitive layers

| Command | Purpose |
|---|---|
| `determ-wallet shamir split <hex> -t T -n N` | Split a secret into N Shamir shares with threshold T. Shares print one per line as `<x_hex>:<y_hex>`. |
| `determ-wallet shamir combine <share> [<share> ...]` | Reconstruct from ≥ T shares. Returns the original secret hex. |
| `determ-wallet envelope encrypt --plaintext <hex> --password <str> [--aad <hex>] [--iters N]` | AEAD-wrap arbitrary data (AES-256-GCM, PBKDF2-HMAC-SHA-256 keying, default 600k iters). |
| `determ-wallet envelope decrypt --envelope <blob> --password <str> [--aad <hex>]` | Unwrap. Returns plaintext hex; exit code 2 on AEAD tag failure. |

### Recovery flows

| Command | Purpose |
|---|---|
| `determ-wallet create-recovery --seed <hex> --password <str> -t T -n N --out <file> [--scheme {passphrase\|opaque}]` | Persist a T-of-N recovery setup as a single JSON document. Scheme `passphrase` (default) uses PBKDF2 directly off the password; scheme `opaque` routes through the OPAQUE adapter. |
| `determ-wallet recover --in <file> --password <str> [--guardians <i,j,k,...>]` | Reconstruct the seed from ≥ T envelopes. Scheme is auto-detected from the setup. Optional pubkey checksum gate prevents silent corruption. |

### OPAQUE primitives (diagnostic)

| Command | Purpose |
|---|---|
| `determ-wallet oprf-smoke` | Smoke-test libsodium primitives (ristretto255 scalar/point ops + Argon2id). Outputs sample scalars + a blinded point + a stretched key. |
| `determ-wallet opaque-handshake --mode {register\|authenticate} --password <str> --guardian-id <0..255> [--record <hex>]` | Exercise the OPAQUE adapter directly. Currently the **development stub** adapter; the real `libopaque` integration is tracked as **v2.14** (real OPAQUE wallet recovery) in `docs/V2-DESIGN.md`. See `wallet/PHASE6_PORTING_NOTES.md` for the MSVC porting status that gates the v2.14 ship. |
| `determ-wallet version` | Print version banner including current adapter suite name. |

### Adapter status check

The wallet's `is_stub()` flag exposes whether the linked OPAQUE adapter is the development stub or the real `libopaque` implementation (v2.14). The stub is offline-grindable against a compromised guardian — see `docs/proofs/WalletRecovery.md` (FA12) for the concrete-security bounds. The v2.14 ship replaces the stub with the real construction; once that lands, this `is_stub()` returns `false` for production builds. The wallet binary's `version` command surfaces the active suite tag.

## Exit codes

- `0` — success
- `1` — parsing error, missing argument, RPC failure, or assertion failure
- `2` — cryptographic failure (AEAD tag mismatch, OPAQUE authentication rejected, recovery reconstruction failed)
