# Determ Documentation

The protocol-level architecture and design rationale lives in the top-level [`README.md`](../README.md). This `docs/` directory holds operator + user-facing material.

## Index

- [QUICKSTART.md](QUICKSTART.md) — 5-minute walkthrough: build, run a 3-node cluster, send transactions, snapshot create + restore.
- [CLI-REFERENCE.md](CLI-REFERENCE.md) — every `determ` subcommand at a glance, grouped by purpose.
- [PROTOCOL.md](PROTOCOL.md) — formal v1 protocol specification: wire formats, hash inputs, consensus state machine, message types. What an external implementer needs to build a compatible client.
- [UNIT-TESTS.md](UNIT-TESTS.md) — S-035 Option 1 seed coverage map (64 in-process `determ test-*` subcommands; 1309 assertions across cryptographic primitives, chain commitment, randomness, network surface, governance staging + apply, central state-transition path, snapshot round-trip incl. dedup-set preservation, light-client inclusion-proof primitive incl. all-namespace proof verification, FA6 abort+equivocation slashing apply, stake-lifecycle apply + full state-machine composition, cross-shard inbound + outbound receipt apply, E1/E3/E4 subsidy distribution apply, R7 under-quorum-merge state machine apply, A1 end-to-end lifecycle, v2.18 DApp registry lifecycle, S-007 overflow protection, S-033 10-namespace state_root coverage, multi-tx block apply semantics) + extension guide for adding new tests.
- [SECURITY.md](SECURITY.md) — canonical security posture. **27 findings mitigated in-session** + 4 partial (S-030 D1 effective + D2 partial via S-033 apply-layer with v2.7 F2 spec'd for full consensus-layer D2 closure; S-016 inbound-receipts via Option 2 time-ordered admission with v2.7 F2 closing fully via intersection commitment; S-035 Path 3 path-portability shipped in-session via `tools/common.sh` — Options 1 [gtest seed] + 2 [deterministic-simulation framework] outstanding as v1.x quality work; S-036 `EXTENDED`-mode-specific MERGE_BEGIN witness-window with v2.11 spec'd for full closure). **Zero fully-open findings in any severity tier — zero open Critical, zero open High, zero open Medium, zero fully-open Low/Op.** Track A (localized fixes) 14/14 complete. S-037 + S-038 (paired closures this session): S-037 was the `dapp_registry` snapshot-restore gap; testing it surfaced S-038 — the S-033 state_root verification gate was dormant on production blocks because `Node::try_finalize_round` never populated `body.state_root`. Both shipped; `tools/test_dapp_snapshot.sh` (12/12 PASS) now strictly verifies end-to-end state_root match through restore. **S-018 (final-open Medium) closed in-session**: `json_require<T>` / `json_require_hex` / `json_require_array` helpers (`include/determ/util/json_validate.hpp`) applied to every attack-relevant wire-format consumer (gossip envelope, Transaction/Block/AbortEvent/EquivocationEvent/GenesisAlloc, ContribMsg/AbortClaimMsg/BlockSigMsg, GenesisConfig, node_key.json); new regression `tools/test_s018_json_validation.sh` (10/10 PASS) exercises field-name diagnostics for missing-field / wrong-type / wrong-hex-length / non-array errors. **S-035 Path 3 (final-fully-open Low/Op) shipped in-session**: `tools/common.sh` helper + sed-conversion of all then-49 tests (now 53; new tests added in-session use the helper from the start) to portable `$DETERM` / `$PROJECT_ROOT` references; Linux/Mac/Windows runnable from the repo root. Reconciles the rev.7 audit (preserved out-of-tree) and the prior in-tree `OPEN-VULNERABILITIES.md` (superseded and removed) against current code. What a security reviewer should look at.

## Behavioral test suite

`tools/test_*.sh` currently holds **119 shell-driven regression tests** spanning the protocol surface — every protocol feature, security closure, and economic primitive has at least one paired test. Representative items:

| Test | Asserts |
|---|---|
| `test_bearer.sh` | Bearer-wallet TRANSFER round-trip across 3 nodes |
| `test_bft_escalation.sh` | K-of-K → BFT fallback when committee gets stuck |
| `test_sharded_smoke.sh` | Beacon + shard genesis hashes distinct; both chains advance independently |
| `test_domain_registry.sh` | DOMAIN_INCLUSION mode (no-stake validators agree on head) |
| `test_zero_trust_cross_chain.sh` | Cross-chain gossip plumbing (`BEACON_HEADER` ↔ `SHARD_TIP`) |
| `test_cross_shard_transfer.sh` | TRANSFER from shard 0 → shard 1 credits destination end-to-end |
| `test_equivocation_slashing.sh` | `submit_equivocation` RPC → bake evidence into block → stake forfeited |
| `test_snapshot_bootstrap.sh` | Receiver fast-bootstraps from donor's snapshot with no genesis required |
| `test_state_root.sh` | S-033 Merkle-root state commitment changes when state changes |
| `test_state_proof.sh` | v2.2 state_proof inclusion-proof RPC + leaf-hash validation |
| `test_verify_state_proof.sh` | v2.2 light-client demonstrator — fetches state-proof, verifies locally via `crypto::merkle_verify`; asserts tampered value_hash / sibling-hash / mismatched --state-root all FAIL while valid proofs PASS |
| `test_headers_rpc.sh` | v2.2 light-client header-sync — `headers` RPC + `determ headers` CLI + `determ verify-headers` chain-integrity CLI; asserts response shape, light-client field set present, heavy fields stripped, pagination, out-of-range handling, server-side count cap (256), `block_hash` field + prev_hash chain links, verify-headers OK on valid chains and FAIL on tampered prev_hash / wrong --prev-hash anchor |
| `test_verify_block_sigs.sh` | v2.2 light-client committee-signature verifier — `determ verify-block-sigs`; asserts K-of-K committee Ed25519 signatures verify against `compute_block_digest`, tampered signature / wrong committee pubkey / missing committee member all FAIL, accepts `determ headers` envelope shape |
| `test_json_cli.sh` | `--json` flag on info CLIs (validators / committee / peers / chain-summary); asserts each returns the expected JSON shape, validators --json entries are verify-block-sigs-compatible, default output is NOT JSON (--json required for machine consumption), and validators --json pipes directly into verify-block-sigs --committee for end-to-end light-client verification |
| `test_merkle.sh` | v2.1 Merkle primitives unit test (S-035 Option 1 seed) — in-process `crypto::merkle_root` + `merkle_proof` + `merkle_verify` + `merkle_leaf_hash` + `merkle_inner_hash`; 12 assertions covering empty-set, single-leaf, balanced + unbalanced trees, tampering detection (value_hash / sibling-hash / target_index), domain separation (leaf vs inner), determinism, and sort-invariance |
| `test_committee_selection.sh` | v2.1 committee-selection primitives unit test (S-035 Option 1 seed) — in-process `crypto::select_m_creators` (S-020 hybrid: both rejection-sampling and partial-Fisher-Yates branches), `select_after_abort_m`, and `epoch_committee_seed`; 13 assertions covering determinism, seed-sensitivity, branch coverage at both sides of the 2K vs N threshold, edge cases (K=N, K=1), distinct-without-replacement, in-range invariant. Foundation tests for FA1 / FA2 / FA5 / FA8 — every committee at every round is derived through these functions |
| `test_shard_routing.sh` | v2.1 cross-shard routing primitive unit test (S-035 Option 1 seed) — in-process `crypto::shard_id_for_address`; 7 assertions covering single-shard degenerate case, determinism, in-range invariant, salt-sensitivity, distribution uniformity (1000 addresses across 4 shards >5% per shard), case-sensitivity, empty-address handling. Foundation test for FA7 cross-shard receipt atomicity — every cross-shard tx's destination is derived through this function |
| `test_ed25519.sh` | Ed25519 sign/verify unit test (S-035 Option 1 seed) — in-process `crypto::sign` / `crypto::verify` / `generate_node_key`; 10 assertions covering key-shape, sign+verify round-trip, tampered-message rejection, tampered-signature rejection, wrong-pubkey rejection, RFC-8032 determinism (same key+msg → same sig), empty-message edge case, distinct-key distinct-sig, cross-key verify rejection, 4 KB long-message streaming. Foundation test for FA1 / FA2 / FA5 / FA6 / FA7 / FA10 — every signature claim in the protocol reduces to Ed25519 EUF-CMA |
| `test_sha256.sh` | SHA-256 wrapper + Big-Endian encoding unit test (S-035 Option 1 seed) — in-process `crypto::sha256` + `SHA256Builder`; 10 assertions covering NIST FIPS 180-4 test vectors (empty input, "abc", 56-byte input exercising the >55-byte padding path), incremental Builder ↔ one-shot equivalence, multi-piece append correctness, **and the Preliminaries §1.3 big-endian uint64_t / int64_t encoding** that every signing_bytes / compute_block_digest / merkle_leaf_hash path depends on for cross-platform protocol determinism. Foundation test under every hash claim in the entire codebase |
| `test_anon_address.sh` | Anon-address helpers unit test (S-035 Option 1 seed) — in-process `is_anon_address` / `normalize_anon_address` / `parse_anon_pubkey` / `make_anon_address`; 12 assertions covering case-insensitive acceptance (S-028 closure), invalid-input rejection (missing 0x, wrong length, non-hex), case-normalization to canonical lowercase, round-trip via make_anon_address ↔ parse_anon_pubkey, registered-domain pass-through. Faster unit-level counterpart to test_anon_address_case.sh (which exercises the same surface end-to-end through 3-node RPC — 1+ minute vs ~1 second) |
| `test_headers_gossip.sh` | v2.2 gossip-layer header-sync (HEADERS_REQUEST / HEADERS_RESPONSE wire messages — MsgType 17/18); asserts gossip-layer fetch via `determ headers --peer host:port` returns a valid envelope, content matches the RPC fetch byte-for-byte, gossip-fetched headers pass through verify-headers + verify-block-sigs cleanly, and pagination semantics match. Closes the last v2.2 outstanding ask — light clients can now peer directly with full nodes without RPC binding |
| `test_genesis_message.sh` | `GenesisConfig::genesis_message` hash-mixing contract unit test (S-035 Option 1 seed); 10 assertions covering backward-compat default-skips-mix invariant, custom-yields-distinct-hash, empty-string-distinct-from-default, determinism under override, JSON round-trip, absent-key default-fallback, size cap enforcement (256B max), and boundary acceptance. Locks in the operator-facing inscribed-message feature against silent regressions that would either break existing chain identity or allow chain-identity collisions |
| `test_state_root_unit.sh` | `Chain::compute_state_root()` commitment algebra unit test (S-035 Option 1 seed); 13 assertions over the S-033 / v2.1 state-Merkle surface — determinism (the K-of-K consensus precondition: identical state → identical root), purity (no internal-state leak between calls), non-zero baseline (k: leaves always present), per-field sensitivity for every public `set_*()` that maps into a k:-namespace leaf, invertibility (change-then-revert returns to original root), cross-namespace distinction (no accidental collisions between unrelated field mutations), and order independence (setter call order doesn't affect root — leaves sorted internally). Unit-level counterpart to network-level `test_state_root.sh` (3-node RPC) and `test_dapp_snapshot.sh` (snapshot S-037+S-038 contract); catches encoding-drift regressions in <1s without spinning up a chain |
| `test_block_rand.sh` | V8 randomness primitives unit test (S-035 Option 1 seed) — in-process `compute_delay_seed` + `compute_block_rand` + `proposer_idx` + `required_block_sigs` + `count_round1_aborts`; 21 assertions covering determinism + every-input-field sensitivity (block_index, prev_hash, tx_root, creator_dh_inputs, ordered_secrets), creator_dh_inputs / ordered_secrets ORDER sensitivity (the committee-selection-order contract pairing Phase-1 commits with Phase-2 reveals), domain separation between the two hash functions, proposer_idx in-range invariant + abort-rotation, required_block_sigs golden vectors for MD = K and BFT = ceil(2K/3) at K = 1..12, count_round1_aborts round-2 filter. FA1 / FA5 / FA8 foundation — a regression here either silently forks randomness across nodes (different committees per node → safety failure) or lets a producer reorder Phase-2 reveals to bias future selection (FA1 unpredictability violation) |
| `test_rate_limiter.sh` | S-014 token-bucket rate limiter unit test (S-035 Option 1 seed) — in-process `net::RateLimiter` (the shared helper consumed identically by RpcServer and GossipNet); 16 assertions covering disabled-mode bypass (default + explicit configure(0,0)), enable + getter round-trip, first-touch FULL invariant (legitimate callers don't get hit cold), burst exhaustion (4th consume fails at burst=3 same-instant), per-key independence (exhausting key A doesn't throttle key B — the central security property), reconfigure semantics, refill timing (100ms sleep at rate=20/s yields ≥1 new token), burst-cap invariant (long sleep at high rate does NOT overflow burst — defeats slow-leak attacks), and 100-distinct-keys-at-scale. Unit-level counterpart to wire-level `test_rpc_rate_limit.sh` + `test_gossip_rate_limit.sh` |
| `test_block_digest.sh` | `compute_block_digest` (FA1 signature target) unit test (S-035 Option 1 seed); 19 assertions covering BOTH the inclusion contract (every field in compute_block_digest changes the digest when mutated — defeats silent-removal regressions) AND the deliberate S-030 D2 exclusion contract (fields NOT in compute_block_digest MUST NOT change it: delay_output / creator_dh_secrets / cumulative_rand are Phase-2-reveal; abort_events / equivocation_events / timestamp are v2.7 F2 territory; state_root is apply-time-gated via S-033 + S-038; partner_subset_hash for R4 Phase 3). "Fences" the digest at exactly the surface FA1 / S-030 D2 / v2.7 F2 assume — prevents a future commit from silently moving a field across the boundary and breaking the design assumptions |
| `test_block_hash.sh` | `Block::signing_bytes()` + `Block::compute_hash()` unit test (S-035 Option 1 seed); 16 assertions over the FA1 chain-anchor identity. compute_hash binds EVERY consensus-relevant field including Phase-2-reveal fields and apply-time-recomputed state_root, so its output becomes prev_hash on every subsequent block. Covers determinism + purity, field-sensitivity for Phase-2-reveal fields (delay_output / creator_dh_secrets / cumulative_rand / creator_block_sigs), zero-skip backward-compat for partner_subset_hash (R4 Phase 3) and state_root (S-033) — both bound only when non-zero so pre-feature blocks retain byte-identical hashes, creators[] ORDER sensitivity, and the S-030 D2 chain-anchor distinction (two same-digest blocks differing in equivocation_events still have different compute_hash outputs) |
| `test_binary_codec.sh` | Wire-format codec unit test (A3 / S8 closure + S-022 cap table; S-035 Option 1 seed); 35 assertions covering JSON envelope (v0) + binary envelope (v1) round-trip across multiple MsgType variants (HELLO + STATUS_REQUEST + STATUS_RESPONSE + TRANSACTION + CONTRIB), `is_binary_envelope` format-detection contract, malformed-input rejection (garbage bytes + truncated valid JSON), and `max_message_bytes` golden vectors for every enumerated MsgType (16 MB / 4 MB / 1 MB tiers + default-tight 1 MB fence for future MsgType additions). The wire format is the trust boundary between peers — a regression here would either break interoperability or widen the S-022 attack surface |
| `test_wire_types.sh` | Block-internal wire types JSON round-trip + S-018 strict-rejection unit test (S-035 Option 1 seed); 39 assertions covering CrossShardReceipt (FA7 / V12 — 10 fields), AbortEvent (FA3 — 4 fields), EquivocationEvent (FA6 — 8 fields), and GenesisAlloc (chain-identity allocation — 5 fields incl. R1 empty-region legacy default + zero-stake round-trip). S-018 strict-rejection locked in for all four types: missing required field throws with clear field-name diagnostic; wrong-length hex throws (json_require_hex length check). CrossShardReceipt::from_json was hardened in the same commit — previously permissive via j.value() defaults; now uses json_require / json_require_hex to match the rest of the S-018 surface (defense in depth at the parse layer, even though receipts are also bound into the parent block's signing_bytes via K-of-K) |
| `test_transaction.sh` | Transaction unit test (S-035 Option 1 seed); 28 assertions over Transaction::signing_bytes + compute_hash + Ed25519 sign/verify + JSON round-trip. Locks in signing_bytes determinism + per-field sensitivity + sig/hash EXCLUSION (would be circular), compute_hash == SHA-256(signing_bytes) golden contract, Ed25519 sign/verify + tampered-tx-fails round-trip, full JSON round-trip for TRANSFER + type-preservation for each of 9 enum variants (REGISTER through DAPP_CALL), S-018 strict-rejection, unique-tx-identity contract (mempool-dedup foundation) |
| `test_merge_event_codec.sh` | MergeEvent codec unit test (R4 under-quorum merge wire format; S-035 Option 1 seed); 19 assertions: BEGIN + END round-trips with empty-region preservation, size invariant, decode rejection paths (too-short / invalid event_type / region_len > 32 / size mismatch), determinism + per-field sensitivity, maximum-region (32 bytes) round-trip. Locks in the canonical-binary wire format driving cross-shard merge coordination |
| `test_consensus_msgs.sh` | ContribMsg + BlockSigMsg + AbortClaimMsg + commitment-hash unit test (S-035 Option 1 seed); 28 assertions covering make_contrib_commitment determinism + per-input sensitivity + tx_hashes ORDER sensitivity (sorted-ascending contract), make_abort_claim_message determinism + per-input sensitivity (round defeats Phase-1 vs Phase-2 replay), domain separation between commitment hashes, full JSON round-trip for all three message types, real Ed25519 sign/verify integration |
| `test_tx_root.sh` | compute_tx_root unit test (FA2 censorship-resistance primitive; S-035 Option 1 seed); 10 assertions: union semantics ({A,B} ∪ {B,C} == {A,B,C}, NOT intersection {B}), dedup across lists, list permutation invariance, empty inner list invariance, sensitivity to added tx. A regression to intersection semantics (S-025 deleted) would silently break union-rule censorship resistance |
| `test_genesis.sh` | compute_genesis_hash + make_genesis_block unit test (chain-identity origin; S-035 Option 1 seed); 22 assertions over chain_id sensitivity + the **S-039 diagnostic-UX gap lock-in** (m_creators / k_block_sigs / block_subsidy / min_stake / initial_shard_count / bft_enabled are NOT bound into the hash — operator-UX gap, fix deferred as wire-compat break) + fields that ARE bound (shard_id / chain_role / suspension_slash + merge_threshold_blocks non-default / genesis_message / committee_region non-empty) + make_genesis_block invariants + JSON round-trip + oversize rejection + S-018 defense-in-depth lock-in for wrong-type rejection on initial_creators / initial_balances / param_keyholders. Discovered S-039 during test authoring |
| `test_envelope.sh` | wallet/envelope.hpp AES-256-GCM + PBKDF2-HMAC-SHA-256 AEAD wrapping primitive unit test (A2 Phase 2 wallet recovery share envelopes + S-004 option 2 passphrase-encrypted keyfiles; S-035 Option 1 seed); 27 assertions covering encrypt/decrypt round-trip + envelope shape + AEAD safety properties (wrong-pw / empty-pw / mismatched-AAD / tampered-ct / tampered-tag all fail; fresh salt + nonce per encryption guarantee distinct ciphertexts — defeats artifact-correlation attacks) + serialize/deserialize canonical hex round-trip with bad-input rejection + edge cases (empty plaintext, empty AAD). A regression here would silently weaken at-rest security for every encrypted wallet artifact |
| `test_resolve_fork.sh` | Chain::resolve_fork unit test (S-029 BFT-mode fork-choice rule; S-035 Option 1 seed); 10 assertions covering the documented priority (heaviest sig set → fewer aborts → smallest block_hash) + edge cases (identical blocks, zero-sigs, sentinel-zero handling in BFT mode, abort-tie-break beats hash-tie-break). A regression would either let the wrong block win (peers diverge → FA1 violation) or make resolution non-deterministic across nodes |
| `test_shamir.sh` | Shamir's Secret Sharing over GF(2^8) unit test (wallet/shamir.cpp; A2 Phase 1 wallet recovery primitive; S-035 Option 1 seed); 18 assertions covering T-of-N reconstruction across all C(5,3) subsets, T-1 below-threshold safety, share-shape invariants (distinct non-zero x, fresh polynomial per split), degenerate thresholds (T=1, T=N), empty-secret edge case, and invalid-input rejection (threshold=0 / threshold > share_count / empty / duplicate x / mismatched sizes). Unit-level counterpart to the network-level `test_wallet_shamir.sh` (wallet-binary CLI smoke test); both lock in the same A2 Phase 1 primitive at different layers |
| `test_random_state.sh` | Random-state primitives unit test (crypto/random.cpp foundation layer; S-035 Option 1 seed); 27 assertions over compute_dh_output / compute_dh_output_m (2-share + M-share folds) / update_random_state (per-block chain) / compute_abort_hash + chain_abort_hash (S5 anti-cartel defense — abort-dependent committee re-selection so attackers can't pre-plan abort sequences) / genesis_random_state. Covers determinism + argument-order sensitivity + per-input sensitivity for each function. Foundation layer below test_block_rand.sh's compute_delay_seed / compute_block_rand. A regression would silently fork randomness across nodes (different folds → different committees) or weaken S5 anti-cartel-navigation |
| `test_snapshot_defense.sh` | S-018 defense-in-depth lock-in for `Chain::restore_from_snapshot` wrong-type collection rejection (S-035 Option 1 seed); 11 assertions covering every optional collection field (accounts / stakes / registrants / applied_inbound_receipts / merge_state / abort_records / dapp_registry / pending_param_changes). Sending any one as a scalar / number / object instead of an array now throws a clean "S-018: JSON field 'X' has wrong type" diagnostic naming the offending field. Baseline (minimal valid snapshot) loads cleanly; backward-compat case (empty optional fields) preserves behavior. Snapshots arrive via SNAPSHOT_RESPONSE gossip (16 MB cap — the only unbounded-tier channel) so wrong-type diagnostics at the parse layer matter for attack triage |
| `test_encoding.sh` | `types.hpp` encoding helpers unit test (S-035 Option 1 seed); 23 assertions covering `to_hex` / `from_hex` / `from_hex_arr<N>` round-trips + case-insensitive parsing + odd-length / wrong-length rejection + templated overloads for Hash + Signature + leading-zero preservation, plus `to_string(ChainRole)` / `to_string(ShardingMode)` mappings, determinism (no std::ostringstream internal-state leak between calls), and `now_unix()` post-2017 sanity. **Foundation under every hex serialization** in the codebase — Block JSON, Transaction sig, Merkle leaf hashing, RPC output, snapshot format all transit through these helpers, so a regression would cascade across the entire wire format |
| `test_chain_helpers.sh` | Chain read-side API unit test (S-035 Option 1 seed); 23 assertions covering balance / next_nonce / stake (locked + lockfree variants — the read paths used by concurrent RPC handlers while apply runs); height / empty / head_hash on default chain; shard routing (single-shard degenerate case + multi-shard is_cross_shard distribution); operator-tunable setter round-trips (block_subsidy / min_stake / suspension_slash / unstake_delay); A1 supply counters all zero on default chain. Locks safety-critical defaults: `balance(unknown)==0`, `next_nonce(unknown)==0`, `shard_count==1` (single-shard => is_cross_shard==false unconditionally). A regression in default-value behavior would cascade through every safety proof assuming a consistent state view |
| `test_json_validate.sh` | S-018 json_validate helpers unit test (S-035 Option 1 seed); 24 assertions covering `json_require<T>` / `json_require_hex` / `json_require_array` happy paths + every error path. Locks the operator-facing error-message contract: missing-field error has S-018 prefix + "missing" keyword + field name; wrong-type error has "wrong type" + field name; wrong-hex-length error states "expected N / got M" counts; wrong-array-type error names observed type. Foundation under every S-018-hardened from_json in the codebase — Transaction, Block, AbortEvent, EquivocationEvent, GenesisAlloc, CrossShardReceipt, ContribMsg, BlockSigMsg, AbortClaimMsg + gossip envelope dispatchers all transit through these helpers. Direct unit test complements `test_s018_json_validation.sh` (which exercises representative through-paths end-to-end) |
| `test_block_roundtrip.sh` | Block::to_json / Block::from_json full field-set round-trip unit test (S-035 Option 1 seed); 41 assertions covering minimal block, block with transactions, K-of-K committee block (creators / creator_tx_lists / creator_ed_sigs / creator_dh_inputs / creator_dh_secrets / creator_block_sigs / tx_root / delay_seed / delay_output), BFT-mode block, block with abort_events / equivocation_events / cross-shard receipts / inbound_receipts / initial_state (genesis), zero-skip fields (state_root + partner_subset_hash backward-compat), and compute_hash invariance through JSON round-trip — the CRITICAL invariant that block_hash is identical before and after JSON transit (gossip sender + receiver MUST agree on block_hash, otherwise the prev_hash chain breaks) |
| `test_config_roundtrip.sh` | Config::to_json / Config::from_json — operator config save+reload round-trip (S-035 Option 1 seed); 47 assertions covering all 32 operator-tunable fields (ports / peers / rate-limits / regions / sharding mode / governance flags / timing knobs), plus default Config round-trip, empty-JSON → defaults path, and enum integer encoding. Permissive contract preserved: missing optional fields silently default rather than throw, the documented intentional choice for operator-facing config. Without round-trip stability, operators' saved configs would silently revert fields on reload, breaking their intent without a visible error |
| `test_tx_binary_codec.sh` | Transaction binary codec round-trip unit test (S-035 Option 1 seed); 24 assertions covering the v1 binary wire-format path for TRANSACTION MsgType — the **S-002 critical fixed-slot path** for amount/fee/nonce. Pre-S-002 closure the decoder dropped these values during binary transit because the trailer doesn't carry them; corrupted txs entered the mempool until the validator filtered them later. This test locks the post-fix behavior: TRANSFER full round-trip + S-002 sig-verify invariant (compute_hash unchanged) + trailer-overflow for >32-byte payloads + every TxType discriminator + boundary values (zero + UINT64_MAX) all round-trip through `encode_binary` / `decode_binary` for `Message{TRANSACTION}` |
| `test_chain_append.sh` | Chain::append + head + at + head_hash mutation invariants unit test (S-035 Option 1 seed); 16 assertions covering empty-Chain rejection paths (head / head_hash / at(0) throw with clear messages), genesis-like first append, second append with correct prev_hash → height advances, **prev_hash continuity invariant** (append with wrong prev_hash throws 'prev_hash mismatch' — the central chain-integrity invariant without which a peer could insert a block at the wrong height and silently corrupt state), 5-block chain construction with at(i) consistency, and **prev_hash transitivity** (every block's prev_hash matches the prior block's compute_hash — the chain-anchor invariant that makes prev_hash a tamper-evident link) |
| `test_state_types.sh` | Chain state struct defaults + UINT64_MAX sentinel semantics unit test (S-035 Option 1 seed); 30 assertions covering AccountState / StakeEntry / RegistryEntry / DAppEntry. **The UINT64_MAX sentinel for unlock_height and inactive_from is a protocol-critical invariant** — "active until set to a concrete height by an apply-path event". A regression to a zero default would cause every fresh stake to look immediately unlockable (every fresh registrant to look immediately deregistered), breaking suspension policy + UNSTAKE semantics. Also locks cross-struct sentinel consistency: StakeEntry.unlock_height, RegistryEntry.inactive_from, and DAppEntry.inactive_from all share UINT64_MAX so apply paths compare uniformly |
| `test_validator_config.sh` | BlockValidator setters + validate() genesis short-circuit unit test (S-035 Option 1 seed); 16 assertions covering default-construct, all 11 public setters across their documented value ranges (K / M / BFT enable / escalation / epoch / shard_id / committee_region / sharding_mode / governance_mode / param_keyholders / param_threshold), and the **validate() genesis short-circuit invariant** — blocks at index 0 return OK regardless of validator config (genesis trust is anchored in the pinned genesis hash). check_* helpers are private and exercised by network-level integration tests |
| `test_timing_profiles.sh` | TimingProfile constants lock-in unit test (S-035 Option 1 seed); 54 assertions covering every named profile in chain/params.hpp. **Operators pin profile names in their config files** — a regression that silently changes any documented value (cluster=BEACON/CURRENT/M=K=3, web=SHARD/EXTENDED/M=3,K=2, regional=SHARD/CURRENT/M=5,K=4, global=BEACON/EXTENDED/M=7,K=5, tactical=SHARD/EXTENDED/M=K=3) would shift consensus posture across every chain pinning that profile without a visible error. Also locks the test/prod parity invariant (`*_test` profiles mirror M/K/role/mode of their prod sibling, differing only in sub-30ms timers) |
| `test_params_constants.sh` | chain/params.hpp protocol-level constants lock-in unit test (S-035 Option 1 seed); 16 assertions covering deployment-critical defaults — MIN_STAKE / UNSTAKE_DELAY / SUSPENSION_SLASH (stake-economy), REGISTER payload geometry (PUBKEY_SIZE / REGION_MAX / MIN_SIZE / MAX_SIZE + legacy alias), TRANSFER_PAYLOAD_MAX (A4 memo cap), ZEROTH_ADDRESS (E1 NEF pool's low-order curve25519 pseudo-account), plus cross-arithmetic invariants (SUSPENSION_SLASH × 100 == MIN_STAKE — the BFT-safety economic accounting that means 100 baked aborts zero a minimally-staked validator). Every chain whose genesis doesn't override inherits these defaults; a silent change would shift behavior across deployments without an error |
| `test_supply_invariant.sh` | A1 unitary supply invariant read-API unit test (S-035 Option 1 seed); 16 assertions covering the formula expected_total = genesis + subsidy + inbound - slashed - outbound and the A1 invariant live_total_supply == expected_total on default Chain. Locks: default zero state for all 5 counters, formula's arithmetic shape (defends against sign-flip / field-reorder regressions), pure-function determinism for both expected_total + live_total_supply, and **setter independence** (operator-tunable config setters don't affect expected_total — the formula depends only on the 5 named counters; the network-level test_a1_unitary_*.sh suite exercises the apply-path mutations that tick the counters) |
| `test_enum_values.sh` | Protocol-level enum integer encodings unit test (S-035 Option 1 seed); 46 assertions covering every value of TxType (TRANSFER=0..DAPP_CALL=10), MsgType (HELLO=0..HEADERS_RESPONSE=18), ConsensusMode (MUTUAL_DISTRUST=0, BFT=1), ChainRole (SINGLE=0..SHARD=2), ShardingMode (NONE=0..EXTENDED=2), InclusionModel (STAKE/DOMAIN=0/1) + sizeof invariants for all 6 enums (uint8_t wide). **Every enum integer is on the wire** — Transaction::signing_bytes prepends `static_cast<uint8_t>(type)`, compute_block_digest binds consensus_mode, MsgType is the gossip envelope type byte. A reorder of any slot silently forks the wire format without an operator-visible error |
| `test_block_accessors.sh` | Block default-construction + field accessor invariants unit test (S-035 Option 1 seed); 36 assertions covering chain::Block's default state across every field (scalar defaults, collection defaults, zero-skip backward-compat fields for state_root + partner_subset_hash) and field-assignment preservation (transactions push_back, creators order, parallel committee-aligned vectors, BFT mode + proposer, state_root non-zero exactness, compute_hash determinism + sensitivity). The default-construction invariants are protocol-critical: partial Block construction during apply paths must not expose half-initialized garbage |
| `test_make_block_sig.sh` | make_block_sig Phase-2 BlockSigMsg producer unit test (S-035 Option 1 seed); 15 assertions covering the third K-of-K consensus message producer alongside make_contrib + make_abort_claim. Tests data-field preservation, the central sign/verify contract (sig verifies under signer's pubkey over block_digest), cross-signer distinctness + key binding, RFC 8032 determinism (same key + digest → same sig), and sig-domain documentation (sig binds to block_digest only — block_index / signer string / delay_output / dh_secret don't affect sig but field-distinct invariants preserved). Phase-2 K-of-K signing requires K committee members each producing one of these msgs; a regression would either fork the signing path or produce a sig that verifies against the wrong content |
| `test_domain_separation.sh` | Cross-cutting non-collision unit test across every protocol commitment hash (S-035 Option 1 seed); 20 assertions verifying that compute_block_digest / make_contrib_commitment / make_abort_claim_message / compute_delay_seed / compute_block_rand / compute_tx_root / compute_genesis_hash / Transaction::compute_hash / Block::compute_hash all produce distinct outputs even on similar inputs. A cross-domain collision would enable cross-protocol replay where a sig made for one commitment verifies in another context. Plus S-033/S-038 state_root exclusion-fence proofs (state_root mutation leaves compute_block_digest unchanged AND changes Block::compute_hash) |
| `test_tx_signing_bytes.sh` | Transaction::signing_bytes byte-layout golden-vector unit test (S-035 Option 1 seed); 40 assertions locking the exact wire format: type byte at offset 0, from/to strings each null-terminated, amount/fee/nonce each 8 bytes BIG-ENDIAN at known offsets [3..10] / [11..18] / [19..26], payload at offset 27. Default Transaction → exactly 27 zero bytes. A regression in any byte ordering or BE-vs-LE encoding would silently break sig verification across versions — clients running old code would sign over a different layout than the validator recomputes |
| `test_merge_event_bytes.sh` | MergeEvent::encode byte-layout golden-vector unit test (S-035 Option 1 seed); 48 assertions locking the exact R4 under-quorum-merge wire format: event_type at offset 0 (BEGIN=0x00, END=0x01), shard_id 4 bytes LITTLE-ENDIAN at [1..4], partner_id 4 bytes LE at [5..8], effective_height 8 bytes LE at [9..16], evidence_window_start 8 bytes LE at [17..24], region_len byte at offset 25, region utf-8 bytes at [26..]. **Endian-contrast invariant**: MergeEvent uses LE (native memory layout for x86/ARM wire targets), Transaction uses BE (cross-platform hash-input convention per Preliminaries §1.3); a regression flipping endianness would silently fork the apply path across shards during under-quorum-merge events |
| `test_make_genesis_block.sh` | make_genesis_block bootstrap-contract unit test (S-035 Option 1 seed); 34 assertions covering structural defaults (index=0, prev_hash=zero, timestamp=0, empty transactions + 4 committee-aligned vectors, tx_root=zero), creators[] sorted ALPHABETICALLY regardless of cfg insertion order (deterministic byte-equal genesis across nodes from different `determ init` invocations), initial_state population from initial_creators preserving domain + ed_pub + stake + region, initial_balances merge semantics (matching-domain balance ADDS to existing entry; non-matching domain creates new entry with stake=0 + ed_pub=zero — pure-balance recipients like faucet / treasury / pre-mine allocations), and determinism (same GenesisConfig → byte-identical Block on every call). The bootstrap contract for every chain instance — a regression here would silently break cross-node genesis agreement |
| `test_pending_param_changes.sh` | A5 Phase 2 governance staging-primitive unit test (S-035 Option 1 seed); 13 assertions covering `Chain::stage_param_change` + `pending_param_changes()` getter. Default empty, single stage (map size 1, name/value byte-for-byte preserved), two entries at same height (vector push_back preserves insertion order — the deterministic-apply contract within a height bucket), entries at different heights ordered ascending (std::map iteration), empty-value vector preserved (delete-param sentinel), 256-byte value round-trip, Chain-instance independence (no static state leak). Paired end-to-end via `tools/test_governance.sh` |
| `test_merge_state.sh` | Chain merge-state read API + R4 governance threshold setters (S-035 Option 1 seed); 14 assertions covering default-empty `merge_state()`, `is_shard_merged()` false with `out_partner` unmodified, null-pointer permissive form, `shards_absorbed_by()` empty on empty map, the three R4 threshold setters (`merge_threshold_blocks=100`, `revert_threshold_blocks=200` for 2× hysteresis, `merge_grace_blocks=10`) round-trip + setter independence, MergePartnerInfo default `partner_id=0` + empty `refugee_region` (R1 global pool) |
| `test_chain_apply_block.sh` | `Chain::append` / `apply_transactions` central state-transition unit test (S-035 Option 1 seed); 28 assertions across 8 sub-cases — genesis bootstrap (accounts + stakes + registrants from `initial_state`; alice has registry from `initial_creators`, bob has none from `initial_balances` zero-ed_pub merge), empty-block apply, TRANSFER apply (debit + credit + nonce++ with fee routing to creator; bad nonce + insufficient balance silently-skip), STAKE apply (8-byte LE payload), REGISTER apply (active_from = height + derive_delay), prev_hash continuity (chain integrity invariant), A1 invariant across the full sequence. **Implementation gotcha** (documented in the wrapper): every block with fee-charging txs MUST set `b.creators` non-empty — empty creators with fees breaks A1, throws inside apply, and (uncaught) terminates via STATUS_STACK_BUFFER_OVERRUN |
| `test_snapshot_roundtrip.sh` | `Chain::serialize_state` + `restore_from_snapshot` round-trip unit test (S-035 Option 1 seed); 15 assertions covering the snapshot wire format used by `determ snapshot create/inspect/fetch`, SNAPSHOT_RESPONSE gossip, and the S-037/S-038 closure path. Account / stake / registry state preserved; A1 supply counters + invariant holds post-restore; 4 genesis-pinned constants; **`compute_state_root` preserved across round-trip — the central S-033/S-037/S-038 contract**; rejection paths (unsupported version + non-object + legacy minimal snapshot); determinism (same snapshot → same restored state_root, critical for cross-node bootstrap agreement) |
| `test_state_proof_unit.sh` | `Chain::state_proof` v2.2 light-client inclusion-proof primitive unit test (S-035 Option 1 seed); 12 assertions covering proof generation + `crypto::merkle_verify` validation under chain `state_root`; tampering rejection (value_hash + sibling-hash + target_index + cross-proof-swap — the malicious-server defense surface); non-membership (absent key → nullopt; sorted-leaves design contract); determinism + post-mutation re-proof (TRANSFER changes value_hash AND new proof verifies under new state_root). Network-level `tools/test_state_proof.sh` exercises the RPC path; this is the in-process foundation |
| `test_abort_event_apply.sh` | `AbortEvent` apply-side handling — rev.8 Phase-1 suspension slashing unit test (S-035 Option 1 seed); 13 assertions. Phase-1 slashing (stake reduced by SUSPENSION_SLASH=10); S-032 abort_records cache populated; Phase-2 NO-slashing contract (timing-skew on healthy creators don't slash); aborted-without-stake (DOMAIN_INCLUSION — records still tracked); stake exhaustion (no negative); A1 invariant (accumulated_slashed += SUSPENSION_SLASH; live decreases by exactly that). Mirrors registry.cpp's suspension policy; required for BFT-mode safety |
| `test_equivocation_apply.sh` | `EquivocationEvent` apply-side handling — FA6 full equivocation slashing + deregistration unit test (S-035 Option 1 seed); 10 assertions. Full stake forfeiture (entire locked → 0); registry deactivation (inactive_from = b.index+1); robustness on ghost equivocator; A1 invariant; determinism (two chains see same equivocation → same `compute_state_root`). Dual mechanism (forfeit + deregistration) unifies STAKE_INCLUSION and DOMAIN_INCLUSION |
| `test_unstake_deregister_apply.sh` | UNSTAKE + DEREGISTER apply-side handling — stake-lifecycle complement to STAKE (S-035 Option 1 seed); 15 assertions across six blocks. UNSTAKE too-early (stake/balance unchanged, fee REFUNDED, nonce++); DEREGISTER sets `inactive_from = height + derive_delay` + stake `unlock_height = inactive_from + unstake_delay_`; DEREGISTER on non-registrant defensive no-op; UNSTAKE post-unlock_height success (stake reduced, balance += amount); UNSTAKE insufficient locked silently fails with fee refund; A1 invariant (UNSTAKE moves value stake→balance, no net supply change) |
| `test_cross_shard_receipt_apply.sh` | Cross-shard inbound receipt apply unit test — rev.9 B3.4 (S-035 Option 1 seed); 12 assertions covering the inbound credit + dedup-set semantics. Basic credit (`to` account credited by amount); A1 `accumulated_inbound` counter + invariant; **dedup contract** (duplicate `(src_shard, tx_hash)` NOT re-credited — exactly-once-credit guarantee under chain replay); multiple distinct receipts in single block; receipt to non-existent domain creates entry; `inbound_receipt_applied` predicate reflects dedup-set membership; determinism across two chains |
| `test_param_change_apply.sh` | A5 Phase 2 PARAM_CHANGE apply unit test (S-035 Option 1 seed); 16 assertions covering stage → activate-at-effective_height. Staging contract; activation at apply entry; effective_height=0 activates at first non-genesis apply; multi-param same-height (insertion order); multi-height (sequential); unknown name (silent chain no-op + hook still fires for Node-side validator-field forwarding); known param fires hook AND mutates field; determinism across chains |
| `test_subsidy_distribution.sh` | E1/E3/E4 subsidy distribution apply unit test (S-035 Option 1 seed); 17 assertions. FLAT mode (subsidy split equally; dust to creator[0]); LOTTERY mode (jackpot pays subsidy×M when `cumulative_rand[0..7]` BE % M == 0, otherwise 0); E4 finite pool (cap drains to 0); A1 invariant (subsidy mints exactly `subsidy_this_block`); empty-creators no-op (A1-safe) |
| `test_merge_event_apply.sh` | MERGE_EVENT apply unit test — R7 under-quorum-merge state machine (S-035 Option 1 seed); 15 assertions. BEGIN inserts `(shard_id → {partner_id, refugee_region})`; `is_shard_merged` predicate; END removes entry on partner match; wrong-partner END = no-op; partner-ring constraint at BEGIN; single-shard chains no-op; multiple distinct merges tracked; `shards_absorbed_by` inverse-lookup; determinism |
| `test_cross_shard_outbound_apply.sh` | Cross-shard outbound TRANSFER apply (rev.9 B3 source side) unit test (S-035 Option 1 seed); 11 assertions. Sender debited (fee returns via creator); dst NOT credited locally (credit via inbound receipt on dst shard); A1 `accumulated_outbound` += amount; live supply decreases by exactly amount; single-shard fallback (`is_cross_shard` always false); determinism |
| `test_supply_lifecycle.sh` | A1 unitary-supply invariant end-to-end across mixed-tx lifecycle (S-035 Option 1 seed); 17 assertions. genesis → subsidy mint → TRANSFER → STAKE → slash → DEREGISTER → unlock-wait → UNSTAKE → final A1 identity formula. The interaction test catching regressions where individually-correct apply paths don't compose correctly |
| `test_dapp_state_transition.sh` | Full v2.18 DApp registry lifecycle apply unit test (S-035 Option 1 seed); 22 assertions. op=0 create (all fields preserved, `registered_at` set); op=0 update on same domain (fields REPLACED but `registered_at` PRESERVED, `inactive_from` reset); op=1 deactivate (sets `inactive_from = height + DAPP_GRACE_BLOCKS`, entry persists during grace); independent domain coexists; determinism. Complements `test_dapp_register.sh` (network-level mechanics) + `test_dapp_call.sh` (message delivery) |
| `test_overflow_paths.sh` | S-007 overflow-protection apply paths unit test (S-035 Option 1 seed); 10 assertions. TRANSFER receiver overflow + inbound receipt overflow both throw S-007 with diagnostic; Phase-1 rollback contract (height/balance/state_root all byte-identical after throw); boundary at exactly UINT64_MAX is OK (strict `>` check); sanity normal TRANSFER unaffected; A1 invariant preserved across throw rollback. Defends against wrap-to-zero attacks on long-lived balances |
| `test_state_root_namespaces.sh` | Exhaustive 10-namespace `compute_state_root` coverage unit test — S-033 invariant (S-035 Option 1 seed); 12 assertions. Each of the 10 namespaces (a:/s:/r:/d:/i:/b:/m:/p:/k:/k:c:) — mutating via the appropriate apply or setter changes state_root; baseline equality on identical chains; cross-namespace independence (different namespace mutations → distinct roots). Catches forgotten-leaf-emission and wrong-key-encoding regression classes that would silently fork at the verification gate |
| `test_multi_tx_block.sh` | Multi-tx block apply semantics unit test (S-035 Option 1 seed); 18 assertions across six blocks. Two TRANSFERs from same sender with ascending nonces (both apply, ordering preserved); same sender + wrong nonce on second (defensive skip); two different senders in same block (independent nonce counters); interleaved alice→bob→alice; insufficient balance mid-block (silent skip with no nonce++; subsequent tx still applies); A1 invariant after multi-tx. Covers the in-block tx-ordering contract enabling sender-batched operations in a single block |
| `test_state_proof_namespaces.sh` | `Chain::state_proof` verify path across all major state namespaces — a/s/r/b/d (S-035 Option 1 seed); 9 assertions. Per-namespace inclusion + `crypto::merkle_verify` under root for accounts, stakes, registrants, abort_records, DApp registry; cross-namespace independence (5 distinct value_hashes); cross-namespace proof-swap rejection; non-existent key → nullopt; determinism (2 proofs byte-identical). Complements `test_state_proof_unit.sh` (a:-namespace in-depth coverage) |
| `test_applied_receipt_restore.sh` | applied_inbound_receipts dedup-set survives snapshot restore unit test (S-035 Option 1 seed); 10 assertions. Pre-restore baseline (3 distinct receipts credit, dedup-set predicate matches); snapshot → restore preserves balance + dedup-set entries + predicate; **critical contract** — duplicate `(src_shard, tx_hash)` on restored chain silently skipped (exactly-once-credit preserved across fast-sync bootstrap, defeating double-credit attacks); fresh receipt post-restore credits normally; S-033 state_root preserved through restore (i:-namespace included); A1 `accumulated_inbound` + invariant preserved |
| `test_stake_accounting.sh` | Comprehensive stake-state-machine invariants unit test (S-035 Option 1 seed); 12 assertions. Genesis (locked = initial_stake, unlock_height = UINT64_MAX); STAKE adds to locked, preserves unlock_height; slash deducts SUSPENSION_SLASH; DEREGISTER sets unlock_height = inactive_from + unstake_delay; UNSTAKE pre-unlock silent-fail + fee refund; UNSTAKE post-unlock moves locked → balance; A1 conservation — STAKE moves value (no supply change), full STAKE→UNSTAKE round-trip conserves supply. Complements `test_unstake_deregister_apply.sh` (per-tx) with full-lifecycle composition |
| `test_atomic_scope.sh` | A9 Phase 2D nested-scope rollback primitive |
| `test_composable_batch.sh` | COMPOSABLE_BATCH all-or-nothing semantics under partial-failure |
| `test_dapp_register.sh` / `test_dapp_call.sh` / `test_dapp_e2e.sh` | v2.18/v2.19 DApp substrate end-to-end |
| `test_dapp_snapshot.sh` | S-037 + S-038 joint surface: DApp registry survives snapshot bootstrap; producer's `body.state_root` matches receiver's recomputed root post-restore |
| `test_s018_json_validation.sh` | S-018 closure: `json_require<T>` / `json_require_hex` helpers + converted `from_json` paths across `chain/block.cpp` + `node/producer.cpp` + `chain/genesis.cpp` + `net/messages.cpp` + `crypto/keys.cpp` surface clear field-name diagnostics on malformed gossip / RPC / snapshot / keyfile input. 9 assertions cover missing-field, wrong-type, wrong-hex-length cases. |
| `test_account_encrypted.sh` | v2.17 AES-256-GCM keyfile envelope |
| `test_rpc_hmac_auth.sh` | v2.16 / S-001 HMAC-SHA-256 RPC auth |
| `test_mempool_bounds.sh` | S-008 mempool admission + fee-priority eviction |
| `test_rpc_rate_limit.sh` / `test_gossip_rate_limit.sh` | S-014 token-bucket rate-limit (RPC + gossip) |
| `test_chain_integrity.sh` | S-021 chain.json head_hash tampering detection |
| `test_anon_address_case.sh` | S-028 case-insensitive anon-address normalization |
| `test_governance_param_change.sh` | A5 PARAM_CHANGE multisig + whitelist end-to-end |
| `test_under_quorum_merge.sh` | R7 merge_state_ + MERGE_BEGIN/MERGE_END application |

Run the full suite with:

```bash
bash tools/run_all.sh                          # all tests + PASS/FAIL summary
FAST=1 bash tools/run_all.sh                   # in-process subset only (~3s, 5 tests, no flakes)
QUIET=1 bash tools/run_all.sh                  # summary only (no per-test stdout)
ONLY_PATTERN='test_dapp' bash tools/run_all.sh # subset by regex
SKIP_PATTERN='test_equiv' bash tools/run_all.sh # skip known-flaky on a platform
```

`tools/run_all.sh` iterates every `tools/test_*.sh`, captures per-test outcome via the suite's PASS/FAIL marker convention, and exits non-zero if anything failed. Per-test failures don't stop the suite — an operator gets the full failure picture in one run. **`FAST=1`** short-circuits to the deterministic in-process subset (`determ test-*` subcommand wrappers): atomic_scope, composable_batch, dapp_register, dapp_call, s018_json_validation, merkle, committee_selection, shard_routing, ed25519, sha256, anon_address. ~5 seconds total, no clusters, no network, no flakes — useful for dev iteration. The portable `DETERM_BIN` / `DETERM_WALLET_BIN` override hooks (see `tools/common.sh`) flow through automatically. Plain bash loop also still works:

```bash
for t in tools/test_*.sh; do bash "$t"; done
```

Note: multi-node tests share fixed ports (7771-3 / 8771-3 / 779x / 8830 / etc.) and have a documented Windows TIME_WAIT flake on back-to-back runs. In-process tests (state_root, atomic_scope, composable_batch, dapp_register, chain_integrity, s018_json_validation) are deterministic and complete in under 5 seconds each.

**Portability (S-035 Path 3 — shipped).** Every test sources `tools/common.sh` which platform-detects the determ + determ-wallet binaries (Windows MSVC multi-config, Linux/Mac single-config) and resolves `PROJECT_ROOT` to a Windows-style absolute path on Git Bash via `pwd -W`. Override via `DETERM_BIN=/path/to/determ` env var for CI runners with custom build layouts. The 53-test suite is now runnable on Linux/Mac/Windows from the repo root with no per-platform editing.

## Out of scope

Determ v1 is a payment + identity chain. **Not in scope**: smart contracts, EVM/WASM, gas, off-chain storage, oracles, bridges, ZK proofs, on-chain governance. See [`README.md` §15](../README.md) for the explicit non-goals list.
